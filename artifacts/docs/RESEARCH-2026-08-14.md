# Vendor-documentation research, 2026-08-14

Raw adversarially-verified findings for the last never-verified entries and the
macOS rows. **This is the evidence, not the change set** - it is deliberately not
applied wholesale. Each target was researched by one agent, then re-checked by a
second whose only job was to refute it.

Two agents failed on connection errors: the Cursor macOS verify stage, so that
target has research but no adversarial pass and must be treated as single-source;
and the synthesis stage.

Every pages_fetched entry records a real HTTP status. A claim marked `inferred`
was NOT on any page anyone opened, and must not be catalogued as documented.

---

## AIRT-0008 Amazon Q Developer (ex-CodeWhisperer)

*14 pages fetched, 14 at 2xx*

- `200` https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/mcp-ide.html
- `200` https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/firewall.html
- `200` https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/troubleshooting-q-logs.html
- `200` https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/context-project-rules.html
- `200` https://aws.amazon.com/blogs/devops/amazon-q-developer-end-of-support-announcement/
- `200` https://raw.githubusercontent.com/aws/aws-toolkit-vscode/master/packages/core/src/shared/lsp/types.ts
- `200` https://raw.githubusercontent.com/aws/aws-toolkit-vscode/master/packages/core/src/shared/lsp/lspResolver.ts
- `200` https://raw.githubusercontent.com/aws/aws-toolkit-vscode/master/packages/core/src/auth/sso/cache.ts
- `200` https://raw.githubusercontent.com/Amazon-Q-Developer/language-servers/main/app/aws-lsp-codewhisperer-runtimes/webpack.config.js
- `200` https://raw.githubusercontent.com/aws/amazon-q-developer-cli/main/docs/agent-format.md
- `200` https://raw.githubusercontent.com/aws/amazon-q-developer-cli/main/docs/agent-file-locations.md
- `200` https://raw.githubusercontent.com/aws/amazon-q-developer-cli/main/crates/chat-cli/src/util/paths.rs
- `200` https://raw.githubusercontent.com/awslabs/amazonq.nvim/main/language-server/README.md
- `200` https://github.com/Amazon-Q-Developer/language-servers **(redirected)**

### Claims

**[corrected / vendor-documented]** Windows CLI log directory is %TEMP%\qlog\
  - actual: CONFLICT: doc says %TEMP%\qlog\; vendor source says %TEMP%\amazon-q\logs
  - evidence: MY PRIMARY REFUTATION OF THE PRIOR PASS. The prior pass proposed two Windows rows (%TEMP%\qlog\qchat.log, %TEMP%\qlog\mcp.log) resting on troubleshooting-q-logs.html alone. The vendor's own CLI source contradicts the doc on exactly the OS this catalog targets. crates/chat-cli/src/util/paths.rs, logs_dir(): `if #[cfg(unix)] { Ok(runtime_dir()?.join("qlog")) } else if #[cfg(windows)] { Ok(std::env::temp_dir().join("amazon-q").join("logs")) }`. The literal "qlog" appears ONLY in the unix branch; the Windows branch builds %TEMP%\amazon-q\logs. Both sources are vendor. The doc page's Windows row may be a copy-down error from the unix rows. A responder who collects only %TEMP%\qlog\ on Windows risks collecting nothing. Collect BOTH candidate roots and resolve on a live host before the catalog commits to either.
  - source: https://raw.githubusercontent.com/aws/amazon-q-developer-cli/main/crates/chat-cli/src/util/paths.rs

**[corrected / vendor-documented]** Prior pass: 'the concrete Windows expansion of the LSP cache root is NOT stated in the source read and should be confirmed on a live host rather than inferred'
  - actual: %LOCALAPPDATA%\aws\toolkits\language-servers (Windows); ~/.cache/aws/toolkits/language-servers (Linux/macOS)
  - evidence: The prior pass flagged this as unresolvable, but the answer is in the doc comment on the very function it quoted. lspResolver.ts: `/** Gets platform-specific "cache" dir ("$LOCALAPPDATA/aws/…" or "~/.cache/aws/…"). Lazy-calls getCacheDir() to avoid failure on Windows. */ public static defaultDir() { return path.join(fs.getCacheDir(), `aws/toolkits/language-servers`) }`. So the Windows LSP path is vendor-stated, not inferred: %LOCALAPPDATA%\aws\toolkits\language-servers\AmazonQ\<version>\servers\aws-lsp-codewhisperer.js. Under-reporting rather than over-reporting, but it left the catalog's most collectable new path unstated.
  - source: https://raw.githubusercontent.com/aws/aws-toolkit-vscode/master/packages/core/src/shared/lsp/lspResolver.ts

**[corrected / inferred]** Prior pass process: every cited page was fetched and listed
  - actual: three source files were quoted verbatim but absent from pages_fetched
  - evidence: GROUND-RULE-1 DEFECT IN THE PRIOR PASS. It quoted running code from lspResolver.ts (defaultDir), packages/core/src/auth/sso/cache.ts (defaultCacheDir), and crates/chat-cli/src/util/paths.rs (AGENTS_DIR/PROMPTS_DIR/PROFILES_DIR) while listing none of the three in pages_fetched. I fetched all three at 200 and every quoted string was accurate, so this is a citation-hygiene failure, not fabrication. But as submitted those claims were unauditable, and paths.rs is precisely the file that contains the %TEMP%\qlog contradiction above - so the omission hid a refutation.
  - source: https://raw.githubusercontent.com/aws/aws-toolkit-vscode/master/packages/core/src/auth/sso/cache.ts

**[missing-row / vendor-documented]** Three MCP config paths is the complete set
  - actual: .amazonq/default.json
  - evidence: HEADLINE FINDING OF THE PRIOR PASS - I RE-FETCHED AND IT HOLDS. mcp-ide.html verbatim: "When you use the GUI to add an MCP server to Q Developer in the IDE, the configuration is stored in one of two files: At the global scope: ~/.aws/amazonq/default.json / At the local scope: .amazonq/default.json". Then: "However, for legacy reasons, it is also possible to put MCP configuration information in two other locations: At the global scope: ~/.aws/amazonq/mcp.json / At the local scope: .amazonq/mcp.json". Four files; catalog carries three; the missing one is the current-format workspace file. Precedence verbatim: "Q Developer gives precedence to workspace level configurations for MCP servers, their permissions, and the settings stored." So the missing file is also the winning file.
  - source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/mcp-ide.html

**[confirmed / vendor-documented]** Language-server binary is named aws-lsp-codewhisperer-token-binary.js
  - actual: aws-lsp-codewhisperer-token-binary.js
  - evidence: Independently re-fetched both sources. webpack.config.js verbatim: `const nodeJsBearerTokenBundleConfig = { ...baseConfig, entry: { 'aws-lsp-codewhisperer-token-binary': path.join(__dirname, 'src/token-standalone.ts') }, target: 'node' }` with baseConfig output `path.resolve(__dirname, 'build')` and `filename: '[name].js'`. amazonq.nvim README verbatim: "Language Server used with this plugin is available at `./build/aws-lsp-codewhisperer-token-binary.js` file" plus the full path `language-servers/app/aws-lsp-codewhisperer-runtimes/build/aws-lsp-codewhisperer-token-binary.js`. Name is real and vendor-sourced.
  - source: https://raw.githubusercontent.com/Amazon-Q-Developer/language-servers/main/app/aws-lsp-codewhisperer-runtimes/webpack.config.js

**[corrected / vendor-documented]** That binary is what the IDE extension runs (install_method: IDE extension, typical_parent Code.exe / idea)
  - actual: <cachedir>/aws/toolkits/language-servers/AmazonQ/<version>/servers/aws-lsp-codewhisperer.js
  - evidence: Re-fetched types.ts and the quoted block is genuinely present: `lsp = '<cachedir>/aws/toolkits/language-servers/AmazonQ/3.3.0/servers/aws-lsp-codewhisperer.js'`, `node = '<cachedir>/aws/toolkits/language-servers/AmazonQ/3.3.0/servers/node'`, `ui = '<cachedir>/aws/toolkits/language-servers/AmazonQ/3.3.0/clients/amazonq-ui.js'`. No '-token-binary' segment. Prior pass's correction stands. ONE REFINEMENT: it said -token-binary.js is "built from the neovim_q branch". Imprecise - the token-binary entry is declared in the MAIN-branch webpack config I fetched; the nvim README says the plugin uses a "slightly modified server implementation" from the neovim_q branch. The bundle is a mainline standalone artifact; the nvim plugin ships a modified build of it. The operative correction (it is not the VS Code runtime file) is unaffected.
  - source: https://raw.githubusercontent.com/aws/aws-toolkit-vscode/master/packages/core/src/shared/lsp/types.ts

**[missing-row / vendor-documented]** The token binary is the only language-server binary
  - actual: aws-lsp-codewhisperer-iam-binary.js
  - evidence: Confirmed verbatim in the same file I fetched: `const nodeJsIamBundleConfig = { ...baseConfig, entry: { 'aws-lsp-codewhisperer-iam-binary': path.join(__dirname, 'src/iam-standalone.ts') }, target: 'node' }`. Two node-target standalone bundles ship from one build. NOTE the prior pass did not mention that the module exports four configs total - `module.exports = [nodeJsBearerTokenBundleConfig, nodeJsIamBundleConfig, webworkerConfig, mainWebpageConfig]` - but the other two (worker.js, main.js into dist/) are browser/webworker artifacts, so 'two standalone node bundles' is the right framing for an endpoint catalog.
  - source: https://raw.githubusercontent.com/Amazon-Q-Developer/language-servers/main/app/aws-lsp-codewhisperer-runtimes/webpack.config.js

**[corrected / vendor-documented]** The extension bundles node, so the process row treats it as a shared system interpreter
  - actual: <cachedir>/aws/toolkits/language-servers/AmazonQ/<version>/servers/node (or node.exe)
  - evidence: types.ts verbatim: "Path to `node` (or `node.exe`) executable/binary." with the cachedir example. The extension executes its own privately-pathed interpreter, so the image path IS a strong discriminator for the IDE case, inverting the entry's eventlog rationale. Prior pass correct.
  - source: https://raw.githubusercontent.com/aws/aws-toolkit-vscode/master/packages/core/src/shared/lsp/types.ts

**[corrected / inferred]** Sysmon EID 1 selector: CommandLine contains amazon-q-developer
  - actual: CommandLine contains aws-lsp-codewhisperer
  - evidence: SOURCING DOWNGRADED FROM THE PRIOR PASS, which marked this vendor-documented. No vendor page states any Sysmon selector; AWS publishes no detection guidance here. What IS vendor-documented is the set of filenames (aws-lsp-codewhisperer.js, aws-lsp-codewhisperer-token-binary.js, aws-lsp-codewhisperer-iam-binary.js) and that none contains the literal 'amazon-q-developer'. The conclusion that the catalogued selector fires on nothing is sound; the replacement selector is my/its inference from those filenames and must be labelled as such.
  - source: https://raw.githubusercontent.com/aws/aws-toolkit-vscode/master/packages/core/src/shared/lsp/types.ts

**[unverifiable / inferred]** %USERPROFILE%\.vscode\extensions\amazonwebservices.amazon-q-vscode-*
  - actual: on-disk directory layout is not stated by AWS anywhere I opened
  - evidence: SOURCING DOWNGRADED FROM THE PRIOR PASS, which called this 'confirmed / vendor-documented'. AWS documents the marketplace extension identity; the lowercased publisher.name-version on-disk directory convention is Microsoft's VS Code behaviour, not an AWS statement. I did not fetch what-is.html this pass and cannot personally attest the itemName link either. The path is very likely right in practice but it is convention, not vendor documentation - exactly the ~/.windsurf/mcp.json failure mode this catalog was burned by. Confirm on a live host. The prior pass's two incompleteness notes DO survive: the row is os:[windows] only while the entry claims windows/macos/linux, and the EOL blog names four IDE surfaces (verbatim: "across VS Code, JetBrains, Eclipse, and Visual Studio") while the catalog carries an install dir for one.
  - source: https://aws.amazon.com/blogs/devops/amazon-q-developer-end-of-support-announcement/

**[confirmed / vendor-documented]** <repo>/.amazonq/rules/*.md
  - actual: project-root/.amazonq/rules
  - evidence: Re-fetched. context-project-rules.html verbatim: "Project rules are defined in Markdown files in the project's `project-root/.amazonq/rules` folder." and "It must be a Markdown file." Independently corroborated twice more: agent-format.md resources example `"file://.amazonq/rules/**/*.md"`, and paths.rs `pub const RULES_PATTERN: &str = ".amazonq/rules/**/*.md";`. A sibling vendor page third-party-context-project-rules.html uses the same folder.
  - source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/context-project-rules.html

**[confirmed / vendor-documented]** ~/.aws/sso/cache/*.json - plaintext AWS SSO tokens
  - actual: ~/.aws/sso/cache
  - evidence: Corroborated in vendor source that I fetched directly: cache.ts `const defaultCacheDir = () => path.join(fs.getUserHomeDir(), '.aws/sso/cache')`, and `export const getCacheDir = () => DevSettings.instance.get('ssoCacheDirectory', defaultCacheDir())` - so the dir is overridable via the ssoCacheDirectory dev setting. Also independently in the CLI: paths.rs `pub fn mcp_auth_dir(&self) -> Result<PathBuf> { Ok(home_dir(self.os)?.join(".aws").join("sso").join("cache")) }`. I did NOT re-fetch cli-configure-sso.html, so the prior pass's AWS CLI User Guide quote is carried on its authority, not mine - but the path is independently established from source.
  - source: https://raw.githubusercontent.com/aws/aws-toolkit-vscode/master/packages/core/src/auth/sso/cache.ts

**[corrected / inferred]** capabilities.plaintext_credentials: false
  - actual: true
  - evidence: Verdict stands but sourcing corrected: this is an INTERNAL CONSISTENCY defect in the catalog, not something any vendor page states. The entry sets plaintext_credentials false while its own credentials block declares storage: plaintext / secret_type: aws-sso-token. The bearer token is written to a JSON file on disk with no OS keychain involvement per cache.ts. A wrong capability flag drops the entry from any plaintext-credential filter.
  - source: https://raw.githubusercontent.com/aws/aws-toolkit-vscode/master/packages/core/src/auth/sso/cache.ts

**[confirmed / vendor-documented]** ~/.aws/amazonq/mcp.json (global) and <repo>/.amazonq/mcp.json (workspace)
  - actual: ~/.aws/amazonq/mcp.json and .amazonq/mcp.json
  - evidence: Re-fetched mcp-ide.html; both are listed verbatim under "for legacy reasons". Corroborated in agent-format.md useLegacyMcpJson: "(`~/.aws/amazonq/mcp.json` for global and `cwd/.amazonq/mcp.json` for workspace)" and in paths.rs (`MCP_CONFIG = ".amazonq/mcp.json"` workspace, `".aws/amazonq/mcp.json"` global). Paths correct; the catalog does not record that these are LEGACY, gated on useLegacyMcpJson which mcp-ide.html says "By default, this field is set to true."
  - source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/mcp-ide.html

**[confirmed / vendor-documented]** ~/.aws/amazonq/default.json - agent configuration, global scope in the IDE
  - actual: ~/.aws/amazonq/default.json
  - evidence: mcp-ide.html verbatim, twice: "At the global scope: ~/.aws/amazonq/default.json" and "If you select global scope, the MCP server configuration is stored in ~/.aws/amazonq/default.json and available across all your projects."
  - source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/mcp-ide.html

**[confirmed / vendor-documented]** The CLI reads global and workspace files together and takes the union
  - actual: union for the CLI legacy files; precedence for the IDE default.json pair
  - evidence: agent-format.md verbatim: "When set to `true`, the agent will have access to all MCP servers defined in the global and local configurations in addition to those defined in the agent's `mcpServers` field." (The prior pass rendered 'in addition to' in caps as if quoted emphasis - the source is lowercase.) Meanwhile mcp-ide.html gives the IDE pair PRECEDENCE semantics, not union. Two resolution models under one entry; the notes capture one.
  - source: https://raw.githubusercontent.com/aws/amazon-q-developer-cli/main/docs/agent-format.md

**[missing-row / vendor-documented]** MCP config files are the agent's only spawn/persistence surface
  - actual: hooks field in agent JSON (agentSpawn, userPromptSubmit, preToolUse, postToolUse, stop)
  - evidence: Re-fetched agent-format.md; the hooks block is real and documented verbatim. Trigger list: "agentSpawn: Triggered when the agent is initialized.", "userPromptSubmit: Triggered when the user submits a message.", "preToolUse: Triggered before a tool is executed. Can block the tool use.", "postToolUse: Triggered after a tool is executed.", "stop: Triggered when the assistant finishes responding." Each hook takes a required `command` string; the doc's own examples run `git status`, `ls -la`, and shell redirection into /tmp audit logs. Arbitrary local command execution on documented triggers, living in the same JSON as mcpServers. A further doc exists at docs/hooks.md that neither pass has opened.
  - source: https://raw.githubusercontent.com/aws/amazon-q-developer-cli/main/docs/agent-format.md

**[missing-row / vendor-documented]** Entry has no CLI agent-configuration artifacts
  - actual: ~/.aws/amazonq/cli-agents/ and .amazonq/cli-agents/
  - evidence: Re-fetched agent-file-locations.md: local "`.amazonq/cli-agents/`", global "`~/.aws/amazonq/cli-agents/`", and verbatim "If both local and global directories contain agents with the same name, the **local agent takes precedence**." Also "Q CLI will automatically create the global agents directory (`~/.aws/amazonq/cli-agents/`) if it doesn't exist." Confirmed in source - paths.rs `workspace::AGENTS_DIR = ".amazonq/cli-agents"`, `global::AGENTS_DIR = ".aws/amazonq/cli-agents"`. These files carry mcpServers, allowedTools and hooks simultaneously.
  - source: https://raw.githubusercontent.com/aws/amazon-q-developer-cli/main/docs/agent-file-locations.md

**[missing-row / vendor-documented]** Entry carries no log artifacts
  - actual: qlog directory holding chat.log, qchat.log, mcp.log, translate.log
  - evidence: Re-fetched troubleshooting-q-logs.html. Files and sensitivity confirmed verbatim: "Log files may contain sensitive information from your conversations and interactions with Amazon Q, including file paths, code snippets, command outputs, account IDs, and resource names" and "All log files are created automatically when you first use the CLI, even without verbose flags." macOS `$TMPDIR/qlog/`, Linux/WSL `$XDG_RUNTIME_DIR/qlog/` or `$TMPDIR/qlog/` or `/tmp/qlog/`. ONE MISATTRIBUTION IN THE PRIOR PASS: it credited mcp.log with recording "MCP server lifecycle". The page says mcp.log is "Model Context Protocol server logs (populated when using MCP servers)"; the phrase "MCP server lifecycle" belongs to the qchat.log bullet. See the separate Windows-path claim above - the Windows root is disputed by vendor source.
  - source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/troubleshooting-q-logs.html

**[corrected / vendor-documented]** Network egress: q.us-east-1.amazonaws.com
  - actual: https://q.profile-region.amazonaws.com
  - evidence: Re-fetched firewall.html. The allowlist row is region-templated, and the page defines the token verbatim: "`profile-region` with the AWS Region where your Amazon Q Developer profile is installed." A Frankfurt-profile tenant egresses to q.eu-central-1.amazonaws.com and would not match the catalogued indicator.
  - source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/firewall.html

**[corrected / vendor-documented]** Network egress: codewhisperer.*.amazonaws.com
  - actual: https://codewhisperer.us-east-1.amazonaws.com
  - evidence: Confirmed in the re-fetched allowlist table: exactly one codewhisperer row, pinned to us-east-1, no regional token. The catalog wildcards the region on the host the vendor pins and pins the region on the host the vendor templates - the two errors are mirror images.
  - source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/firewall.html

**[corrected / vendor-documented]** Network egress: *.awsapps.com/start
  - actual: idc-directory-id-or-alias.awsapps.com
  - evidence: Confirmed: the table row is a bare host with a substitutable directory id or alias and no /start path. The page defines the token: "`idc-directory-id-or-alias` with your IAM Identity Center instance's directory ID or alias." A path suffix is unmatchable by DNS or TLS SNI telemetry regardless.
  - source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/firewall.html

**[corrected / vendor-documented]** Prior pass: '11 further documented hosts plus 4 S3 buckets' are missing
  - actual: 14 further hosts (17 documented, 3 catalogued) plus 4 buckets
  - evidence: COUNT ERROR IN THE PRIOR PASS. I counted the re-fetched allowlist table: 17 host rows. The catalog carries 3, so 14 are missing - the prior pass's own evidence string enumerates 14 items while its headline says 11. Also a GROUND-RULE-3 VIOLATION: it wildcarded the bucket names as `amazonq-code-scan-{us-east-1,eu-central-1}-*`. The vendor writes them in full - amazonq-code-scan-us-east-1-29121b44f7b, amazonq-code-scan-eu-central-1-9374e402cc5, amazonq-code-transformation-us-east-1-c6160f047e0, amazonq-code-transformation-eu-central-1-a0a89cc2b94 - each with both an https URL and an arn:aws:s3::: ARN. That is the same wildcarding sin it correctly charged the catalog with on codewhisperer.
  - source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/firewall.html

**[unverifiable / inferred]** Network description: HTTPS_PROXY honored
  - actual: not stated on the vendor proxy page
  - evidence: SOURCING DOWNGRADED FROM THE PRIOR PASS, which marked an absent claim 'vendor-documented' - incoherent by construction. I re-read the whole proxy section of firewall.html: no HTTPS_PROXY environment variable anywhere. It documents per-IDE GUI settings (Eclipse "HTTPS Proxy URL", VS Code "Http: Proxy", JetBrains, Visual Studio Toolkit Proxy menu); the only env vars named are NODE_OPTIONS and NODE_EXTRA_CA_CERTS, and those are for certificates on Visual Studio. The page also states verbatim: "SOCKS proxies and Proxy Auto-Configuration (PAC) files are not supported." HTTPS_PROXY may hold for the underlying node runtime but that is convention.
  - source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/firewall.html

**[confirmed / vendor-documented]** IDE plugins EOL 2027-04-30
  - actual: April 30, 2027
  - evidence: Re-fetched the blog. Verbatim: "Amazon Q Developer IDE plugins and paid Subscriptions will reach end of support on April 30, 2027, giving customers 12 months to transition to Kiro." Published 2026-04-30, modified 2026-06-10. All the prior pass's supplementary dates check out verbatim: "New signups will no longer available starting May 15, 2026"; "starting May 29, 2026, Opus 4.6 will no longer be available on Q Developer Pro"; plugins "remain published on all four IDE marketplaces with a deprecation notice" and keep critical bugfixes; "Code transformation is included in this end of support." Scope caveat also verbatim: Console and first-party experiences "are not impacted by this sunset". Endpoint artifacts die 2027-04-30; the service does not.
  - source: https://aws.amazon.com/blogs/devops/amazon-q-developer-end-of-support-announcement/

**[unverifiable / vendor-documented]** CodeWhisperer folded in 2024-04-30
  - actual: April 30, 2024 (not re-verified this pass)
  - evidence: I did not re-fetch service-rename.html, so I cannot personally attest the quote. Carried on the prior pass's authority. The date is consistent with the EOL blog's framing but the blog does not restate it. Low risk, but flagged rather than rubber-stamped.
  - source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/service-rename.html

**[stale-reference / vendor-documented]** Upstream source repo is github.com/aws/language-servers
  - actual: github.com/Amazon-Q-Developer/language-servers
  - evidence: CONFIRMED BY MY OWN FETCH. Requested https://github.com/aws/language-servers, got statusCode 200 with final url https://github.com/Amazon-Q-Developer/language-servers - the repo was transferred to a new GitHub org and GitHub is redirecting. Note the old org path still resolves through the redirect, and awslabs/amazonq.nvim's README still links to github.com/aws/language-servers, so nothing is broken today - but a future pass should record the post-transfer org rather than enshrining the pre-transfer URL.
  - source: https://github.com/Amazon-Q-Developer/language-servers

**[unverifiable / inferred]** Prior pass: two GitHub code-search hits for the binary name were this catalog's own repo, excluded as circular
  - actual: not reproducible in this pass
  - evidence: I ran no GitHub code search and cannot confirm or refute the self-citation claim. It is moot for the conclusion - the binary name is independently established from the vendor webpack config and the awslabs nvim README, neither of which is this catalog. Recording it as unverified so it is not carried forward as an established fact. (Worth noting the checking host's working directory is the catalog repo itself, so circularity is a live hazard for any code-search evidence in this entry.)
  - source: https://raw.githubusercontent.com/awslabs/amazonq.nvim/main/language-server/README.md

**[confirmed / inferred]** Entry confidence: low, never verified
  - actual: low remains correct; do not stamp last_verified
  - evidence: This remains a documentation-and-source pass only. Neither pass touched a host with Amazon Q installed, and two central Windows paths (%TEMP%\qlog vs %TEMP%\amazon-q\logs, and the %LOCALAPPDATA% cache root) can only be settled on a live install. last_verified must not be stamped from either pass.
  - source: https://raw.githubusercontent.com/aws/amazon-q-developer-cli/main/crates/chat-cli/src/util/paths.rs

### Candidate new rows

- `%LOCALAPPDATA%\amazon-q\data.sqlite3` [windows, macos, linux] - MISSED BY BOTH PASSES. paths.rs database_path_static(): dirs::data_local_dir()/amazon-q/data.sqlite3. A SQLite database in the CLI's local data dir - structured, queryable agent state that no config file or log can substitute for. Arguably the single highest-value uncollected artifact in this entry. Root is the platform data-local dir (%LOCALAPPDATA% on Windows, ~/.local/share on Linux, ~/Library/Application Support on macOS per the dirs crate); confirm exact expansion on a live host.
  - source: https://raw.githubusercontent.com/aws/amazon-q-developer-cli/main/crates/chat-cli/src/util/paths.rs
- `~/.aws/amazonq/.cli_bash_history` [windows, macos, linux] - MISSED BY BOTH PASSES. paths.rs global::CLI_BASH_HISTORY = ".aws/amazonq/.cli_bash_history". A shell-history file written by the agent itself - a direct record of commands run through the CLI, independent of the qlog files and not TEMP-resident, so it survives reboot. Hidden (leading dot) inside an already-hidden directory.
  - source: https://raw.githubusercontent.com/aws/amazon-q-developer-cli/main/crates/chat-cli/src/util/paths.rs
- `%TEMP%\amazon-q\logs\ (Windows, per vendor source) vs %TEMP%\qlog\ (Windows, per vendor doc)` [windows] - DISPUTED - COLLECT BOTH. paths.rs logs_dir() builds temp_dir()/amazon-q/logs on the Windows branch and only uses "qlog" on the unix branch, while troubleshooting-q-logs.html tabulates %TEMP%\qlog\ for Windows. Vendor doc and vendor source disagree on the OS this catalog targets first. Same four files either way (chat.log, qchat.log, mcp.log, translate.log). TEMP-resident: collect before reboot.
  - source: https://raw.githubusercontent.com/aws/amazon-q-developer-cli/main/crates/chat-cli/src/util/paths.rs
- `$XDG_RUNTIME_DIR/qlog/ or $TMPDIR/qlog/ or /tmp/qlog/` [macos, linux] - Undisputed on non-Windows - both the doc table and paths.rs runtime_dir() agree (dirs::runtime_dir, then TMPDIR, then the macOS confstr temp dir or std temp_dir). Holds chat.log, qchat.log, mcp.log, translate.log. Vendor warns logs carry conversation content, code snippets and command outputs, and are created automatically with no verbose flag.
  - source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/troubleshooting-q-logs.html
- `.amazonq/default.json` [windows, macos, linux] - The highest-value MCP row and the one the catalog misses. Current (non-legacy) workspace-scope MCP config; vendor states workspace configurations take precedence. Travels with the repo, so it is both a supply-chain vector and the config that wins at runtime.
  - source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/mcp-ide.html
- `%LOCALAPPDATA%\aws\toolkits\language-servers\AmazonQ\<version>\servers\aws-lsp-codewhisperer.js` [windows, macos, linux] - The language server the VS Code extension actually executes - not the catalogued -token-binary.js. Downloaded at runtime into a versioned cache dir, so extension-dir collection alone misses it. Windows root is vendor-stated in lspResolver.ts's doc comment ($LOCALAPPDATA/aws/…; ~/.cache/aws/… elsewhere), which the prior pass wrongly called unstated. Siblings: servers/node (bundled interpreter) and clients/amazonq-ui.js.
  - source: https://raw.githubusercontent.com/aws/aws-toolkit-vscode/master/packages/core/src/shared/lsp/lspResolver.ts
- `~/.aws/amazonq/cli-agents/*.json and .amazonq/cli-agents/*.json` [windows, macos, linux] - CLI agent configs, local taking precedence over global on name conflict. Each file carries mcpServers, allowedTools AND a hooks block whose agentSpawn/userPromptSubmit entries execute arbitrary shell commands on documented triggers - MCP locator, authorisation record and persistence mechanism in one artifact. Workspace copies are repo-portable.
  - source: https://raw.githubusercontent.com/aws/amazon-q-developer-cli/main/docs/agent-file-locations.md
- `~/.aws/amazonq/cli-checkouts/ and ~/.aws/amazonq/knowledge_bases/ and ~/.aws/amazonq/global_context.json` [windows, macos, linux] - MISSED BY BOTH PASSES. paths.rs global constants SHADOW_REPO_DIR, KNOWLEDGE_BASES_DIR, GLOBAL_CONTEXT. cli-checkouts is a shadow repo - agent-held copies of source outside the user's own working tree, a data-retention and exfil-staging question. knowledge_bases holds indexed content the agent retrieved. global_context.json holds persistent cross-session context.
  - source: https://raw.githubusercontent.com/aws/amazon-q-developer-cli/main/crates/chat-cli/src/util/paths.rs
- `.amazonq/cli-todo-lists/ and .amazonq/.subagents/ and .amazonq/prompts/ and ~/.aws/amazonq/prompts/ and ~/.aws/amazonq/profiles/` [windows, macos, linux] - Remaining paths.rs constants. .subagents is dot-prefixed inside .amazonq, so a naive listing of .amazonq/* can skip it depending on the collector's hidden-file handling. profiles/ is the legacy pre-cli-agents directory. prompts/ holds reusable prompt text at both scopes.
  - source: https://raw.githubusercontent.com/aws/amazon-q-developer-cli/main/crates/chat-cli/src/util/paths.rs
- `AmazonQ.md, AGENTS.md, README.md (repo root)` [windows, macos, linux] - MISSED BY BOTH PASSES. paths.rs DEFAULT_AGENT_RESOURCES = ["file://AmazonQ.md", "file://AGENTS.md", "file://README.md"] - agents ingest these repo files as context by DEFAULT, no config required. Repo-portable indirect-prompt-injection surface that needs no .amazonq directory to exist at all, so it is invisible to any collection scoped to .amazonq/.
  - source: https://raw.githubusercontent.com/aws/amazon-q-developer-cli/main/crates/chat-cli/src/util/paths.rs
- `LSP bundle download with conditional hash verification (lspResolver.ts downloadRemoteTargetContent)` [windows, macos, linux] - MISSED BY BOTH PASSES and stronger than merely naming the download host. The extension fetches the LSP bundle from a manifest-supplied URL and verifies sha384 - but the code reads `// Skip hash verification if no hash is provided` / `if (!fetchResult.hash) { return [{ filename, data }] }`. A manifest entry omitting a hash yields an unverified executable bundle written to the cache dir and run by the bundled node. That is the concrete integrity gap under the entry's own T1195.002 mapping.
  - source: https://raw.githubusercontent.com/aws/aws-toolkit-vscode/master/packages/core/src/shared/lsp/lspResolver.ts
- `~/.aws/sso/cache/<sha1>.json and ~/.aws/sso/cache/aws-toolkit-vscode-client-id-<region>[-<sha256>].json` [windows, macos, linux] - Filename structure for the SSO cache, which lets a responder tell the bearer-token file from the client-registration file instead of globbing *.json blindly. cache.ts: token cache file is sha1 of the encoded key plus .json; registration cache file is the aws-toolkit-vscode-client-id- prefix plus region and optional scope hash. Directory is overridable via the ssoCacheDirectory dev setting.
  - source: https://raw.githubusercontent.com/aws/aws-toolkit-vscode/master/packages/core/src/auth/sso/cache.ts
- `aws-toolkit-language-servers.amazonaws.com and aws-language-servers.us-east-1.amazonaws.com` [windows, macos, linux] - Runtime download origin for the language-server bundle (documented purpose: "Amazon Q Developer in the IDE, language processing"). The agent fetches and executes code from here after install, so install-time integrity says nothing about what is running. Pair with the conditional-hash row above.
  - source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/firewall.html
- `amazonq-code-scan-us-east-1-29121b44f7b, amazonq-code-scan-eu-central-1-9374e402cc5, amazonq-code-transformation-us-east-1-c6160f047e0, amazonq-code-transformation-eu-central-1-a0a89cc2b94` [windows, macos, linux] - Where source code physically leaves the host - vendor states Amazon Q uploads artifacts to these AWS service-owned S3 buckets for code reviews and code transformations. Names recorded in full per ground rule 3 (the prior pass wildcarded the hash suffixes). Each has both an https://<bucket>.s3.amazonaws.com/ URL and an arn:aws:s3:::<bucket> ARN. Only the bucket in the profile's Region needs allowlisting.
  - source: https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/firewall.html
- `aws-lsp-codewhisperer-iam-binary.js` [windows, macos, linux] - Second standalone node bundle built alongside the token binary (IAM auth rather than bearer-token auth), from src/iam-standalone.ts into the same build/ dir. Catalog names only the token variant.
  - source: https://raw.githubusercontent.com/Amazon-Q-Developer/language-servers/main/app/aws-lsp-codewhisperer-runtimes/webpack.config.js

### Summary

Adversarial re-verification of AIRT-0008. I re-fetched 14 URLs myself (all 200; one redirect). The prior pass is mostly sound and its headline finding survives, but it has one substantive error, one significant omission, a citation-hygiene failure, a count error, a ground-rule-3 violation, and several sourcing labels inflated to "vendor-documented" that do not earn it.

WHAT SURVIVES. The headline holds: I re-fetched mcp-ide.html and the vendor does document FOUR MCP config files, the catalog carries three, and the missing one (.amazonq/default.json) is both the current non-legacy format and the one the vendor says takes precedence. Also confirmed verbatim against my own fetches: the token-binary and iam-binary webpack entries; the <cachedir>/.../servers/aws-lsp-codewhisperer.js and servers/node paths in types.ts; the qlog file set and the sensitivity/auto-creation warnings; cli-agents at both scopes with local precedence; the hooks block and all five triggers; project-root/.amazonq/rules with the Markdown requirement; ~/.aws/sso/cache; the firewall table (codewhisperer pinned, q templated, awsapps bare, no HTTPS_PROXY anywhere); the EOL dates and the four-IDE scope; and the amazonq.nvim quote, which is verbatim-exact. The aws/language-servers to Amazon-Q-Developer/language-servers transfer is real - my fetch returned 200 with the new org as final url.

WHAT I REFUTE. (1) The Windows log path is wrong or at best disputed. The prior pass proposed two %TEMP%\qlog\ rows on the doc's authority; the vendor's own CLI source contradicts it - paths.rs logs_dir() uses "qlog" only on the unix branch and builds temp_dir()/amazon-q/logs on Windows. Vendor doc vs vendor source, on the OS this catalog targets first. Collect both, resolve on a live host. (2) The prior pass declared the Windows LSP cache root "NOT stated in the source read" - it is stated, in the doc comment of the very function it quoted: "$LOCALAPPDATA/aws/…" or "~/.cache/aws/…". It flagged as unresolvable something already on the page. (3) It quoted running code from three files - lspResolver.ts, cache.ts, paths.rs - that appear nowhere in its pages_fetched. I fetched all three and every quote was accurate, so this is hygiene not fabrication, but the omitted file is exactly the one carrying refutation (1). (4) "11 further documented hosts" is 14 - the table has 17 rows, the catalog has 3, and its own evidence string lists 14. (5) It wildcarded the S3 bucket names as amazonq-code-scan-{...}-*; the vendor writes them in full with hash suffixes, so it committed the same wildcarding error it correctly charged the catalog with on codewhisperer.

SOURCING DOWNGRADES. Four claims labelled vendor-documented do not qualify. The %USERPROFILE%\.vscode\extensions\amazonwebservices.amazon-q-vscode-* path is a Microsoft on-disk convention - AWS documents the marketplace identity, not the directory layout - so it is inferred and unverifiable from AWS sources, which is precisely the ~/.windsurf/mcp.json trap. "HTTPS_PROXY honored" was marked unverifiable AND vendor-documented, which is incoherent for a claim absent from the page. The replacement Sysmon selector is an inference from filenames; AWS publishes no detection guidance. The plaintext_credentials contradiction is an internal catalog defect, not a vendor statement. I also could not reproduce the GitHub code-search self-citation claim and have marked it unverifiable - note the checking host's working directory IS the catalog repo, so circularity is a standing hazard here. I did not re-fetch service-rename.html, so the 2024-04-30 fold-in is carried on the prior pass's authority, not mine, and I have downgraded it accordingly rather than rubber-stamping it.

WHAT BOTH PASSES MISSED. paths.rs is far richer than either pass reported. Uncollected and undocumented in the entry: a SQLite database at <data-local>/amazon-q/data.sqlite3 (probably the highest-value artifact in the whole entry), a .cli_bash_history file recording commands, a cli-checkouts shadow repo holding agent-side copies of source, knowledge_bases/, global_context.json, cli-todo-lists/, a dot-prefixed .subagents/ that naive .amazonq/* globbing can skip, and DEFAULT_AGENT_RESOURCES = AmazonQ.md / AGENTS.md / README.md, which agents ingest by default with no config present at all - an injection surface invisible to any collection scoped to .amazonq/. Separately, lspResolver.ts contains a real supply-chain gap neither pass found: "Skip hash verification if no hash is provided" - a manifest entry omitting a hash yields an unverified executable bundle written to the cache dir and run by the bundled node. That is a sharper T1195.002 finding than naming the download host.

Entry confidence: low is still correct, and last_verified must not be stamped. This remains documentation-and-source only; two central Windows paths can only be settled on a live install.

---

## AIRT-0007 Devin (Cognition) — adversarial re-verification

*25 pages fetched, 19 at 2xx*

- `200` https://docs.devin.ai/cli/enterprise/devin-auth
- `200` https://docs.devin.ai/cli/reference/configuration/config-file
- `200` https://docs.devin.ai/cli/extensibility/mcp/configuration
- `200` https://docs.devin.ai/cli/troubleshooting
- `200` https://docs.devin.ai/cli/enterprise/system-config
- `200` https://docs.devin.ai/desktop/cascade/mcp
- `200` https://docs.devin.ai/api-reference/overview
- `200` https://docs.devin.ai/desktop/enterprise-policies
- `200` https://docs.devin.ai/desktop/getting-started
- `200` https://docs.devin.ai/enterprise/deployment/overview
- `200` https://docs.devin.ai/cli
- `200` https://docs.devin.ai/product-guides/deployment-capabilities
- `200 (93,675 chars as fetched)` https://docs.devin.ai/llms.txt
- `200 (raw curl, no Location header)` https://docs.devin.ai/get-started/devin-intro
- `301 Moved Permanently (raw curl)` https://cognition.com/ **(redirected)**
- `200 (raw curl)` https://cognition.com/
- `301 Moved Permanently (raw curl)` https://cognition.com/ **(redirected)**
- `301 Moved Permanently (raw curl)` https://docs.devin.ai/cli **(redirected)**
- `200 (raw curl)` https://cli.devin.ai/install.sh
- `200 (raw curl)` https://cli.devin.ai/install.ps1
- `200 (raw curl)` https://static.devin.ai/cli/setup.ps1
- `200 (raw curl, no Location header)` https://app.devin.ai/
- `404 Not Found (raw curl on apex; documented base path is /v3/*)` https://api.devin.ai/
- `307 Temporary Redirect (raw curl)` https://docs.devin.ai/desktop/getting-started **(redirected)**
- `308 Permanent Redirect (raw curl) — prior agent recorded this as '200'` https://docs.devin.ai/cli **(redirected)**

### Claims

**[missing-row / vendor-documented]** (prior agent) api.devin.ai — verdict 'unverifiable', sourcing 'inferred'. Prior agent wrote: 'I did not open any vendor page that names api.devin.ai as a host... Do not add this row until someone opens the API reference and reads the base URL off the page.'
  - actual: Add api.devin.ai as a vendor-documented egress row, 443/TCP, base path /v3/organizations/* and /v3/enterprise/*. Add 'cog_' as a documented credential prefix to the credentials block, alongside Personal Access Tokens.
  - evidence: REFUTED — the prior agent's own self-imposed condition is satisfied. I fetched https://docs.devin.ai/api-reference/overview (200) and the base URL is printed on the page twice, verbatim: Organization API — 'Base URL: https://api.devin.ai/v3/organizations/*'; Enterprise API — 'Base URL: https://api.devin.ai/v3/enterprise/*'. A worked curl example on the same page uses 'https://api.devin.ai/v3/organizations/$DEVIN_ORG_ID/sessions' with header 'Authorization: Bearer $DEVIN_API_KEY'. DNS A: 34.210.16.210, 35.83.94.100, 54.212.255.240 (AWS us-west-2, distinct from the 13.33.x CloudFront ranges serving app./cli./static.devin.ai — the prior agent's origin-separation inference was correct). Note the apex https://api.devin.ai/ returns 404, which does NOT refute the row: the documented surface is under /v3/. The page also yields a credential detail the prior agent missed entirely: service user credentials carry a 'cog_' prefix ('Both scopes use service user credentials (cog_ prefix)'), and Personal Access Tokens are a separate documented auth path. 'cog_' is a greppable secret prefix — high-value for secret-scanning and for triaging a leaked token to this vendor.
  - source: https://docs.devin.ai/api-reference/overview

**[corrected / vendor-documented]** (prior agent) The '*.cognition.ai' correction, and specifically its 'Decisive' evidence: 'the string "cognition.ai" appears ZERO times in docs.devin.ai/llms.txt, the vendor's own complete 94,596-character documentation index.'
  - actual: Keep the correction (row should name cognition.com; cognition.ai retained as 301 source and live mail domain), but drop the llms.txt argument and the character count from the evidence. Rest the claim on the observed 301 alone, which is sufficient.
  - evidence: The CONCLUSION survives but the headline REASONING is refuted and must not be carried into the catalog. Verified facts: raw curl on https://cognition.ai/ returns 'HTTP/1.1 301 Moved Permanently' with 'Location: https://cognition.com/'; https://cognition.com/ returns 200. Confirmed. The mail-domain survival is also confirmed — 'support@cognition.ai' appears on cli/troubleshooting and in the nav of every docs page I fetched, and 'enterprise@cognition.ai' appears twice on enterprise/deployment/overview. BUT the 'decisive' llms.txt test is worthless as evidence, and I falsified it by running the complement: in the same file I measured cognition.ai=0 AND cognition.com=0, devinenterprise=0, api.devin.ai=0, devinapps=1, codeium=1. llms.txt is an index of docs.devin.ai page URLs, so essentially no external hostname appears in it. By the prior agent's own logic, zero hits would equally 'prove' cognition.com is not an IOC — while they simultaneously recommend adding cognition.com. The argument is self-defeating. Two further inaccuracies: I measured the file at 93,675 characters, not 94,596; and the parenthetical 'DNS A 76.76.21.21, a Vercel apex shared with devin.ai, devinapps.com and codeium.com' is attached to a passage about cognition.com — cognition.ai does resolve to 76.76.21.21, but cognition.com resolves to 64.239.109.1, a different host entirely.
  - source: https://cognition.ai/

**[corrected / vendor-documented]** (prior agent) mcp block correction, sourced solely to https://docs.devin.ai/cli/reference/configuration/config-file
  - actual: Correction stands, but cite cli/extensibility/mcp/configuration as the primary MCP reference. Flag .devin/mcp_config.local.json as the default add-target. Record MCP OAuth tokens as an on-disk credential artifact with UNDOCUMENTED location.
  - evidence: Every path the prior agent listed is CONFIRMED verbatim on the config-file page: '~/.config/devin/config.json' (User-wide settings), '.devin/config.json' (Project settings, committed), '.devin/config.local.json' (Project local overrides, gitignored), '~/.config/devin/mcp_config.json' (User-wide MCP servers), '.devin/mcp_config.json', '.devin/mcp_config.local.json', and the Windows note 'the user config paths are %APPDATA%\\devin\\config.json and %APPDATA%\\devin\\mcp_config.json ... not ~\\.config\\devin\\'. The v3000.3 / Local 3.6 version split is confirmed. HOWEVER this is the prior agent's own incomplete-but-correct failure: it never opened the dedicated MCP page at https://docs.devin.ai/cli/extensibility/mcp/configuration, which the config-file page links to and which adds four forensically material facts. (1) Default write target: 'By default, servers are saved to local scope (.devin/mcp_config.local.json, gitignored)' — so the gitignored per-project file is where a `devin mcp add` lands unless -s is passed, making it the FIRST place to look, not the last. (2) Devin CLI's own config supports the same secret indirection the prior agent attributed only to the Cascade file: 'Reference it from an environment variable (${env:VAR}), read it from a file (${file:/path})'. (3) MCP OAuth tokens are persisted on disk — 'The OAuth tokens are stored locally and refreshed automatically' and 'logout deletes the persisted tokens for that server' — but the storage path is NOT documented on any page I opened, so it must be recorded as an undocumented-location credential artifact, not guessed. (4) A 'disabled': true flag can neutralise a server while leaving it configured. Also omitted: the config-file page states newer versions 'migrate any mcpServers entries found there automatically on startup', which changes collection strategy.
  - source: https://docs.devin.ai/cli/extensibility/mcp/configuration

**[corrected / vendor-documented]** (prior agent) Devin Desktop disk rows, sourced to desktop/getting-started and desktop/cascade/mcp only
  - actual: Desktop rows stand but are materially incomplete. Add the Windows registry policy key, ADMX/ADML, /etc/windsurf/policies/policy.json, /Applications/Devin.app, and the macOS .mobileconfig profile.
  - evidence: All quoted Desktop paths CONFIRMED verbatim: 'C:\\Program Files\\Windsurf', 'C:\\Users\\[YourUsername]\\AppData\\Local\\Programs\\Windsurf', 'rm -rf ~/.codeium/windsurf', 'C:\\Users\\[YourUsername]\\.codeium\\windsurf', '~/.codeium/windsurf/mcp_config.json', 'windsurf://windsurf-mcp-registry?serverName=<server-name>', the apt/yum repo URLs and key paths, package 'devin-desktop' with 'windsurf remains as a transitional package', and the legacy-Cascade split note verbatim. BUT the prior agent missed an entire vendor page — https://docs.devin.ai/desktop/enterprise-policies (200) — carrying the highest-value Desktop artifacts in the whole entry, including the only Windows REGISTRY artifact anywhere in this research. It documents: Windows registry policy path 'Software\\Policies\\Windsurf\\{ProductName}' (e.g. 'Software\\Policies\\Windsurf\\Windsurf' or 'Software\\Policies\\Windsurf\\WindsurfInsiders'), explicitly contrasted with VS Code's 'Software\\Policies\\Microsoft\\{ProductName}'; ADMX/ADML at 'C:\\Windows\\PolicyDefinitions' and 'C:\\Windows\\PolicyDefinitions\\en-US\\windsurf.adml'; Linux '/etc/windsurf/policies/policy.json', explicitly contrasted with VS Code's '/etc/vscode/policy.json'; and the macOS app bundle, which is the detail that breaks the prior agent's own 'everything is named Windsurf' framing — the vendor says to open '/Applications' and right-click 'Devin.app', then 'Contents/Resources/app/policies'. So macOS uses Devin.app while Windows uses \\Windsurf and Linux uses /etc/windsurf: the naming trap runs BOTH ways, and a sweep keyed only on 'Windsurf' misses the Mac bundle. Separately, the system-config page notes the system.json directories 'are the same machine-wide, administrator-writable directories Devin Desktop uses for system-level rules and hooks' (linking desktop/cascade/memories and desktop/cascade/hooks) — more artifacts in those same dirs that neither agent has enumerated.
  - source: https://docs.devin.ai/desktop/enterprise-policies

**[confirmed / vendor-documented]** (prior agent) 'plaintext_credentials: true; credentials.toml' with macOS/Linux and Windows paths
  - actual: Confirmed exactly as reported. No change.
  - evidence: Re-fetched https://docs.devin.ai/cli/enterprise/devin-auth (200, no redirect) and confirmed the section heading 'Credentials file location' and the text verbatim: 'After devin auth login, Devin CLI stores your API token in credentials.toml. By default, this token is persistent and does not expire. You can copy the credentials file between your own machines to reuse the same authentication, but treat it as sensitive: anyone with this file can authenticate as you.' The platform table reads exactly: macOS / Linux — '$XDG_DATA_HOME/devin/credentials.toml when XDG_DATA_HOME is set; otherwise ~/.local/share/devin/credentials.toml'; Windows — '%APPDATA%\\devin\\credentials.toml (typically C:\\Users\\<you>\\AppData\\Roaming\\devin\\credentials.toml)'. Case and separators as reported by the prior agent are correct. Independently corroborated on cli/troubleshooting: 'Devin CLI API tokens do not expire by default.' This is the strongest finding in the entry and it holds under re-verification.
  - source: https://docs.devin.ai/cli/enterprise/devin-auth

**[confirmed / vendor-documented]** (prior agent) supported_os must become [windows, macos, linux, cloud]; 'Minimal endpoint footprint' is false; local_code_execution: true
  - actual: Confirmed. Add the Homebrew cask artifact and the Desktop-bundled install vector, and soften 'better first-appearance indicators' — the bundled path bypasses both install domains.
  - evidence: Re-fetched https://docs.devin.ai/cli (200). The contrast is verbatim as quoted: 'Devin CLI is a local coding agent that runs directly in your terminal. It works with your local files and environment' vs 'Devin is our cloud-based AI software engineer that runs in a virtual machine.' Install vectors confirmed on the page: 'curl -fsSL https://cli.devin.ai/install.sh | bash', 'brew install --cask devin-cli', 'irm https://static.devin.ai/cli/setup.ps1 | iex', plus the two signed installers devin-updater-x86_64-pc-windows.exe / devin-updater-aarch64-pc-windows.exe. Local execution independently corroborated on cli/troubleshooting: 'At session startup, Devin runs $SHELL as an interactive login shell once and reads exported variables from your shell configuration, such as .bash_profile, .bashrc, .zshrc, .zprofile, or your fish config.' Two additions the prior agent omitted: (a) the Homebrew cask 'devin-cli' is a macOS package-manager artifact (Caskroom receipt, brew list) with no row; (b) a fourth install vector exists — Devin CLI is bundled with Devin Desktop and installed via Command Palette 'Install Devin CLI', gated on the admin toggle 'Show "Install Devin CLI" in the Devin Desktop Command Palette'. That vector leaves no cli.devin.ai/static.devin.ai network trace at all, which undercuts the prior agent's claim that those domains are reliable first-appearance indicators.
  - source: https://docs.devin.ai/cli

**[confirmed / vendor-documented]** (prior agent) '*.devinapps.com' row confirmed verbatim, with the second meaning (<name>.devinapps.com static frontends) suggested as an addition
  - actual: Confirmed verbatim, including the iframe detail and the dual meaning. Add Fly.io as a documented deployment destination for Devin-built backends.
  - evidence: Both halves re-verified. enterprise/deployment/overview (200) contains verbatim: 'End user workstations must be able to reach `*.devinapps.com` over HTTPS (port 443). This domain serves Devin's interactive session tools — including the IDE (VSCode frontend) and Desktop (browser viewer) — which are loaded in the user's browser via iframes.' and 'If your organization uses a corporate proxy or firewall, ensure `*.devinapps.com` is on the allowlist for user workstations.' The Cross-Tenant Communication table confirms Ports 'HTTPS/443' and 'On startup, Devin establishes a secure WebSocket connection to an isolated container in Cognition's tenant'. product-guides/deployment-capabilities (200) confirms the second meaning verbatim: 'Devin uploads the contents of a build directory and serves them as a static site at a unique https://<name>.devinapps.com subdomain', and 'Enterprise organizations: deployment is disabled for all sessions.' One egress destination on that page neither agent recorded: 'FastAPI backends, deployed to Fly.io' — Devin-authored backends leave the Cognition estate entirely and land on Fly.io, which belongs in the egress picture for non-enterprise orgs.
  - source: https://docs.devin.ai/enterprise/deployment/overview

**[confirmed / vendor-documented]** (prior agent) system.json machine-wide policy paths for macOS / Linux / Windows
  - actual: Confirmed exactly, including the Devin/devin case asymmetry across platforms. No change.
  - evidence: Re-fetched https://docs.devin.ai/cli/enterprise/system-config (200). The file-location table reads exactly as the prior agent reported, and I checked case and separators character by character: macOS '/Library/Application Support/Devin/system.json'; Linux '/etc/devin/system.json'; Windows 'C:\\ProgramData\\Devin\\system.json'. Note the case asymmetry is real and correctly transcribed — 'Devin' is capitalised in the macOS and Windows paths but lowercase in the Linux path ('/etc/devin/'). The non-redirectable claim is verbatim: 'The path to system.json cannot be redirected by an environment variable on stable, next, or enterprise builds, so users cannot point the CLI at a policy of their own.' enterprise_host confirmed with the worked example 'acme.devinenterprise.com' and 'Rejects any login whose resulting account belongs to a different host'. devin-updater confirmed verbatim: 'The devin-updater binary reads the same setting, so background updates go through the same proxy as the CLI itself.'
  - source: https://docs.devin.ai/cli/enterprise/system-config

**[confirmed / vendor-documented]** (prior agent) Devin CLI log paths and 48-hour gzip behaviour; ~/.local/bin/devin binary location
  - actual: Confirmed exactly. Optionally add the CHISEL_LOG_* env vars and the 'chisel' / 'windsurf_api_client' internal component strings.
  - evidence: Re-fetched https://docs.devin.ai/cli/troubleshooting (200). Verbatim: 'Logs are also always written to a per-run log file under the CLI's data directory, regardless of these env vars: macOS / Linux: ~/.local/share/devin/cli/logs/devin_<timestamp>_<pid>.log ; Windows: %APPDATA%\\devin\\cli\\logs\\devin_<timestamp>_<pid>.log'. Retention verbatim: 'Log files from finished processes that have been untouched for 48 hours are gzipped at startup to keep the directory small, so older logs are .log.gz.' Sensitivity verbatim: 'Trace-level logs can include sensitive data such as Authorization headers and tokens.' Binary check verbatim: 'Verify the binary exists: ls -la ~/.local/bin/devin (or the install location shown during setup)' — the prior agent correctly preserved the vendor's hedge rather than presenting it as the only location. Worth adding: the same page documents CHISEL_LOG_STDOUT / CHISEL_LOG_STDERR and RUST_LOG targets ('chisel', 'windsurf_api_client', 'connect_rpc'), and the internal component name 'chisel' plus 'windsurf_api_client' are useful string indicators in memory and process artifacts.
  - source: https://docs.devin.ai/cli/troubleshooting

**[confirmed / vendor-documented]** (prior agent) local_listener: conditional — loopback proxy under `devin --sandbox` with domain filtering configured
  - actual: Confirmed. Keep as conditional.
  - evidence: Verbatim on the config-file page, sandbox section: 'When --sandbox is active and domain filtering is configured, a managed network proxy starts on loopback and the sandbox restricts all child traffic to route through it.' The instability caveat the prior agent flagged is also verbatim and appears as a warning callout above it: 'Sandbox network filtering is currently unstable. If you need this feature, please reach out to your account representative for stability timelines.' The prior agent's decision to record this as conditional rather than flatly true is the correct call and I would not upgrade it. Additional context on the same page: enterprise allowlists are authoritative and replace the local allowed_domains, while enterprise denylists are additive — so the effective filter on a managed host is not what the local file says.
  - source: https://docs.devin.ai/cli/reference/configuration/config-file

**[confirmed / vendor-documented]** (prior agent) process row: devin confirmed; add devin-updater and devin-desktop; re-examine 'persistence: None' against background auto-update
  - actual: Confirmed. Scope the auto-update/persistence challenge to self-managed macOS/Linux installs, and add the versioned-install 'current' symlink as an artifact.
  - evidence: 'devin' confirmed on docs.devin.ai/cli: 'enter a project directory and type devin to activate Devin CLI'. 'devin-updater' confirmed on cli/enterprise/system-config verbatim. 'devin-desktop' confirmed as the launcher command on desktop/getting-started: 'Keep the "Install devin-desktop terminal command" option checked' with the example 'devin-desktop ~/Developer/my-project'. The auto-update challenge is well-founded and the config-file page supports it more strongly than the prior agent argued — auto_update defaults true and the mechanism is described as 'new releases are downloaded and activated while Devin CLI runs', with 'a swap of the current symlink only takes effect on the next launch'. That 'current' symlink is itself an unrecorded on-disk artifact indicating a versioned install layout. One scope limit the prior agent did not carry over: the page restricts this to 'background auto-update on macOS and Linux' and 'Only applies to self-managed installations (curl | bash on macOS/Linux). Installations bundled with another product (e.g. Windsurf) ignore this setting and update through their parent application.' So the persistence challenge applies to self-managed macOS/Linux installs, not universally.
  - source: https://docs.devin.ai/cli/enterprise/system-config

**[confirmed / vendor-documented]** (prior agent) references: 'Devin documentation' https://docs.devin.ai/get-started/devin-intro — citation is healthy
  - actual: Confirmed live, 200, no redirect. Cite trailing-slash-free /cli and the resolved /desktop/getting-started.
  - evidence: Raw curl returns 'HTTP/1.1 200 OK' with no Location header. Citation does not move and does not 404. The prior agent's sibling note is confirmed and slightly refined by my own probes: https://docs.devin.ai/desktop returns 307 to /desktop/getting-started (as they said), and https://docs.devin.ai/cli/ returns 308 to /cli — the prior agent recorded that one as status '200' with redirected:true, which is a transcription error in their pages_fetched table (they logged the post-redirect status, not the raw one). Immaterial to the entry, but it is the kind of imprecision the ground rules ask us to record.
  - source: https://docs.devin.ai/get-started/devin-intro

**[missing-row / vendor-documented]** (prior agent) cli.devin.ai and static.devin.ai as missing install/update egress rows
  - actual: Add both rows, 443/TCP. Qualify the first-appearance claim: Homebrew and Desktop-bundled installs bypass both domains.
  - evidence: Confirmed and slightly strengthened. Raw curl: https://cli.devin.ai/ returns 301 to https://docs.devin.ai/cli, but https://cli.devin.ai/install.sh returns 200 and — a check the prior agent asserted from doc text but did not perform — https://cli.devin.ai/install.ps1 also returns 200. https://static.devin.ai/cli/setup.ps1 returns 200. DNS: cli.devin.ai 13.33.82.40/.45/.59/.12; static.devin.ai 13.33.109.113/.109/.95/.36 (CloudFront ranges, rotating). Both hosts are documented on docs.devin.ai/cli and cli/troubleshooting. Caveat that must ride along with this row, and which the prior agent's 'better first-appearance indicators than app.devin.ai' framing overstates: two of the four documented install vectors do not touch either domain — 'brew install --cask devin-cli' pulls from Homebrew infrastructure, and the Devin Desktop Command Palette install ships the binary from an already-installed IDE.
  - source: https://docs.devin.ai/cli

**[missing-row / vendor-documented]** (prior agent) *.devinenterprise.com as a missing enterprise-tenant egress row
  - actual: Add as <tenant>.devinenterprise.com, explicitly flagged as a documented hostname pattern rather than a probed live host.
  - evidence: Confirmed verbatim on cli/enterprise/system-config: the enterprise_host option is documented as 'Devin enterprise host that users must authenticate against, e.g. "acme.devinenterprise.com". Accepts a bare host or a full URL', with the worked JSON example '"enterprise_host": "acme.devinenterprise.com"' and the enforcement text 'Rejects any login whose resulting account belongs to a different host'. The reasoning holds: an estate allowlisting only app.devin.ai and *.devinapps.com will not carry enterprise-tenant CLI auth traffic. Note this is a documented example hostname pattern, not a resolved live host — 'acme' is the vendor's placeholder tenant. The row should be recorded as the pattern <tenant>.devinenterprise.com, and the catalog should not imply the apex was probed. I did not resolve devinenterprise.com.
  - source: https://docs.devin.ai/cli/enterprise/system-config

**[missing-row / vendor-documented]** (prior agent) windsurf-stable.codeiumdata.com as a missing Linux Desktop install/update egress row
  - actual: Add. Also add devin.ai/download/* as the Mac/Windows Desktop download path and windsurf.com (changelog, download-next) as a live third-party-branded dependency.
  - evidence: Confirmed verbatim on desktop/getting-started. apt: 'https://windsurf-stable.codeiumdata.com/wVxQEIWkwPUEAGf3/windsurf.gpg' dearmored to '/etc/apt/keyrings/windsurf-stable.gpg', repo line 'deb [arch=amd64 signed-by=/etc/apt/keyrings/windsurf-stable.gpg] https://windsurf-stable.codeiumdata.com/wVxQEIWkwPUEAGf3/apt stable main' written to '/etc/apt/sources.list.d/windsurf.list'. yum: baseurl 'https://windsurf-stable.codeiumdata.com/wVxQEIWkwPUEAGf3/yum/repo/', gpgkey '.../yum/RPM-GPG-KEY-windsurf', written to '/etc/yum.repos.d/windsurf.repo'. DNS A: 104.18.18.221, 104.18.19.221 (Cloudflare). The naming-trap quote is verbatim: 'The repository URLs still use the pre-rebrand windsurf naming, but the package is now called devin-desktop.' Prior agent missed the Mac/Windows equivalent: those downloads come from devin.ai, not from this host — 'https://devin.ai/download/confirmation?code=ma' (Mac), '?code=wu' (Windows), '?code=li' (Linux tarball), and the prerelease channel is 'https://windsurf.com/editor/download-next'.
  - source: https://docs.devin.ai/desktop/getting-started

**[missing-row / vendor-documented]** (prior agent, catalog-level) Devin Desktop overlaps the existing Windsurf entry — needs a dedup/alias decision
  - actual: Dedup decision still needed. Drop the unverified cognition.com/blog/devin-in-windsurf citation; the rebrand is provable from docs.devin.ai alone.
  - evidence: The underlying facts are confirmed and the flag-rather-than-decide posture is right. Confirmed verbatim on desktop/cascade/mcp: 'The MCP configuration on this page applies to the legacy Cascade agent only. The Devin Local agent — the default agent for new tabs — configures MCP servers in the Devin CLI config files instead.' So one host genuinely carries MCP servers in two disjoint locations depending on which agent drove. Shared identifiers with the existing Windsurf entry are confirmed: '~/.codeium/windsurf/mcp_config.json', 'C:\\Program Files\\Windsurf', the 'windsurf://' scheme, and windsurf.com hosting the changelog and prerelease download. The enterprise-policies page adds three more shared identifiers the prior agent never saw: the 'Software\\Policies\\Windsurf\\{ProductName}' registry root, 'windsurf.admx', and '/etc/windsurf/policies/policy.json'. One correction to the prior agent's framing: I did NOT verify cognition.com/blog/devin-in-windsurf — they cited it for the merge but I did not fetch it, and the rebrand is adequately evidenced by the vendor docs themselves ('the pre-rebrand windsurf naming'), so the blog citation should be dropped rather than carried unverified.
  - source: https://docs.devin.ai/desktop/cascade/mcp

**[corrected / third-party]** (prior agent) DNS A-record assertions used as supporting evidence throughout (e.g. app.devin.ai 13.33.109.119/.66/.26; cognition.ai 76.76.21.21 'a Vercel apex shared with devin.ai, devinapps.com and codeium.com')
  - actual: Do not catalogue specific A records as IOCs. Drop the 'Vercel apex shared with...' assertion; cognition.com is not on 76.76.21.21.
  - evidence: Mostly reproducible but presented with more precision than the data supports, and one attribution is wrong. My resolution: app.devin.ai 13.33.109.66/.111/.119/.26; cli.devin.ai 13.33.82.40/.45/.59/.12; static.devin.ai 13.33.109.113/.109/.95/.36; cognition.ai 76.76.21.21; windsurf-stable.codeiumdata.com 104.18.18.221/104.18.19.221. Sets overlap the prior agent's but are not identical — these are CDN pools that rotate per-resolver and per-query, so any specific A record is a snapshot, not an indicator, and should not be catalogued as one. The wrong attribution: the prior agent's cognition.com passage carries the parenthetical 'DNS A 76.76.21.21, a Vercel apex shared with devin.ai, devinapps.com and codeium.com'. cognition.ai is on 76.76.21.21, but cognition.com resolves to 64.239.109.1 — a different provider entirely. I did not resolve devin.ai, devinapps.com, or codeium.com, so the 'shared apex' assertion is unverified by me and should be dropped. Sourcing downgraded to third-party: DNS is my own observation, not vendor documentation.
  - source: https://docs.devin.ai/enterprise/deployment/overview

### Candidate new rows

- `Software\Policies\Windsurf\{ProductName}  (e.g. Software\Policies\Windsurf\Windsurf, Software\Policies\Windsurf\WindsurfInsiders)` [windows] - Windows Registry Group Policy root for Devin Desktop — the only registry artifact in this entry and entirely absent from the prior research. Vendor explicitly contrasts it with VS Code's Software\Policies\Microsoft\{ProductName}, so a VS Code-shaped sweep finds nothing. Proves MDM management and records enforced settings such as AllowedExtensions and EnableTelemetry that the user cannot override.
  - source: https://docs.devin.ai/desktop/enterprise-policies
- `C:\Windows\PolicyDefinitions\windsurf.admx  and  C:\Windows\PolicyDefinitions\<locale>\windsurf.adml (e.g. en-US)` [windows] - ADMX/ADML policy templates installed to enable the Group Policy above. Their presence dates the point at which Devin Desktop came under central management, independent of whether the policy values are still set.
  - source: https://docs.devin.ai/desktop/enterprise-policies
- `/etc/windsurf/policies/policy.json` [linux] - Linux machine-wide Devin Desktop policy file, root-owned 644. Vendor explicitly distinguishes it from VS Code's /etc/vscode/policy.json. Holds AllowedExtensions and UpdateMode; deleting it reverts to defaults, so absence is not proof it was never managed.
  - source: https://docs.devin.ai/desktop/enterprise-policies
- `/Applications/Devin.app  (policies sample at Contents/Resources/app/policies; profile is a .mobileconfig installed under System Settings > Privacy & Security > Profiles)` [macos] - macOS Devin Desktop bundle. Breaks the 'everything is still named Windsurf' assumption in the opposite direction: the Mac bundle is Devin.app while Windows installs to \Windsurf and Linux policy lives in /etc/windsurf. A sweep keyed only on 'Windsurf' misses the Mac host; a sweep keyed only on 'Devin' misses the Windows and Linux ones. Both name families must be searched on every OS.
  - source: https://docs.devin.ai/desktop/enterprise-policies
- `MCP OAuth tokens for Devin CLI servers — storage location NOT documented on any vendor page opened` [windows, macos, linux] - Vendor states 'The OAuth tokens are stored locally and refreshed automatically' and that 'logout deletes the persisted tokens for that server', but never prints the path. Recorded deliberately as an undocumented-location credential artifact so nobody infers one from convention — this is precisely the failure mode that burned the catalog on Windsurf. Needs a live-host confirmation before any path is written down.
  - source: https://docs.devin.ai/cli/extensibility/mcp/configuration
- `.devin/mcp_config.local.json (default write target for `devin mcp add`, gitignored)` [windows, macos, linux] - Prior research listed this file but buried it among four siblings. Vendor: 'By default, servers are saved to local scope (.devin/mcp_config.local.json, gitignored).' Unless -s/--scope is passed, every MCP server an operator or attacker adds lands here — gitignored, per-project, invisible to repo review. It is the first file to collect, not the last.
  - source: https://docs.devin.ai/cli/extensibility/mcp/configuration
- `https://api.devin.ai/v3/organizations/*  and  https://api.devin.ai/v3/enterprise/*  (egress, 443/TCP)` [cloud] - Vendor-documented API base URLs, which the prior research left as an unverified inference. Distinct AWS origin (34.210.16.210, 35.83.94.100, 54.212.255.240) from the CloudFront hosts serving app./cli./static.devin.ai. Apex returns 404 — hunt the /v3/ paths, not the root.
  - source: https://docs.devin.ai/api-reference/overview
- `Credential prefix 'cog_' (service user keys); Personal Access Tokens as a separate auth path` [windows, macos, linux, cloud] - Greppable secret prefix for this vendor — 'Both scopes use service user credentials (cog_ prefix)'. Lets secret-scanning attribute a leaked token to Devin and lets a responder triage repo/CI/env exposure. Entirely absent from the prior research, which recorded only credentials.toml.
  - source: https://docs.devin.ai/api-reference/overview
- `https://devin.ai/download/confirmation?code=ma | ?code=wu | ?code=li  (egress, Desktop installer download)` [windows, macos, linux] - Mac and Windows Devin Desktop installers come from devin.ai, not from windsurf-stable.codeiumdata.com (which serves only the Linux apt/rpm repos). Prior research covered only the Linux path, leaving the two most common desktop OSes without a documented install indicator.
  - source: https://docs.devin.ai/desktop/getting-started
- `Homebrew cask 'devin-cli' (brew install --cask devin-cli)` [macos] - Fourth documented CLI install vector, with its own on-disk evidence (Caskroom directory, brew receipts) and no traffic to cli.devin.ai or static.devin.ai. Together with the Devin Desktop Command Palette install, it means absence of those two domains in proxy logs does not rule out Devin CLI on the host.
  - source: https://docs.devin.ai/cli
- `Fly.io (egress destination for Devin-deployed FastAPI backends)` [cloud] - Vendor: 'FastAPI backends, deployed to Fly.io', with generated Dockerfile and fly.toml. Code Devin writes can leave the Cognition estate entirely for a third-party host. Disabled for enterprise orgs and under secure mode, so its presence is itself a signal about the tenant's configuration.
  - source: https://docs.devin.ai/product-guides/deployment-capabilities
- `Devin CLI versioned install layout — the 'current' symlink swapped by background auto-update` [macos, linux] - Vendor describes updates as staged to temp paths and promoted by atomic rename, with 'a swap of the current symlink' taking effect on next launch. The symlink and the retained prior version directories are a version-history artifact. Scope: self-managed curl|bash installs on macOS/Linux only — bundled installs update through the parent application.
  - source: https://docs.devin.ai/cli/reference/configuration/config-file
- `System-level Devin Desktop rules and hooks in the same admin-writable dirs as system.json (/Library/Application Support/Devin/, /etc/devin/, C:\ProgramData\Devin\)` [windows, macos, linux] - Vendor notes these 'are the same machine-wide, administrator-writable directories Devin Desktop uses for system-level rules and hooks'. Hooks are executable policy — an admin-writable hook is a code-execution surface neither this pass nor the prior one has enumerated. Flagged for a follow-up fetch of desktop/cascade/hooks and desktop/cascade/memories, which neither agent has opened.
  - source: https://docs.devin.ai/cli/enterprise/system-config

### Summary

Most of the prior agent's work survives — I re-fetched ten of its cited pages and the verbatim quotes check out, including case and separators on every path string. But three findings are wrong or materially incomplete, and one of them inverts a verdict.

REFUTED — api.devin.ai. The prior agent marked it unverifiable/inferred and wrote 'Do not add this row until someone opens the API reference and reads the base URL off the page.' I opened it (200). The page prints 'Base URL: https://api.devin.ai/v3/organizations/*' and 'Base URL: https://api.devin.ai/v3/enterprise/*', plus a worked curl example. This is vendor-documented, not inferred. The prior agent had the right instinct — refusing to catalogue an unopened host — but then declined to spend one fetch settling it. The same page also yields 'cog_', the vendor's service-user credential prefix, a greppable secret indicator absent from the prior research entirely.

REFUTED as reasoning — the llms.txt 'decisive' test on *.cognition.ai. The conclusion stands (cognition.ai does 301 to cognition.com; I reproduced it), but the argument used to support it is self-defeating and must not enter the catalog. The prior agent said 'cognition.ai appears ZERO times in llms.txt.' True. I ran the complement: cognition.com is also zero, as are devinenterprise and api.devin.ai. llms.txt is an index of docs.devin.ai page URLs — essentially no external hostname appears in it. By that logic, zero hits would equally prove cognition.com is not an IOC, while the prior agent recommends adding it. Also: the file is 93,675 chars as I fetched it, not 94,596; and cognition.com resolves to 64.239.109.1, not the 76.76.21.21 Vercel apex the prior agent's parenthetical attaches to it. Rest this row on the observed 301 alone, which is sufficient.

INCOMPLETE-BUT-CORRECT — the prior agent hit the exact failure it was told to hunt for, twice. (1) It sourced the whole MCP correction to the config-file reference page and never opened the dedicated MCP page at cli/extensibility/mcp/configuration, which the config page links to. That page adds the fact that most changes triage: `devin mcp add` writes by default to .devin/mcp_config.local.json — gitignored, per-project — so the quietest place to add a server is the first place to look, not a footnote among four siblings. It also documents that Devin CLI's own config supports ${env:} and ${file:} indirection (the prior agent attributed that only to the Cascade file) and that MCP OAuth tokens persist on disk at a location no vendor page states. I recorded that as an undocumented-location artifact rather than guessing a path. (2) It missed desktop/enterprise-policies entirely — a live vendor page carrying the only Windows registry artifact in the whole entry, 'Software\Policies\Windsurf\{ProductName}', explicitly contrasted against VS Code's Microsoft key, plus windsurf.admx under C:\Windows\PolicyDefinitions and /etc/windsurf/policies/policy.json. That page also breaks the prior agent's own naming-trap framing in the opposite direction: the macOS bundle is /Applications/Devin.app while Windows installs to \Windsurf and Linux policy lives in /etc/windsurf. Both name families have to be swept on every OS.

Two smaller corrections. The prior agent's 'cli.devin.ai and static.devin.ai are better first-appearance indicators than app.devin.ai' overstates the case: two of the four documented install vectors — Homebrew cask devin-cli, and the Devin Desktop Command Palette 'Install Devin CLI' — touch neither domain, so their absence from proxy logs does not clear a host. And its persistence challenge against auto-update is sound but should be scoped: the vendor limits background auto-update to self-managed curl|bash installs on macOS and Linux.

What stands unchanged and is worth the maintainer's confidence: credentials.toml and its exact platform paths, the three system.json paths (including the real Devin/devin case asymmetry across platforms), the log paths and 48-hour gzip retention, the *.devinapps.com quote including the iframe detail, the --sandbox loopback proxy recorded as conditional rather than flatly true, and the healthy state of the catalogued devin-intro citation. The prior agent's core thesis — that 'cloud-only, minimal endpoint footprint' is now false and the entry needs a rewrite rather than a patch — is correct and, with the registry and API rows added, understated. I edited no files.

---

## AIRT-0014 Microsoft Copilot Studio Agents

*14 pages fetched, 14 at 2xx*

- `200` https://learn.microsoft.com/en-us/microsoft-copilot-studio/configure-where-computer-use-runs
- `200` https://learn.microsoft.com/en-us/microsoft-copilot-studio/requirements-quotas
- `200` https://learn.microsoft.com/en-us/microsoft-copilot-studio/computer-use
- `200` https://learn.microsoft.com/en-us/power-automate/desktop-flows/governance
- `200` https://learn.microsoft.com/en-us/troubleshoot/power-platform/power-automate/desktop-flows/binary-caching-issues
- `200` https://learn.microsoft.com/en-us/microsoft-copilot-studio/mcp-add-existing-server-to-agent
- `200` https://learn.microsoft.com/en-us/power-automate/desktop-flows/requirements
- `200` https://learn.microsoft.com/en-us/troubleshoot/power-platform/power-automate/desktop-flows/how-to-get-power-automate-desktop-installer-logs
- `200` https://learn.microsoft.com/en-us/microsoft-copilot-studio/requirements-quotas **(redirected)**
- `200` https://learn.microsoft.com/en-us/power-automate/ip-address-configuration
- `200` https://learn.microsoft.com/en-us/microsoft-copilot-studio/agent-extend-action-mcp
- `200` https://learn.microsoft.com/en-us/azure/sap/microsoft-ai/copilot-studio/architecture-on-premises-data-gateway
- `200` https://www.crowdstrike.com/en-us/blog/falcon-aidr-protects-copilot-studio-agents-and-claude-code/
- `200` https://learn.microsoft.com/en-us/microsoft-copilot-studio/fundamentals-what-is-copilot-studio

### Claims

**[confirmed / vendor-documented]** Prior review: description "Endpoint footprint is browser only" is CORRECTED because computer use BYOM requires Power Automate for desktop on a customer Windows machine
  - actual: Stands, verbatim. I re-fetched the page and every quoted string is present exactly as reported. Browser-only holds for authoring only.
  - evidence: configure-where-computer-use-runs, section "Bring-your-own-machine": "This option lets you use Windows machines that you own and manage." Requirement bullet: "Install Power Automate for desktop version 2.61.132.25266 or later. Include the Power Automate web extension installation for web browser interactions." Register a new machine: "Because computer use runs on the machine, dedicate a machine to computer use to avoid interruptions." and "During installation, make sure you check the Install the machine-runtime app to connect to the Power Automate cloud portal option." All four confirmed word-for-word.
  - source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/configure-where-computer-use-runs

**[corrected / vendor-documented]** Prior review: "Three documented execution locations" — Hosted browser, Cloud PC pool, Bring-your-own-machine
  - actual: Three is undercounted. A FOURTH option — Power Automate hosted machines — is documented in a Note in the BYOM section of the same page, and has its own network-endpoint section elsewhere. The prior review committed the same incomplete-but-correct failure it was auditing: it stopped at the three H2 headings and missed the Note.
  - evidence: configure-where-computer-use-runs, Note under "Register a new machine": "If your environment has Power Automate Hosted Process capacity, create a hosted machine that already has Power Automate for desktop installed and automatically registered to the environment." Corroborated by ip-address-configuration, which carries a dedicated section "Public endpoints for hosted machines and hosted machine groups" (go.microsoft.com, enterpriseregistration.windows.net, device.login.microsoftonline.com, login.microsoftonline.com — "Required for joining machines to Microsoft Entra"). A Microsoft-provisioned, Entra-joined Windows host is a distinct endpoint footprint from BYOM.
  - source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/configure-where-computer-use-runs

**[corrected / inferred]** Prior review: "BYOM is the only GA path for production computer use"
  - actual: Presented as vendor-documented; it is inference. No page states BYOM is GA. The prior review derived it from the absence of a preview banner on the BYOM section. The inference is reasonable but must not be catalogued as documented, and it is weakened by the hosted-machines option above.
  - evidence: configure-where-computer-use-runs carries explicit preview banners on Hosted browser and Cloud PC pool and the note "it's not recommended for production use" on Hosted browser (both confirmed verbatim). It makes no GA/preview statement about Bring-your-own-machine at all. Absence of a banner is not a vendor claim.
  - source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/configure-where-computer-use-runs

**[corrected / vendor-documented]** Prior review: supported_os should include windows — "Windows 10/11 (Home, Pro, Enterprise) or Windows Server 2016/2019/2022/2025; ARM not supported"
  - actual: The OS string is transcribed correctly but is WRONG IN SCOPE for this entry — the subtle error the ground rules warn about. Windows Home cannot run cloud-triggered desktop flows, and Copilot Studio computer use is cloud-triggered. Listing Home as a supported computer-use host is a false positive a responder would act on. Correct scoping: Windows 10/11 Pro or Enterprise, or Windows Server 2016/2019/2022/2025. ARM unsupported.
  - evidence: desktop-flows/requirements: the OS sentence is verbatim as reported, but the very next paragraph reads "If your device runs Windows 10 Home or Windows 11 Home, you can use Power Automate to create desktop flows and monitor them on the Power Automate portal. However, you can't trigger desktop flows from the cloud." The adjacent table makes it explicit: "Runtime | Cloud runtime (attended/unattended) | Windows Home: No | Windows Enterprise/Pro/Server: Yes".
  - source: https://learn.microsoft.com/en-us/power-automate/desktop-flows/requirements

**[unverifiable / inferred]** Prior review: "multi-session not supported for computer use (hosted machine groups aren't supported)"
  - actual: Not on the cited page, and the cited page arguably says the opposite. I could not substantiate this anywhere I fetched. It should be dropped or re-sourced, not catalogued.
  - evidence: desktop-flows/requirements contains a full section titled "Prerequisites for multi-user session enabled Windows operating system (high density)" giving per-session CPU/RAM sizing — i.e. multi-session IS supported for Power Automate for desktop generally. Nothing on that page mentions computer use or hosted machine groups being excluded. The parenthetical appears to be imported from elsewhere without a citation.
  - source: https://learn.microsoft.com/en-us/power-automate/desktop-flows/requirements

**[confirmed / vendor-documented]** Prior review: local_code_execution should flip to true when computer use is configured
  - actual: Stands. Quote verified verbatim on both the configure-where page and the computer-use page.
  - evidence: "Computer use is a tool in Copilot Studio that lets your agent interact with and automate tasks on a Windows computer. It works with websites and desktop apps by selecting buttons, choosing menus, and entering text into fields on the screen... it performs the task on a computer you set up by using a virtual mouse and keyboard." Present on both pages.
  - source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/computer-use

**[confirmed / vendor-documented]** Prior review: risk should be raised from low, citing the maker-provided-credentials Warning on the computer-use page
  - actual: Quote is verbatim and the conclusion stands — but note a process defect: the prior review cited learn.microsoft.com/en-us/microsoft-copilot-studio/computer-use for two separate claims while that URL appears nowhere in its own pages_fetched list. I fetched it; the content checks out, so this is a citation-hygiene failure rather than a fabrication. I also found stronger risk evidence it missed.
  - evidence: computer-use, Warning under "Maker-provided credentials (default)": "If you share an agent with this setting, anyone using it can act with the original author's access on the configured machine." Additional risk material the prior review did not surface — Note under "Access control": "Access control only prevents the model from taking actions on websites or applications that aren't in the allow list. It doesn't stop the model from opening them." That is a documented containment gap, not just a sharing risk.
  - source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/computer-use

**[corrected / vendor-documented]** Prior review: plaintext_credentials: false "holds", evidenced by "Maker-provided credentials... End user credentials"
  - actual: The verdict holds but the evidence is the wrong section, and the section that actually governs credentials was missed entirely. computer-use has a "Stored credentials" section where a maker types usernames and passwords into the tool. They are not written to the endpoint in plaintext, so the flag survives — but there are TWO documented secret stores, and the second is customer-controlled Azure Key Vault, which the entry's secrets_location does not mention.
  - evidence: computer-use, "Stored credentials": "Define the credentials that computer use uses to sign in to websites and applications... Passwords are either stored in Power Platform internal storage (no configuration required) or in an Azure Key Vault that you provide." Internal storage option: "Power Platform encrypts and stores secrets internally." Azure Key Vault option requires "the subscription ID, resource group name, and key vault name" and that "the Azure subscription that has the vault must have the PowerPlatform resource provider registered."
  - source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/computer-use

**[corrected / vendor-documented]** Prior review: the requirements-quotas "Required services" table supplies the missing network indicators — 9 domains listed
  - actual: All nine domains, their Required flags and their protocols are confirmed verbatim. But two defects. (1) copilotstudio.microsoft.com is NOT in that table and is not on that page at all — it is mis-cited. (2) More seriously, the prior review quoted the table and missed the sentence immediately above it stating this list is not sufficient on its own. That is precisely the incomplete-but-correct failure it was hired to find.
  - evidence: requirements-quotas, "Required services", sentence directly preceding the table: "To use Copilot Studio and Power Automate together, configure all required services for Power Automate, in addition to the services listed for Copilot Studio." Since computer use runs on Power Automate for desktop, any BYOM host is squarely in that scope. Table itself confirmed: *.directline.botframework.com (Yes/HTTPS), *.directline.botframework.com (Yes/WS), pipe.aria.microsoft.com (No/HTTPS), *.powerva.microsoft.com (Yes/HTTPS), *.analysis.windows.net (Yes/HTTPS), bot-framework.azureedge.net (Yes/HTTPS), pa-guided.azureedge.net (No/HTTPS), cci-prod-botdesigner.azureedge.net (Yes/HTTPS), token.botframework.com (Yes¹/HTTPS, "¹ Only required when doing manual authentication").
  - source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/requirements-quotas

**[confirmed / inferred]** Prior review: GCC domains (gcc.powerva.microsoft.us etc.) flagged as docs-search excerpt, not opened — verify before cataloguing
  - actual: Correctly self-flagged and correctly excluded. I did not open a page carrying that GCC list either, so it remains uncatalogued. Note that sovereign-cloud coverage is available from a page that WAS opened this pass — ip-address-configuration enumerates US Government and 21Vianet endpoints for the desktop flows runtime with full domain lists.
  - evidence: No fetched page in either pass contains gcc.powerva.microsoft.us or high.powerva.microsoft.us. ip-address-configuration does contain vetted sovereign lists, e.g. "*.servicebus.usgovcloudapi.net", "*.gateway.gov.island.powerapps.us", "*.gateway.gov.island.appsplatform.us", "tb.events.data.microsoft.com", "*.gateway.mooncake.island.powerapps.cn".
  - source: https://learn.microsoft.com/en-us/power-automate/ip-address-configuration

**[corrected / vendor-documented]** Prior review: local_listener "unverifiable — no page I opened states the machine runtime's inbound/outbound socket posture"
  - actual: The self-assessment was honest but the conclusion is wrong: a vendor page does bear on the socket posture, and the prior review never opened it. Microsoft documents machine connectivity as an outbound Service Bus Relay, which supports rather than merely fails to refute local_listener: false. A netstat check is still the right confirmation, but the flag is no longer evidence-free.
  - evidence: ip-address-configuration, "Public endpoints for desktop flows runtime": "*.servicebus.windows.net | https | Listens on Service Bus Relay over TCP. Needed for machine connectivity." and "*.gateway.prod.island.powerapps.com | https | Needed for machine connectivity." Service Bus Relay is a cloud-side rendezvous reached outbound; the phrasing is ambiguous enough that I would not upgrade the flag to confirmed on documentation alone, but it is materially more than nothing.
  - source: https://learn.microsoft.com/en-us/power-automate/ip-address-configuration

**[corrected / vendor-documented]** Prior review: HKLM\SOFTWARE\WOW6432Node\Microsoft\Power Automate Desktop\Registration is "the highest-value registry key in the set", with AllowedRegistrationTenants / AllowRegisteringOutsideOfAADJoinedTenant / AllowTenantSwitching
  - actual: The key path, all three value names and all three types are confirmed exactly. But "the set" is wrong: the prior review catalogued 2 registry key paths from a page that documents at least SIX distinct ones, and omitted several with direct forensic value. Calling one of two the highest-value in a set of six is the same incomplete-but-correct error under audit.
  - evidence: governance confirms verbatim: HKLM \ SOFTWARE\WOW6432Node\Microsoft\Power Automate Desktop\Registration \ AllowedRegistrationTenants (String), AllowRegisteringOutsideOfAADJoinedTenant (DWORD), AllowTenantSwitching (DWORD). MISSED key paths on the same page: HKLM\SOFTWARE\Microsoft\Power Automate Desktop (holds Cloud — sovereign region 0-6; EnableOrganizationPicker — String containing organizationList of tenant GUIDs, a second cross-tenant indicator; ProxyServer; ProxyBypassList; ProxyNetworkCredentialsKey — String naming a Windows Credential Manager entry; DisableUNCPaths; ConfigureExternalRuns; EnableDesignerExecutionLogs); HKCU\SOFTWARE\Microsoft\Power Automate Desktop (holds DisableAskBeforeRunningASharedFlow — "Power Automate for desktop doesn't display a dialog to confirm running a shared flow, when the author is not trusted", a direct security-check bypass; UseVerboseLogging; NotificationsType); HKLM\SOFTWARE\WOW6432Node\Microsoft\Power Automate Desktop\LogShipper (AllowOptionalDataCollection); HKLM\SOFTWARE\Wow6432Node\Microsoft\Power Automate Desktop\Global (UseUIAccessAutomationServer — UI Access for automating elevated-rights applications, v2.68+).
  - source: https://learn.microsoft.com/en-us/power-automate/desktop-flows/governance

**[confirmed / vendor-documented]** Prior review: HKLM\SOFTWARE\Microsoft\Power Automate Desktop\Global holds the evidence-suppression and video-log switches
  - actual: Confirmed. Every value name is exact: DisableScreenshotCaptureOnError, DisableFlowExecutionActionLogging, DisableRunFilesCleanup, DisableCloudConnectors, VideoLoggerModeOverride, VideoLoggerDisableSubtitles, VideoLogDurationInSeconds, VideoLogOutputPath — all documented under that key with the stated types. The anti-forensics reading is sound. One transcription caveat worth recording for a collector author.
  - evidence: governance confirms each. Caveat: Microsoft's own page is internally inconsistent on case — it writes WOW6432Node for the Registration and LogShipper keys but Wow6432Node for the UI Access key. Registry lookups are case-insensitive so this is cosmetic, but per the catalog's exact-transcription rule both spellings should be recorded as they appear. Also note VideoLoggerDisableSubtitles is documented with inverted-sounding values ("0: Disable video subtitles, 1: Enable video subtitles").
  - source: https://learn.microsoft.com/en-us/power-automate/desktop-flows/governance

**[corrected / vendor-documented]** Prior review: %LOCALAPPDATA%\Microsoft\Power Automate Desktop\Designer\Scripts\<scriptId>\Runs\<runId>\Actions.log is "the cloud-driven execution trail — what a Copilot Studio agent caused to run on this host"
  - actual: Path and registry gate are transcribed exactly right, but the characterisation is backwards. This tree is for DESIGNER-triggered runs — a maker manually running a flow in the Designer UI. It is not the path a cloud-triggered Copilot Studio agent writes to. The prior review's own row #2 correctly identifies the cloud-triggered tree as the sibling \Scripts. Presenting the Designer tree as agent evidence would mislead a responder.
  - evidence: governance, "Configure the generation of desktop flow action logs on the local machine for designer-based runs": the feature "configure[s] whether local Power Automate Desktop Designer runs store their action logs on the local machine", gated on HKLM\SOFTWARE\Microsoft\Power Automate Desktop\EnableDesignerExecutionLogs (DWORD), value 1 giving "%LOCALAPPDATA%\Microsoft\Power Automate Desktop\Designer\Scripts\<scriptId>\Runs\<runId>\Actions.log". The no-auto-cleanup note is confirmed: "This feature doesn't automatically clean up the logs generated during designer runs."
  - source: https://learn.microsoft.com/en-us/power-automate/desktop-flows/governance

**[corrected / vendor-documented]** Prior review: %LOCALAPPDATA%\Microsoft\Power Automate Desktop\Cache\(MSI or MSIX)\Account and msalcache.bin3 as an MSAL credential artifact
  - actual: Substance confirmed, but the path string as written will not resolve. "(MSI or MSIX)" is Microsoft's prose placeholder meaning either Cache\MSI or Cache\MSIX depending on install type — it is not a literal directory name. A collector fed that string verbatim finds nothing. The row must be split into two concrete paths.
  - evidence: binary-caching-issues, under "Under %LOCALAPPDATA%\Microsoft\Power Automate Desktop:" the bullet reads "Cache\(MSI or MSIX): Safe to remove all folders except Account and the msalcache.bin3 file." and "If you delete the Account folder and msalcache.bin3 file, you're signed out of Power Automate Desktop." Both confirmed verbatim; the parenthetical is disjunctive prose.
  - source: https://learn.microsoft.com/en-us/troubleshoot/power-platform/power-automate/desktop-flows/binary-caching-issues

**[corrected / vendor-documented]** Prior review: C:\Windows\ServiceProfiles\UIFlowService\AppData\Local\Cache plus sibling \Users covers the service-account cache
  - actual: Both paths confirmed exactly, and the custom-account relocation to %LOCALAPPDATA%\Cache is confirmed. But a THIRD cache path under the same service profile was missed, and it is the one that matters on newer builds. Two further high-value paths on the same page were also omitted, including a code-delivery path.
  - evidence: binary-caching-issues, "Under C:\Windows\ServiceProfiles\UIFlowService\AppData\Local:" lists "Microsoft\Power Automate Desktop\Cache: Depending on your Power Automate Desktop version, some files might be cached here instead of the locations that are listed earlier" — a third path, missed. Also missed under %LOCALAPPDATA%\Microsoft\Power Automate Desktop: "Console\Scripts: Stores execution logs and cached data for flows that are triggered from Power Automate Desktop Console" and — most significant for DFIR — "DesktopFlowModules: Contains custom modules downloaded from the Asset Library. Missing modules are downloaded again at runtime." That is arbitrary custom code pulled onto the host at runtime and it has no row.
  - source: https://learn.microsoft.com/en-us/troubleshoot/power-platform/power-automate/desktop-flows/binary-caching-issues

**[corrected / vendor-documented]** Prior review: %ProgramData%\Microsoft\Power Automate\Logs and %TEMP%\Power Automate for desktop_*.log from KB 4555577
  - actual: Paths and the admin-rights note are correct in substance. Two defects: the page writes the variable lowercase as %programdata%, not %ProgramData% (cosmetic on Windows, but the catalog's rule is exact transcription); and the page documents a THIRD log source — Event Viewer — that the prior review did not extract despite citing the page.
  - evidence: how-to-get-power-automate-desktop-installer-logs, Summary: "installer logs for failed installations (found in the %temp% folder), on-premises product logs for machine registration and runtime issues (found in %programdata%\Microsoft\Power Automate\Logs), and Event Viewer application logs for crashes or service failures." Both variables appear lowercase throughout. Installer filenames confirmed: "Power Automate for desktop_*.log" and "Power Automate for desktop_*_*_MicrosoftFlowRPA.log", "found in the %temp% folder of the user who performed the installation". Admin note confirmed: "You need administrator privileges to read or copy these files." MISSED section "Event Viewer logs": "Go to Windows Logs > Application, and look for an error with the source corresponding to the application that experienced the problem."
  - source: https://learn.microsoft.com/en-us/troubleshoot/power-platform/power-automate/desktop-flows/how-to-get-power-automate-desktop-installer-logs

**[confirmed / vendor-documented]** Prior review: abuse_potential should be corrected — BYOM endpoint holds per-action logs, error screenshots and v2.66+ video with subtitles
  - actual: Registry names, types and the 2.66 version gate all confirmed. The reasoning that VideoLogOutputPath defeats a static collector target is sound. One scope caveat: the vendor documents these as Power Automate for desktop flow-execution features generally; no page I opened states they capture computer-use runs specifically. The inference is strong but should be labelled.
  - evidence: governance, "Video logs" Note: "The video logs registry entries apply to Power Automate desktop version 2.66 and later." VideoLogOutputPath (String) value: "Custom path where videos and subtitles will be saved". DisableScreenshotCaptureOnError confirmed: "prevent Power Automate for desktop from taking a screenshot for the action logs when an error occurs during a flow run". KeepRunDefinitionFilesCopy confirmed: "creates a copy of the RunDefinition.json file, preventing the local flow run details from getting cleaned up". DisableRunFilesCleanup confirmed: "Flow run action details stored in the Actions.log file aren't deleted from the local disk after the run is completed."
  - source: https://learn.microsoft.com/en-us/power-automate/desktop-flows/governance

**[confirmed / vendor-documented]** Prior review: counter-finding that "Desktop flows aren't supported as a tool in Microsoft Copilot Studio" — do not add desktop-flow-as-tool rows
  - actual: Confirmed verbatim under Known issues and limitations. This counter-finding is correct and valuable, and it does not conflict with the computer-use finding: the endpoint is reached via the computer use tool running on a registered machine, not via desktop flows exposed as agent tools.
  - evidence: desktop-flows/requirements, final bullet of "Known issues and limitations": "Desktop flows aren't supported as a tool in Microsoft Copilot Studio."
  - source: https://learn.microsoft.com/en-us/power-automate/desktop-flows/requirements

**[confirmed / vendor-documented]** Prior review: mcp_capable true — "Copilot Studio currently supports MCP tools and resources" and "You must turn on generative orchestration to use MCP"
  - actual: Both quotes verbatim. Prompts are listed as an MCP concept but excluded from support, as the prior review stated.
  - evidence: agent-extend-action-mcp lists Resources, Tools and Prompts, then Note: "Copilot Studio currently supports MCP tools and resources." Separate Note: "You must turn on generative orchestration to use MCP."
  - source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/agent-extend-action-mcp

**[corrected / vendor-documented]** Prior review: mcp[0] confirmed via mcp-add-existing-server-to-agent, but "fetched only as a docs-search excerpt, not opened with firecrawl, so treat the exact wording as one step removed"
  - actual: I opened the page. The quoted sentence is verbatim, so the wording caveat can be lifted — the prior review was right to flag it and right on substance. But the block is incomplete in a way that matters: there are THREE documented routes to attach an MCP server, not two, and the third bypasses Copilot Studio entirely. Transport constraints and a concrete detection string were also missed.
  - evidence: mcp-add-existing-server-to-agent, "MCP servers and data policies": "Access to MCP servers in Copilot Studio relies on Power Platform connectors for connectivity. This condition means that if a data policy regulates Power Platform connectors, it also regulates access to the MCP server and its tools for your agent." — verbatim. Heading "Option 2: Create a custom MCP connector in Power Apps" — verbatim. MISSED third route, stated in the intro: "you can register your existing MCP server in Agents 365 using the Agents 365 CLI and Microsoft 365 Admin Center. Once the server is properly registered and approved, it becomes available for use in Copilot Studio." MISSED transport constraint: "Currently, Copilot Studio supports the Streamable transport type" and "Copilot Studio no longer supports SSE for MCP after August 2025." MISSED detection string: the sample connector schema carries "x-ms-agentic-protocol: mcp-streamable-1.0", which identifies an MCP custom connector in a Power Platform solution export. MISSED secrets detail: the wizard collects API key (header or query) or OAuth 2.0 Client ID and Client secret.
  - source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/mcp-add-existing-server-to-agent

**[corrected / vendor-documented]** Prior review: CrowdStrike AIDR claim is "unverifiable / inferred — no fetch performed against any CrowdStrike source"
  - actual: Now verified. I fetched CrowdStrike's own announcement (HTTP 200). The claim is substantiable and should be kept with a reference rather than dropped. The mechanism is worth cataloguing because it is a real, non-endpoint telemetry source for exactly this entry.
  - evidence: CrowdStrike blog, 30 July 2026: "Falcon AIDR plugs into Copilot Studio as an external threat detection provider. Before an agent runs a tool, Falcon AIDR checks the tool name and its input parameters against the organization's policy and returns an allow or block decision inside Copilot Studio's response window." And: "Checks are recorded on the Falcon AIDR Findings page, ready to correlate with the rest of the organization's telemetry in CrowdStrike Falcon Next-Gen SIEM." Sourcing is vendor-documented but the vendor is CrowdStrike, not Microsoft — label accordingly.
  - source: https://www.crowdstrike.com/en-us/blog/falcon-aidr-protects-copilot-studio-agents-and-claude-code/

**[confirmed / vendor-documented]** Prior review: on-premises data gateway is a second independent endpoint footprint
  - actual: All three quotes verbatim and the conclusion stands. Two caveats the prior review understated: the page sits in the Azure SAP docset and frames the gateway as SAP integration, so it is thin support for a general Copilot Studio gateway claim; and the row supplies no collectible path, so it is a scoping note rather than an artifact row. The prior review did say "needs its own path pass", which is the right call.
  - evidence: architecture-on-premises-data-gateway: "Like the SAP Cloud Connector, the on-premises data gateway enables access to the SAP system from Power Platform and Microsoft Copilot Studio, even if firewalls are in place." Install target: "install the on-premises data gateway on a Windows server that has access to your SAP system." Environment binding: "During the installation, you have to log in as a user from your Power Platform environment to establish the trust and connection with this environment." Also confirmed: SAP Connector for Microsoft .NET is a second required install. Independent corroboration that the gateway is in scope for desktop flows generally appears in ip-address-configuration, where the CRL endpoints are annotated "Needed when connecting through the on-premises data gateway."
  - source: https://learn.microsoft.com/en-us/azure/sap/microsoft-ai/copilot-studio/architecture-on-premises-data-gateway

**[confirmed / vendor-documented]** Prior review: the legacy /en-us/power-virtual-agents/* namespace 301s into /en-us/microsoft-copilot-studio/*
  - actual: Reproduced. Fetching the power-virtual-agents URL returns 200 with a final URL in the microsoft-copilot-studio namespace.
  - evidence: Requested https://learn.microsoft.com/en-us/power-virtual-agents/requirements-quotas; scraper reports sourceURL as requested and resolved url https://learn.microsoft.com/en-us/microsoft-copilot-studio/requirements-quotas, statusCode 200. Page title "Quotas and limits - Microsoft Copilot Studio".
  - source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/requirements-quotas

**[confirmed / vendor-documented]** Prior review: both catalogued reference URLs return 200 with no redirect, refreshed 08/03/2026
  - actual: Partially re-verified. I fetched fundamentals-what-is-copilot-studio: 200, no redirect, updated_at 2026-08-03. I did NOT re-fetch the docset root https://learn.microsoft.com/en-us/microsoft-copilot-studio/ this pass, so that half rests on the prior review alone. The overview page is also the likely origin of the browser-only claim.
  - evidence: fundamentals-what-is-copilot-studio returned statusCode 200, final URL identical, metadata updated_at 2026-08-03T14:59:00Z, ms.date 2026-08-03. Page summary confirms the framing that produced the original error: Copilot Studio "requires a modern web browser and is accessible as a web app" — true of authoring, silent on computer use.
  - source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/fundamentals-what-is-copilot-studio

**[confirmed / vendor-documented]** Prior review overall verdict: entry is "vacuous, not correct"; downgrade confidence from high to medium
  - actual: The core verdict survives the adversarial pass intact. The entry's premise is refuted by Microsoft's own documentation, and confidence: high over zero artifact rows is not supportable. My corrections tighten scope and add missing rows; none of them rescue the original entry.
  - evidence: Confirmed across configure-where-computer-use-runs (a customer-managed Windows host is a documented execution location), desktop-flows/requirements (2 GB storage, .NET Framework 4.7.2, TLS 1.2, admin rights to install via MSI), governance (six registry key paths), binary-caching-issues (multiple log and cache trees plus a runtime module-download path) and how-to-get-power-automate-desktop-installer-logs (three log sources including Event Viewer).
  - source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/configure-where-computer-use-runs

### Candidate new rows

- `%LOCALAPPDATA%\Microsoft\Power Automate Desktop\DesktopFlowModules` [windows] - Highest-value row the prior pass missed. Vendor: "Contains custom modules downloaded from the Asset Library. Missing modules are downloaded again at runtime." This is arbitrary tenant-supplied code fetched onto the endpoint at execution time — a code-delivery path onto a computer-use host. Collect and hash the contents; re-download on next run means absence here is not proof of absence historically.
  - source: https://learn.microsoft.com/en-us/troubleshoot/power-platform/power-automate/desktop-flows/binary-caching-issues
- `%LOCALAPPDATA%\Microsoft\Power Automate Desktop\Scripts` [windows] - Confirmed verbatim: "Stores execution logs and cached data for cloud-triggered desktop flows." This is the cloud-triggered tree — the one a Copilot Studio agent run reaches. Siblings Console\Scripts (console-triggered) and Designer\Scripts (designer-triggered) are DIFFERENT provenance and must not be conflated: only this one indicates cloud-initiated activity.
  - source: https://learn.microsoft.com/en-us/troubleshoot/power-platform/power-automate/desktop-flows/binary-caching-issues
- `%LOCALAPPDATA%\Microsoft\Power Automate Desktop\Cache\MSI\Account  (or  Cache\MSIX\Account)  and  msalcache.bin3` [windows] - Corrected transcription of the prior pass's row — the doc's "(MSI or MSIX)" is disjunctive prose, not a directory, and the literal string resolves to nothing. Pick the branch matching the install type. MSAL token cache and signed-in account state: vendor states deleting these signs the machine out. Record existence, timestamps and install branch only; do not read contents.
  - source: https://learn.microsoft.com/en-us/troubleshoot/power-platform/power-automate/desktop-flows/binary-caching-issues
- `C:\Windows\ServiceProfiles\UIFlowService\AppData\Local\Cache  and  ...\AppData\Local\Users  and  ...\AppData\Local\Microsoft\Power Automate Desktop\Cache` [windows] - Three paths, not two — the third was missed and is version-dependent: "Depending on your Power Automate Desktop version, some files might be cached here instead of the locations that are listed earlier." Applies to the default service identity NT SERVICE\UIFlowService. Under a custom service account these relocate to %LOCALAPPDATA%\Cache (i.e. C:\Users\<Username>\AppData\Local\Cache), so resolve the account from the machine runtime Troubleshoot page before collecting.
  - source: https://learn.microsoft.com/en-us/troubleshoot/power-platform/power-automate/desktop-flows/binary-caching-issues
- `HKEY_LOCAL_MACHINE\SOFTWARE\WOW6432Node\Microsoft\Power Automate Desktop\Registration` [windows] - Cross-tenant registration control. AllowedRegistrationTenants (String, comma-separated tenant GUIDs), AllowRegisteringOutsideOfAADJoinedTenant (DWORD, 1 = any tenant), AllowTenantSwitching (DWORD). A host registered to a tenant it is not Entra-joined to is a cross-tenant agent foothold. Pair with HKLM\SOFTWARE\Microsoft\Power Automate Desktop\EnableOrganizationPicker (String) whose organizationList value also carries tenant GUIDs — the prior pass missed that second cross-tenant indicator.
  - source: https://learn.microsoft.com/en-us/power-automate/desktop-flows/governance
- `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Power Automate Desktop\Global` [windows] - Evidence suppression and evidence generation in one key: DisableFlowExecutionActionLogging (stops action-log upload to Dataverse), DisableScreenshotCaptureOnError, DisableRunFilesCleanup, DisableCloudConnectors, CertificateRevocationCheck, PreserveCustomServiceAccount, and the v2.66+ video family VideoLoggerModeOverride / VideoLoggerDisableSubtitles / VideoLogDurationInSeconds / VideoLogOutputPath. Treat non-default values as an anti-forensics signal.
  - source: https://learn.microsoft.com/en-us/power-automate/desktop-flows/governance
- `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Power Automate Desktop  (and the HKEY_CURRENT_USER key of the same path)` [windows] - Missing from the prior pass entirely, and it holds the richest single set. HKLM: Cloud (DWORD 0-6, resolves which sovereign cloud the host talks to — public/GCC/GCC High/DoD/21Vianet), EnableOrganizationPicker, ProxyServer, ProxyBypassList, ProxyNetworkCredentialsKey (String naming a Windows Credential Manager generic credential — credential-adjacent), DisableWindowsProxy, DisableUNCPaths, ConfigureExternalRuns, EnableDesignerExecutionLogs, AllowVerboseLogging. HKCU: DisableAskBeforeRunningASharedFlow (1 = suppresses the confirmation for running a shared flow from an untrusted author — a direct security-check bypass), EnableAskBeforeRunningAFlowExternally, UseVerboseLogging, NotificationsType.
  - source: https://learn.microsoft.com/en-us/power-automate/desktop-flows/governance
- `HKEY_LOCAL_MACHINE\SOFTWARE\Wow6432Node\Microsoft\Power Automate Desktop\Global  →  UseUIAccessAutomationServer` [windows] - Missed key, v2.68+. Vendor: set "to enable UI Access... on the machine that needs to automate applications that need elevated rights". Value 1 lets the automation server drive elevated-privilege UI. On a computer-use host this is a privilege-relevant configuration worth flagging. Note Microsoft writes this key as Wow6432Node here while writing WOW6432Node for Registration and LogShipper on the same page — record both spellings verbatim.
  - source: https://learn.microsoft.com/en-us/power-automate/desktop-flows/governance
- `HKEY_LOCAL_MACHINE\SOFTWARE\WOW6432Node\Microsoft\Power Automate Desktop\LogShipper  →  AllowOptionalDataCollection` [windows] - Missed key. DWORD governing optional diagnostic usage data collection (0 = off, 1 = on). Determines whether client-side diagnostic telemetry left the host at all, which bounds what a vendor-side investigation can recover.
  - source: https://learn.microsoft.com/en-us/power-automate/desktop-flows/governance
- `%programdata%\Microsoft\Power Automate\Logs` [windows] - Case corrected to match the page exactly (the vendor writes %programdata% lowercase). On-premises product logs for machine registration and cloud runtime — proves a host was registered to a Power Platform environment and that cloud-triggered runs reached it. Administrator privileges required to read or copy.
  - source: https://learn.microsoft.com/en-us/troubleshoot/power-platform/power-automate/desktop-flows/how-to-get-power-automate-desktop-installer-logs
- `Windows Event Log: Windows Logs > Application` [windows] - Third documented log source on the installer-logs KB, omitted by the prior pass despite it citing that page. Vendor directs responders here for application crashes and Windows service start failures — i.e. UIFlowService failures. A standard, durable, timestamped artifact that survives the local cache cleanup the other rows are subject to.
  - source: https://learn.microsoft.com/en-us/troubleshoot/power-platform/power-automate/desktop-flows/how-to-get-power-automate-desktop-installer-logs
- `%temp%\Power Automate for desktop_*.log  and  %temp%\Power Automate for desktop_*_*_MicrosoftFlowRPA.log` [windows] - Installer logs in the TEMP folder of the user who performed the installation — establishes who introduced the runtime and when. MSI install requires an administrator account; Microsoft Store install does not, so the install route itself is a signal about whether IT or a maker stood the host up.
  - source: https://learn.microsoft.com/en-us/troubleshoot/power-platform/power-automate/desktop-flows/how-to-get-power-automate-desktop-installer-logs
- `Service: UIFlowService (display name "Power Automate Service"), default identity NT Service\UIFlowService` [windows] - The local agent runtime. Vendor writes the default identity as NT Service\UIFlowService on the binary-caching KB and as NT SERVICE\UIFlowService on the governance page — record as seen. Custom identities appear as DOMAIN\Username on the machine runtime Troubleshoot page and relocate every cache path above, so resolve the identity first. Restart is performed via services.msc > Power Automate Service.
  - source: https://learn.microsoft.com/en-us/troubleshoot/power-platform/power-automate/desktop-flows/binary-caching-issues
- `*.servicebus.windows.net (HTTPS/TCP)  and  *.gateway.prod.island.powerapps.com (HTTPS)` [network] - The actual BYOM-host network indicators, absent from the prior pass because it never opened the Power Automate list that requirements-quotas tells you to also configure. Vendor annotates both "Needed for machine connectivity", and the Service Bus entry as "Listens on Service Bus Relay over TCP" — this is the cloud-trigger channel that lets a Copilot Studio agent reach the host. Sovereign equivalents: *.servicebus.usgovcloudapi.net, *.gateway.gov.island.powerapps.us (GCC/GCCH), *.gateway.gov.island.appsplatform.us (DoD), *.gateway.mooncake.island.powerapps.cn (China).
  - source: https://learn.microsoft.com/en-us/power-automate/ip-address-configuration
- `msedgedriver.azureedge.net  and  chromedriver.storage.googleapis.com (HTTPS)` [network] - Vendor: "Access to desktop flows WebDriver downloaders. WebDriver is used to automate your browser (Microsoft Edge and Google Chrome)." A computer-use host doing browser automation fetches WebDriver binaries — and chromedriver.storage.googleapis.com is a non-Microsoft domain, so this is a distinctive indicator that browser automation was provisioned on the endpoint. Nobody had this row.
  - source: https://learn.microsoft.com/en-us/power-automate/ip-address-configuration
- `server.events.data.microsoft.com / emea.events.data.microsoft.com / tb.events.data.microsoft.com / apac.events.data.microsoft.com, *.api.powerplatform.com, *.dynamics.com, ocsp.digicert.com, ocsp.msocsp.com, mscrl.microsoft.com, crl3.digicert.com, crl4.digicert.com` [network] - Remaining desktop-flows-runtime endpoints. The events.data.microsoft.com variant in traffic reveals the host's telemetry region (global / EMEA / US Gov / China), which corroborates the Cloud registry value. *.api.powerplatform.com is "mandatory for cloud connectors utilization in desktop flows"; *.dynamics.com is "mandatory for custom actions". The CRL entries are annotated "Needed when connecting through the on-premises data gateway."
  - source: https://learn.microsoft.com/en-us/power-automate/ip-address-configuration
- `aka.ms, builds.dotnet.microsoft.com (MSI installer);  go.microsoft.com, enterpriseregistration.windows.net, device.login.microsoftonline.com, login.microsoftonline.com (hosted machines / hosted machine groups)` [network] - Provisioning-time indicators. The installer pair shows a .NET 8 runtime pull during PAD install. The hosted-machine set is the footprint of the fourth execution option the prior pass missed entirely — Microsoft-provisioned hosts that are Entra-joined at provisioning time ("Required for joining machines to Microsoft Entra").
  - source: https://learn.microsoft.com/en-us/power-automate/ip-address-configuration
- `*.directline.botframework.com — BOTH HTTPS and WS` [network] - Runtime conversation channel. Vendor lists this domain on two separate rows, one per protocol; a network row capturing only HTTPS misses the WebSocket half of agent chat traffic. token.botframework.com appears only when manual authentication is configured, so its presence is itself a configuration signal.
  - source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/requirements-quotas
- `Azure Key Vault (subscription ID + resource group + vault name, configured per computer-use tool)` [cloud] - Second documented secrets location for computer use, missed by the prior pass — the entry's secrets_location names only Power Platform connection references. Vendor: "Passwords are either stored in Power Platform internal storage (no configuration required) or in an Azure Key Vault that you provide." Where Key Vault is used, the vault access policy and its Azure diagnostic logs become in-scope evidence outside Power Platform entirely.
  - source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/computer-use
- `Custom connector definition containing  x-ms-agentic-protocol: mcp-streamable-1.0` [cloud] - Concrete detection string for identifying an MCP server attachment in a Power Platform solution export or custom connector listing — the tenant-side equivalent of finding an mcp.json on disk. Copilot Studio supports only the Streamable transport; SSE was dropped after August 2025, so an SSE-configured connector is stale or non-functional.
  - source: https://learn.microsoft.com/en-us/microsoft-copilot-studio/mcp-add-existing-server-to-agent

### Summary

ADVERSARIAL PASS RESULT: the prior review's central verdict survives, but roughly a third of its specifics needed correcting, and it repeated the exact failure mode it was hired to catch. I fetched 14 pages (all 200), including four the prior review cited but never opened or never listed.

WHAT SURVIVES, VERIFIED VERBATIM. The refutation of "Endpoint footprint is browser only" is correct and every quote supporting it is exact — "Windows machines that you own and manage", the 2.61.132.25266 version floor, the web extension, the machine-runtime app checkbox, "dedicate a machine to computer use". The risk warning is verbatim. The MCP block is verbatim. The "Desktop flows aren't supported as a tool in Microsoft Copilot Studio" counter-finding is verbatim and is a genuinely valuable guardrail. The power-virtual-agents redirect reproduces. The registry key HKLM\SOFTWARE\WOW6432Node\Microsoft\Power Automate Desktop\Registration and all three of its values are exact.

THE PRIOR REVIEW COMMITTED ITS OWN INCOMPLETE-BUT-CORRECT FAILURE, THREE TIMES.
(1) NETWORK. It quoted the Copilot Studio "Required services" table accurately — then missed the sentence directly above it: "To use Copilot Studio and Power Automate together, configure all required services for Power Automate, in addition to the services listed for Copilot Studio." Since computer use runs on Power Automate for desktop, every BYOM host is in that scope. The page it never opened contains the endpoints that actually matter for an infected host: *.servicebus.windows.net ("Listens on Service Bus Relay over TCP. Needed for machine connectivity"), *.gateway.prod.island.powerapps.com, and — nobody had this — msedgedriver.azureedge.net and chromedriver.storage.googleapis.com, the WebDriver downloaders a browser-automating host contacts.
(2) REGISTRY. It catalogued 2 key paths from a page documenting at least 6, then called one of them "the highest-value registry key in the set". Missing: HKLM\...\Power Automate Desktop (holds Cloud, which resolves the sovereign cloud; EnableOrganizationPicker, a second cross-tenant tenant-GUID indicator; ProxyNetworkCredentialsKey, which names a Windows Credential Manager entry), the entire HKCU hive including DisableAskBeforeRunningASharedFlow — a documented bypass of the untrusted-author security check — plus the LogShipper key and the UI Access key for automating elevated-rights apps.
(3) ARTIFACTS. Citing the binary-caching KB, it missed DesktopFlowModules: "Contains custom modules downloaded from the Asset Library. Missing modules are downloaded again at runtime." That is arbitrary tenant code delivered to the endpoint at runtime — arguably the single most important row for this entry. It also missed a third ServiceProfiles cache path and, citing the installer-logs KB, missed that same page's Event Viewer section.

SUBTLY WRONG PATH STRINGS AND SCOPE.
- supported_os: the Windows list is transcribed correctly but scoped wrongly. Windows Home cannot run cloud-triggered desktop flows ("Runtime | Cloud runtime | Windows Home: No"), and computer use is cloud-triggered. Listing Home as a valid computer-use host is a false positive.
- "Cache\(MSI or MSIX)" is Microsoft's disjunctive prose, not a directory. A collector fed that string verbatim finds nothing. Split into Cache\MSI and Cache\MSIX.
- %ProgramData% is written %programdata% on the page; cosmetic on Windows but the catalog's rule is exact transcription.
- The Designer\Scripts\...\Actions.log row is described as "the cloud-driven execution trail — what a Copilot Studio agent caused to run". It is the opposite: the vendor scopes it to "designer-based runs", i.e. a maker running a flow by hand. The cloud-triggered tree is the sibling \Scripts.
- Microsoft's own governance page is internally inconsistent on case (WOW6432Node vs Wow6432Node); both should be recorded as seen.

DOWNGRADED FOR SOURCING. "BYOM is the only GA path" was presented as vendor-documented; it is inferred from the absence of a preview banner, and it is weakened by a fourth execution option the prior review missed — Power Automate hosted machines, documented in a Note on the same page and with its own endpoint section. "Multi-session not supported for computer use (hosted machine groups aren't supported)" is not on the cited page, which in fact documents multi-session sizing; marked unverifiable, drop or re-source. The "three execution locations" count is corrected to four.

UPGRADED. The CrowdStrike AIDR claim was left unverifiable with no fetch. I fetched CrowdStrike's own announcement (200, 30 July 2026): Falcon AIDR "plugs into Copilot Studio as an external threat detection provider", checking tool name and input parameters pre-execution and returning allow/block, with findings correlatable in Falcon Next-Gen SIEM. Keep it, cite it, label the sourcing as CrowdStrike-vendor not Microsoft-vendor. The mcp-add-existing-server-to-agent wording caveat can also be lifted — I opened the page and the quote is verbatim — but the block is incomplete: there is a third attachment route (Agents 365 CLI + M365 Admin Center) that bypasses Copilot Studio's own wizard, and the detection string x-ms-agentic-protocol: mcp-streamable-1.0 is the tenant-side equivalent of finding an mcp.json.

ALSO MISSED, AND IT MATTERS FOR plaintext_credentials. The prior review confirmed that flag off the wrong section. computer-use has a "Stored credentials" section where makers enter usernames and passwords directly. The flag still holds — nothing lands on the endpoint in plaintext — but there are TWO secret stores, and the second is a customer-supplied Azure Key Vault, which puts vault access policies and Azure diagnostic logs in scope. The entry's secrets_location names neither.

CITATION HYGIENE. learn.microsoft.com/en-us/microsoft-copilot-studio/computer-use is cited as the source for two claims but appears nowhere in the prior review's own pages_fetched list. I verified the content is genuine, so this is a record-keeping failure rather than a fabrication — but under ground rule 1 it should not have been reported as fetched-and-verified. Separately, I re-fetched only one of the two catalogued reference URLs (fundamentals-what-is-copilot-studio, 200, refreshed 08/03/2026); the docset root still rests on the prior pass alone.

SCOPE DECISION UNCHANGED. These artifacts belong to Power Automate for desktop, a distinct product. I agree with the prior review's lean toward a separate Power Automate for desktop entry cross-referenced from AIRT-0014, and note the fork is now wider than it described: the gateway, hosted machines, and the Power Automate network list all belong to that entry too. The on-premises data gateway row supplies no collectible path and its only source is the Azure SAP docset — keep it as a scoping note, not an artifact row, until it gets its own pass.

---

## AIRT-0033 Claude Computer Use

*19 pages fetched, 19 at 2xx*

- `200` https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/computer_use_demo/streamlit.py
- `200` https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/README.md
- `200` https://github.com/anthropics/claude-quickstarts/tree/main/computer-use-demo **(redirected)**
- `200` https://api.github.com/repositories/849553306 **(redirected)**
- `200` https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/computer_use_demo/tools/computer.py
- `200` https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/computer_use_demo/loop.py
- `200` https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/computer_use_demo/requirements.txt
- `200` https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/Dockerfile
- `200` https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/image/entrypoint.sh
- `200` https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/image/start_all.sh
- `200` https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/image/x11vnc_startup.sh
- `200` https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/image/xvfb_startup.sh
- `200` https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/image/mutter_startup.sh
- `200` https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/image/tint2_startup.sh
- `200` https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/image/novnc_startup.sh
- `200` https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/image/http_server.py
- `200` https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-best-practices/README.md
- `200` https://platform.claude.com/docs/en/agents-and-tools/tool-use/computer-use-tool
- `200` https://raw.githubusercontent.com/anthropics/anthropic-sdk-python/main/src/anthropic/_client.py

### Claims

**[confirmed / vendor-documented]** credentials: ~/.anthropic/api_key - plaintext Anthropic API key persisted by the reference implementation's Streamlit UI, written with mode 0600
  - actual: ~/.anthropic/api_key (exact); chmod(0o600) confirmed; written only on the Anthropic provider path
  - evidence: Independently re-fetched (200). Verbatim in streamlit.py: CONFIG_DIR = PosixPath("~/.anthropic").expanduser(); API_KEY_FILE = CONFIG_DIR / "api_key"; and save_to_storage() ends CONFIG_DIR.mkdir(parents=True, exist_ok=True); file_path = CONFIG_DIR / filename; file_path.write_text(data); file_path.chmod(0o600). Wired to st.text_input("Claude API Key", type="password", key="api_key", on_change=lambda: save_to_storage("api_key", st.session_state.api_key)) inside if st.session_state.provider == APIProvider.ANTHROPIC:. Prior agent's account of this row is accurate in every particular. Worth adding that PosixPath is hardcoded, which is a second independent reason the row correctly omits windows.
  - source: https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/computer_use_demo/streamlit.py

**[corrected / vendor-documented]** ~/.anthropic/api_key lives at /home/computeruse/.anthropic/api_key inside the container, which the documented docker run bind-mounts from the host at $HOME/.anthropic, so the key survives container teardown on the host filesystem
  - actual: -v $HOME/.anthropic:/home/computeruse/.anthropic on 5 of 6 documented runs; the Vertex recipe omits it entirely
  - evidence: The mount string is exact - -v $HOME/.anthropic:/home/computeruse/.anthropic - and Dockerfile's ENV USERNAME=computeruse / ENV HOME=/home/$USERNAME resolves the in-container path. But the prior agent wrote that it appears on 'all four documented docker run invocations (Claude API, Bedrock option 1, Bedrock option 2, and Development)'. Re-reading the README, there are SIX docker run blocks and the mount appears in five: Claude API, Bedrock Option 1, Bedrock Option 2, the Screen size example, and Development. It is ABSENT from the Vertex block, which mounts only the gcloud ADC file. So on a Vertex deployment ~/.anthropic exists only inside the container and dies with it - the api_key, system_prompt and error_*.md files do NOT reach the host. That flips the collection instruction for one of three supported providers and the prior finding asserted the opposite.
  - source: https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/README.md

**[confirmed / vendor-documented]** credentials: ANTHROPIC_API_KEY - plaintext API key passed on the docker run command line (-e ANTHROPIC_API_KEY=...)
  - actual: -e ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY (exact); generic across all Anthropic tooling
  - evidence: Re-fetched README (200): export ANTHROPIC_API_KEY=%your_api_key% then docker run -e ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY. Corroborated in streamlit.py setup_state(): st.session_state.api_key = load_from_storage("api_key") or os.getenv("ANTHROPIC_API_KEY", ""). The prior agent's shared-path caveat is right and should be carried into the row description: this variable proves an Anthropic API client ran, not that this demo ran. The distinguishing string on the same command line is ghcr.io/anthropics/anthropic-quickstarts:computer-use-demo-latest, confirmed verbatim in the README and correctly flagged as NOT to be 'fixed' despite the repo rename.
  - source: https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/README.md

**[confirmed / vendor-documented]** credentials: ~/.aws - host AWS credential directory bind-mounted into the container at /home/computeruse/.aws in the documented Bedrock profile setup
  - actual: -v $HOME/.aws:/home/computeruse/.aws (exact); ~/.aws itself is a SHARED path
  - evidence: Re-fetched (200). Bedrock 'Option 1: (suggested) Use the host's AWS credentials file and AWS profile' shows -v $HOME/.aws:/home/computeruse/.aws alongside -e API_PROVIDER=bedrock -e AWS_PROFILE=$AWS_PROFILE -e AWS_REGION=us-west-2. Path and mount target match character for character. The blast-radius framing (whole directory, not the named profile) is correct.
  - source: https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/README.md

**[corrected / vendor-documented]** the ~/.aws credential row carries secret_type: aws-sso-token
  - actual: secret_type should be api-key (or the row split); aws-sso-token names only ~/.aws/sso/cache/, which the README never references
  - evidence: A defect the prior agent did not look for: it verified the mount string but never checked the row's own vocabulary. The README's documented flow is headed 'Use the host's AWS credentials file and AWS profile' - i.e. ~/.aws/credentials holding aws_access_key_id / aws_secret_access_key, which is long-lived key material, not an SSO token. SSO tokens live only in ~/.aws/sso/cache/. The catalog's own description for the row says '(credentials, config, sso/cache/)', so the row itself acknowledges the directory is broader than its secret_type. secret_type is a closed enum (api-key, oauth-token, jwt-signing-key, session-state, aws-sso-token, admin-credential, embedded-credentials, encryption-key, unknown) and api-key is the closer fit for the vendor-documented path; aws-sso-token describes a subdirectory the vendor never mentions. Filterable-feed consumers screening for aws-sso-token will pull this row and miss it under api-key.
  - source: https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/README.md

**[confirmed / vendor-documented]** credentials: AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY / AWS_SESSION_TOKEN passed on the docker run command line in the documented Bedrock access-key setup
  - actual: Confirmed verbatim
  - evidence: Re-fetched (200). Bedrock 'Option 2: Use an access key and secret' exports all three and passes -e AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID -e AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY -e AWS_SESSION_TOKEN=$AWS_SESSION_TOKEN with -e API_PROVIDER=bedrock -e AWS_REGION=us-west-2. Names match the catalog exactly. Generic AWS variables; attribution needs the surrounding docker run context.
  - source: https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/README.md

**[confirmed / vendor-documented]** credentials: ~/.config/gcloud/application_default_credentials.json bind-mounted file-for-file from the host into the container in the documented Vertex AI setup
  - actual: Path string matches character for character; two recipes build locally, not one
  - evidence: Re-fetched (200). Vertex section: gcloud auth application-default login, then -v $HOME/.config/gcloud/application_default_credentials.json:/home/computeruse/.config/gcloud/application_default_credentials.json. Single-file mount, not the directory - the catalog's 'file-for-file' wording is exact. Prior agent's note that the Vertex recipe builds locally (docker build . -t computer-use-demo) rather than pulling ghcr.io is correct, but its stronger claim that Vertex is 'the ONLY one the README does NOT run from the published ghcr.io image' is wrong: the Development recipe also builds locally, as computer-use-demo:local.
  - source: https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/README.md

**[confirmed / vendor-documented]** network: egress to api.anthropic.com, 443/TCP
  - actual: https://api.anthropic.com confirmed as SDK default, overridable via ANTHROPIC_BASE_URL
  - evidence: Re-fetched the SDK client (200) and extracted the literal lines: if base_url is None: base_url = os.environ.get("ANTHROPIC_BASE_URL") / if base_url is None: base_url = f"https://api.anthropic.com". loop.py instantiates Anthropic(api_key=api_key, max_retries=4) with no base_url override, so the default applies. Screenshot-volume signature is well founded: BaseComputerTool.shell has take_screenshot=True by default with _screenshot_delay = 2.0, and loop.py's _make_api_tool_result returns each as a base64 image block. One caveat the prior agent missed: ANTHROPIC_BASE_URL is honoured ahead of the default, so a gateway or proxy deployment produces zero traffic to api.anthropic.com even on the Anthropic provider.
  - source: https://raw.githubusercontent.com/anthropics/anthropic-sdk-python/main/src/anthropic/_client.py

**[missing-row / vendor-documented]** network block covers this tool's egress completely (single egress row, api.anthropic.com)
  - actual: Three egress destinations in the reference implementation, one catalogued; five platforms at tool level
  - evidence: Re-fetched loop.py (200) and confirmed the prior agent's finding verbatim: class APIProvider(StrEnum) with ANTHROPIC/BEDROCK/VERTEX, branching to Anthropic(api_key=..., max_retries=4) / AnthropicVertex() / AnthropicBedrock(). README documents Bedrock and Vertex as first-class recipes. On those paths there is no api.anthropic.com traffic at all. Selectors are -e API_PROVIDER=bedrock with AWS_REGION, or -e API_PROVIDER=vertex with CLOUD_ML_REGION and ANTHROPIC_VERTEX_PROJECT_ID. Separately, the platform.claude.com Compatibility table lists five supported platforms (Claude API, Claude Platform on AWS, Amazon Bedrock, Google Cloud, Microsoft Foundry) at the tool level, so an entry scoped to the tool rather than the demo has more than three destinations.
  - source: https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/computer_use_demo/loop.py

**[missing-row / vendor-documented]** capabilities.local_listener: true (no listener rows present in the network block)
  - actual: 5900/TCP (x11vnc, -nopw, no auth), 6080/TCP (noVNC), 8080/TCP (binds ::, dual-stack), 8501/TCP (Streamlit) - none catalogued
  - evidence: Every string re-verified against the vendor scripts. x11vnc_startup.sh: x11vnc -display $DISPLAY -forever -shared -wait 50 -rfbport 5900 -nopw - the -nopw is exact, an unauthenticated VNC server on the desktop Claude drives, with -forever -shared permitting concurrent viewers. novnc_startup.sh: /opt/noVNC/utils/novnc_proxy --vnc localhost:5900 --listen 6080 --web /opt/noVNC. http_server.py: class HTTPServerV6(HTTPServer) with address_family = socket.AF_INET6 and server_address = ("::", 8080) serving SimpleHTTPRequestHandler out of static_content. entrypoint.sh: STREAMLIT_SERVER_PORT=8501 python -m streamlit run computer_use_demo/streamlit.py. All four published by every documented docker run: -p 5900:5900 -p 8501:8501 -p 6080:6080 -p 8080:8080. Prior agent's listener findings hold in full.
  - source: https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/image/x11vnc_startup.sh

**[corrected / vendor-documented]** disk: single row 'Docker container running the reference implementation', artifact_type container, forensic_value: low, evidence_type: [program-presence]
  - actual: /tmp/outputs/screenshot_<uuid4hex>.png - high forensic value, evidence_type user-activity/timeline/data-access; row absent
  - evidence: Re-fetched computer.py (200). Confirmed: OUTPUT_DIR = "/tmp/outputs"; in screenshot(), output_dir = Path(OUTPUT_DIR); await asyncio.to_thread(output_dir.mkdir, parents=True, exist_ok=True); path = output_dir / f"screenshot_{uuid4().hex}.png"; capture via gnome-screenshot -f {path} -p or scrot -p {path}; rewritten in place by convert {path} -resize {x}x{y}! {path}. Only the zoom action unlinks anything (temp_path.unlink(missing_ok=True); cropped_path.unlink(missing_ok=True) on its screenshot_/zoomed_ pair), so screenshot() output accumulates untouched. Two additions to the prior finding: the in-place convert means mtime is the resize time, not the capture time; and the row's path field holds prose ('Docker container running the reference implementation') rather than a path, which is why the exporters have nothing to emit for this entry.
  - source: https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/computer_use_demo/tools/computer.py

**[missing-row / vendor-documented]** ~/.anthropic holds the API key (implied sole content of the mounted config dir)
  - actual: ~/.anthropic/system_prompt and ~/.anthropic/error_<float-timestamp>.md - both absent from the catalog; host-persisted except on Vertex
  - evidence: Confirmed against re-fetched streamlit.py, with two corrections to the prior finding. (1) ~/.anthropic/system_prompt: st.text_area("Custom System Prompt Suffix", key="custom_system_prompt", on_change=lambda: save_to_storage("system_prompt", st.session_state.custom_system_prompt)), read back by load_from_storage("system_prompt"), appended to SYSTEM_PROMPT in loop.py. Independently corroborated by the README: 'The container stores settings like the API key and custom system prompt in ~/.anthropic/.' (2) ~/.anthropic/error_<timestamp>.md: _render_error() calls save_to_storage(f"error_{datetime.now().timestamp()}.md", body). CORRECTION A: datetime.now().timestamp() returns a float, so the real filename carries a fractional part - error_1771000000.123456.md - not an integer unix timestamp; a collection glob must be error_*.md. CORRECTION B: the body is str(error) plus a formatted traceback only on the generic branch; the RateLimitError branch writes a rate-limit message and error.message with no traceback. CORRECTION C: both files reach the host only where ~/.anthropic is mounted, which excludes the Vertex recipe.
  - source: https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/computer_use_demo/streamlit.py

**[corrected / vendor-documented]** disk block covers the container's on-disk evidence (no log rows) - prior agent enumerated four container logs
  - actual: Six log paths plus /tmp/.X1-lock, not four
  - evidence: The four logs the prior agent named are all real and verbatim (/tmp/server_logs.txt and /tmp/streamlit_stdout.log from entrypoint.sh; /tmp/x11vnc_stderr.log from x11vnc_startup.sh including the : > truncation on success and rm on crash-restart; /tmp/novnc.log from novnc_startup.sh). But the enumeration is itself incomplete-but-correct: entrypoint.sh's first line is ./start_all.sh, which the prior agent never fetched. start_all.sh runs ./xvfb_startup.sh ./tint2_startup.sh ./mutter_startup.sh ./x11vnc_startup.sh. tint2_startup.sh writes 2>/tmp/tint2_stderr.log and rm's it on success; mutter_startup.sh writes 2>/tmp/mutter_stderr.log and rm's it on success; xvfb_startup.sh tests for /tmp/.X${DISPLAY_NUM}-lock (i.e. /tmp/.X1-lock, since Dockerfile sets ARG DISPLAY_NUM=1), a durable in-container program-presence marker. Also relevant to the entry's 'collect before any agent restart' guidance: the Streamlit Reset button runs subprocess.run("pkill Xvfb; pkill tint2") then subprocess.run("./start_all.sh"), which re-truncates those stderr logs mid-incident.
  - source: https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/image/start_all.sh

**[stale-reference / vendor-documented]** references[1]: https://github.com/anthropics/anthropic-quickstarts/tree/main/computer-use-demo (accessed 2026-08-13)
  - actual: https://github.com/anthropics/claude-quickstarts/tree/main/computer-use-demo
  - evidence: The prior agent's biggest claim survives and is now independently corroborated. Fetching the catalogued GitHub URL returns 200 and the rendered page canonicalises to anthropics/claude-quickstarts throughout - 40+ occurrences of anthropics/claude-quickstarts in the response, zero of anthropics/anthropic-quickstarts. Decisive check the prior agent did not run: https://api.github.com/repos/anthropics/anthropic-quickstarts returns 200 with a final URL of https://api.github.com/repositories/849553306 and a body giving "name": "claude-quickstarts", "full_name": "anthropics/claude-quickstarts", html_url https://github.com/anthropics/claude-quickstarts. The repo was renamed; the old citation lives on GitHub's rename redirect only. Do NOT rewrite the image tag ghcr.io/anthropics/anthropic-quickstarts:computer-use-demo-latest, which is verbatim current in the README.
  - source: https://api.github.com/repositories/849553306

**[confirmed / vendor-documented]** references[0]: https://platform.claude.com/docs/en/agents-and-tools/tool-use/computer-use-tool (accessed 2026-08-13)
  - actual: Live and unmoved; four stale old-name links, not three
  - evidence: Re-fetched: 200, no redirect, final URL identical, title 'Computer use tool - Claude Platform Docs' matching the catalogued reference title. Both quotations the prior agent attributed to this page are genuinely present: 'the reference implementation runs all of this inside a Docker container with appropriate port mappings', and the Data retention section stating screenshots, mouse actions, keyboard inputs and files are captured and stored in your environment, not by Anthropic. One count correction: the prior agent said the page links the old anthropic-quickstarts URL 'in three places'. The extracted link set contains four distinct old-name URLs (tree/computer-use-demo, blob/.../Dockerfile, tree/.../computer_use_demo/tools, blob/.../loop.py) and the tree URL recurs four times in the body.
  - source: https://platform.claude.com/docs/en/agents-and-tools/tool-use/computer-use-tool

**[confirmed / vendor-documented]** capabilities.mcp_capable: false and mcp: []
  - actual: Correct as catalogued; the load-bearing evidence is requirements.txt, not loop.py
  - evidence: The conclusion is right but the prior agent overstated its sourcing: it asserted 'no mcp import anywhere in loop.py, streamlit.py, computer.py, the Dockerfile or the image/ startup scripts' and that the tool list is '(computer, bash, text-editor)', while never fetching computer_use_demo/tools/ (groups.py, base.py, bash.py, edit.py, run.py) where those names actually live - loop.py only references TOOL_GROUPS_BY_VERSION. I ran the decisive check it skipped: computer_use_demo/requirements.txt is streamlit==1.41.0 / anthropic[bedrock,vertex]>=0.39.0 / jsonschema==4.22.0 / boto3>=1.28.57 / google-auth<3,>=2. No MCP package is installed, so no MCP client can exist in the image regardless of what the tools/ modules import. mcp_capable: false with an empty mcp block is correct, and this is the citation to record for it.
  - source: https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/computer_use_demo/requirements.txt

**[corrected / vendor-documented]** description: 'Reference implementation ships as a Docker container' / supported_os: [container]
  - actual: Two vendor reference implementations exist; the macOS-native one (runs/ transcripts, .env API key, TCC grants) is uncatalogued
  - evidence: This is the incomplete-but-correct case the prior agent was asked to find and missed. The demo's own README links a sibling quickstart: 'see the Computer Use Best Practices quickstart, which runs natively on macOS (no container)'. I fetched it (200). It is a second Anthropic-published computer-use reference implementation: 'It targets macOS only... running locally (no Docker)', with pyautogui screen control, sandbox-exec, an editor tool and browser tools, and the same three providers (cfg.provider selects anthropic / vertex / bedrock). Its artifacts are entirely host-side: an API key in a repo-root .env (echo "ANTHROPIC_API_KEY=<YOUR API KEY HERE>" > .env, or cp .env.example .env); trajectory recording where 'Everything the agent sends or receives is also written to runs/<timestamp>/ as a JSONL transcript plus the JPEG screenshots'; a per-run scratch dir (trajectory.py); optional config.toml via CU_CONFIG; sandbox/default.sb; loopback listeners on 127.0.0.1:8000 (FastAPI tool panel) and 127.0.0.1:8001 (localization demo) plus a Streamlit trajectory viewer; and macOS Screen Recording + Accessibility TCC grants to the terminal, which are themselves probative. The Dockerfile facts the prior agent verified are all correct (FROM docker.io/ubuntu:22.04; xvfb/xdotool/scrot/imagemagick/mutter/x11vnc/tint2/firefox-esr/libreoffice; noVNC v1.5.0 + websockify v0.12.0 into /opt/noVNC; ENV USERNAME=computeruse; echo "${USERNAME} ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers; ENTRYPOINT [ "./entrypoint.sh" ]) - but they describe one of two vendor reference implementations, and supported_os: [container] silently excludes the other.
  - source: https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-best-practices/README.md

**[confirmed / vendor-documented]** (task premise) the demo ships a docker compose file
  - actual: Dockerfile + image/entrypoint.sh only; no compose file
  - evidence: Re-fetched the directory listing (200). Contents are .zed/, computer_use_demo/, image/, tests/, .gitignore, CONTRIBUTING.md, Dockerfile, LICENSE, README.md, dev-requirements.txt, pyproject.toml, ruff.toml, setup.sh. Searching the fetched page for 'compose' returns nothing. The prior agent's correction stands: every documented launch is a bare docker run, so the mounts and -e flags exist only in container config metadata (docker inspect) and host shell history, not in a file on disk.
  - source: https://github.com/anthropics/claude-quickstarts/tree/main/computer-use-demo

**[corrected / vendor-documented]** credential rows carry os: [docker, container, macos, linux] (windows omitted) while the disk container row carries os: [windows, macos, linux]
  - actual: 2 of 5 credential rows are [docker, container]; disk row omits container while supported_os is [container]-only
  - evidence: The prior agent's description of the catalog it was reviewing is wrong. Only three of the five credential rows carry [docker, container, macos, linux] (~/.anthropic/api_key, ~/.aws, ~/.config/gcloud/...). The ANTHROPIC_API_KEY row and the AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY/AWS_SESSION_TOKEN row carry only [docker, container]. Its reasoning about why windows is absent is nevertheless sound and vendor-supported - every documented run uses POSIX expansion ($HOME, $(pwd)) and streamlit.py hardcodes PosixPath. The asymmetry it did not notice is the more interesting one: supported_os is exactly [container], yet the single disk row's os is [windows, macos, linux] with no container or docker at all, so the entry's only artifact row is scoped to operating systems the entry says it does not support.
  - source: https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/README.md

**[corrected / vendor-documented]** collection.guidance: 'Capture the MCP config before remediation - it records what the agent was authorised to run.'
  - actual: Guidance references a non-existent MCP config; the real equivalent is ~/.anthropic/system_prompt
  - evidence: Internal contradiction neither the catalog nor the prior agent caught - and the prior agent actually leaned on this sentence as a rhetorical anchor ('the same role the catalog's collection guidance assigns to an MCP config') without noticing it instructs a responder to collect something the same entry declares does not exist. mcp_capable is false and mcp is [], and requirements.txt confirms no MCP component is installed. The guidance is boilerplate carried in from the entry template. It should either be dropped or repointed at ~/.anthropic/system_prompt, which is the file that genuinely records what the agent was told to do.
  - source: https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/computer_use_demo/requirements.txt

**[missing-row / vendor-documented]** the credentials block enumerates every documented credential location
  - actual: GOOGLE_APPLICATION_CREDENTIALS - documented alternative GCP credential pointer, absent from the credentials block
  - evidence: Both the catalog and the prior agent stopped at the five rows. The README's Vertex section documents a sixth in prose immediately after the docker run block: 'You can also set GOOGLE_APPLICATION_CREDENTIALS to use an arbitrary credential file', linking Google's ADC documentation. That is an environment variable naming an arbitrary service-account key file at an operator-chosen path - it defeats a sweep that looks only for ~/.config/gcloud/application_default_credentials.json, and the pointer itself is recoverable from docker inspect and shell history.
  - source: https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/README.md

### Candidate new rows

- `/tmp/outputs/screenshot_<uuid4hex>.png` [docker, container] - The screenshot loop's actual output, and the highest-value artifact this tool produces. computer.py sets OUTPUT_DIR = "/tmp/outputs" and writes output_dir / f"screenshot_{uuid4().hex}.png" on nearly every agent action (shell() defaults take_screenshot=True, _screenshot_delay = 2.0). Nothing deletes them - only the zoom action unlinks its own temp pair - so the directory is a frame-by-frame record of what the agent saw. Note mtimes reflect the in-place convert -resize, not the raw capture. artifact_type session-artifact, forensic_value high, evidence_type user-activity/timeline/data-access. Container-local and bind-mounted by no documented run: acquire from the live container before teardown.
  - source: https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/computer_use_demo/tools/computer.py
- `~/.anthropic/system_prompt` [docker, container, macos, linux] - Operator-supplied system prompt suffix, written by save_to_storage("system_prompt", ...) from the Streamlit sidebar and appended to SYSTEM_PROMPT in loop.py. The direct record of what the agent was instructed to do - and the row the entry's 'capture the MCP config' guidance should point at, since this entry has no MCP. Corroborated in the README: 'The container stores settings like the API key and custom system prompt in ~/.anthropic/.' chmod 0600. Host-persisted via -v $HOME/.anthropic:/home/computeruse/.anthropic on the Claude API, both Bedrock and the Development recipes - but NOT on the Vertex recipe, which omits that mount, so on Vertex it is container-local and dies with the container. artifact_type config-file, evidence_type configuration/user-activity, forensic_value high.
  - source: https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/computer_use_demo/streamlit.py
- `~/.anthropic/error_*.md` [docker, container, macos, linux] - Error dumps from _render_error(): save_to_storage(f"error_{datetime.now().timestamp()}.md", body). Use the glob rather than a literal pattern - datetime.now().timestamp() is a float, so real names look like error_1771000000.123456.md. Body is str(error) plus a formatted traceback on the generic branch, or a rate-limit message plus error.message on the RateLimitError branch. Filenames alone timeline failed runs. Same $HOME/.anthropic mount caveat as system_prompt (absent on Vertex). chmod 0600. artifact_type log, evidence_type execution/timeline, forensic_value medium.
  - source: https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/computer_use_demo/streamlit.py
- `/tmp/server_logs.txt and /tmp/streamlit_stdout.log` [docker, container] - entrypoint.sh: python http_server.py > /tmp/server_logs.txt 2>&1 & and STREAMLIT_SERVER_PORT=8501 python -m streamlit run computer_use_demo/streamlit.py > /tmp/streamlit_stdout.log &. server_logs.txt is the access log for the port-8080 console (SimpleHTTPRequestHandler logs every request), so it answers who connected to the agent's UI and when. streamlit_stdout.log establishes that the agent UI started. Container-local, not bind-mounted, lost on teardown. artifact_type log, evidence_type execution/user-activity.
  - source: https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/image/entrypoint.sh
- `/tmp/novnc.log, /tmp/x11vnc_stderr.log, /tmp/tint2_stderr.log, /tmp/mutter_stderr.log, /tmp/.X1-lock` [docker, container] - Desktop-stack artifacts, reached through entrypoint.sh -> start_all.sh -> {xvfb,tint2,mutter,x11vnc}_startup.sh. novnc_startup.sh redirects the proxy to /tmp/novnc.log (durable). The three *_stderr.log files are live-only by design: x11vnc_startup.sh truncates its log on successful startup (: > /tmp/x11vnc_stderr.log) and rm's it on crash-restart, and tint2_startup.sh and mutter_startup.sh both rm theirs on success - so their presence indicates a failed or in-progress start, not normal operation. xvfb_startup.sh gates on /tmp/.X${DISPLAY_NUM}-lock, which is /tmp/.X1-lock at the Dockerfile default ARG DISPLAY_NUM=1 and is a durable program-presence marker. The Streamlit Reset button re-runs start_all.sh, which re-truncates these - relevant to the entry's 'collect before any agent restart' instruction. artifact_type log, evidence_type execution.
  - source: https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/image/start_all.sh
- `network listener: 5900/TCP - x11vnc, unauthenticated` [docker, container] - x11vnc_startup.sh runs x11vnc -display $DISPLAY -forever -shared -wait 50 -rfbport 5900 -nopw. -nopw means no VNC password: anyone reaching the published port gets interactive control of the desktop Claude is driving, and -forever -shared permits concurrent viewers. Published by every documented docker run with -p 5900:5900. networkArtifact fields apply directly: direction listener, port 5900/TCP, authentication none, exposure_risk high.
  - source: https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/image/x11vnc_startup.sh
- `network listeners: 8080/TCP (HTTP UI), 6080/TCP (noVNC), 8501/TCP (Streamlit)` [docker, container] - capabilities.local_listener is true with no listener rows, so a responder has no netstat signature. http_server.py binds server_address = ("::", 8080) through an AF_INET6 HTTPServerV6 - all interfaces, dual-stack, not loopback. novnc_startup.sh: novnc_proxy --vnc localhost:5900 --listen 6080 --web /opt/noVNC. entrypoint.sh: STREAMLIT_SERVER_PORT=8501. All published to the host by every documented docker run (-p 8080:8080 -p 6080:6080 -p 8501:8501). None requires authentication.
  - source: https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-demo/image/http_server.py
- `network egress: Amazon Bedrock and Google Vertex endpoints` [docker, container] - loop.py branches on APIProvider - Anthropic() / AnthropicBedrock() / AnthropicVertex() - and the README documents both alternatives as first-class recipes, so the single api.anthropic.com row misses two of three deployments. Key on the environment markers rather than guessing regional hostnames the vendor does not enumerate: -e API_PROVIDER=bedrock with AWS_REGION, or -e API_PROVIDER=vertex with CLOUD_ML_REGION and ANTHROPIC_VERTEX_PROJECT_ID. Add a caveat to the existing row too: the SDK honours ANTHROPIC_BASE_URL ahead of its https://api.anthropic.com default, so a gateway deployment produces no api.anthropic.com traffic on the Anthropic provider either.
  - source: https://raw.githubusercontent.com/anthropics/anthropic-quickstarts/main/computer-use-demo/computer_use_demo/loop.py
- `<repo>/runs/<timestamp>/ - JSONL transcript plus JPEG screenshots` [macos] - From the second, uncatalogued vendor reference implementation (claude-quickstarts/computer-use-best-practices), which runs natively on macOS with no container. Its README: 'Everything the agent sends or receives is also written to runs/<timestamp>/ as a JSONL transcript plus the JPEG screenshots, so you can replay it later in the viewer', and the layout lists trajectory.py as 'on-disk transcript + images + per-run scratch dir'. This is the full prompt-and-screenshot record of a computer-use session sitting on the HOST filesystem, surviving indefinitely - strictly higher value than the container demo's /tmp/outputs because it pairs the images with the model's reasoning and tool calls. artifact_type session-artifact, forensic_value high, evidence_type user-activity/timeline/data-access.
  - source: https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-best-practices/README.md
- `<repo>/.env (ANTHROPIC_API_KEY=...) and <repo>/config.toml via CU_CONFIG` [macos] - Same uncatalogued macOS implementation. Its documented setup is echo "ANTHROPIC_API_KEY=<YOUR API KEY HERE>" > .env, or cp .env.example .env - a plaintext API key in a repo-root dotfile with no chmod applied, which is weaker than the container demo's 0600 ~/.anthropic/api_key. Configuration is a TOML file pointed at by CU_CONFIG plus CU_<FIELD_NAME> env overrides; .env.example also documents the Vertex and Bedrock credentials the same implementation accepts. storage plaintext, secret_type api-key.
  - source: https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-best-practices/README.md
- `macOS TCC grants (Screen Recording + Accessibility) and loopback listeners 127.0.0.1:8000 / 127.0.0.1:8001` [macos] - Same uncatalogued macOS implementation. It requires the operator to grant the terminal Screen Recording and Accessibility in System Settings, and on macOS 15+ an additional recurring 'bypass the system private window picker' consent - so the TCC record for the terminal binary is durable, dated, host-side proof that a computer-use agent was authorised to capture the screen and drive input, independent of whether the repo still exists. Its dev UIs listen on 127.0.0.1:8000 (FastAPI tool panel) and 127.0.0.1:8001 (localization demo), both vendor-stated, plus a Streamlit trajectory viewer whose port the README does not state. Sandbox profile at sandbox/default.sb (sandbox-exec) bounds the bash/python tools.
  - source: https://raw.githubusercontent.com/anthropics/claude-quickstarts/main/computer-use-best-practices/README.md

### Summary

MOST OF THE PRIOR REPORT SURVIVES. I re-fetched 19 pages, including six of the URLs it cited, and every path string it quoted is genuinely on the page: `~/.anthropic/api_key` with `chmod(0o600)`, `-v $HOME/.aws:/home/computeruse/.aws`, the full `-v $HOME/.config/gcloud/application_default_credentials.json:/home/computeruse/.config/gcloud/application_default_credentials.json`, `OUTPUT_DIR = "/tmp/outputs"` with `screenshot_{uuid4().hex}.png`, `x11vnc ... -rfbport 5900 -nopw`, `novnc_proxy --vnc localhost:5900 --listen 6080`, `server_address = ("::", 8080)`, `STREAMLIT_SERVER_PORT=8501`. Nothing it marked vendor-documented turned out to be aggregator-sourced or invented; every citation is Anthropic's own repo or docs site.

THE STALE-REFERENCE FINDING IS NOW PROVEN, not just inferred from page metadata. The prior agent read the rename off the rendered HTML. I ran the decisive check it skipped: `https://api.github.com/repos/anthropics/anthropic-quickstarts` returns 200, final URL `https://api.github.com/repositories/849553306`, body `"full_name": "anthropics/claude-quickstarts"`. The repo really was renamed and reference[1] survives only on GitHub's rename redirect. Its warning not to "fix" the `ghcr.io/anthropics/anthropic-quickstarts:computer-use-demo-latest` image tag is correct.

WHAT I REFUTED. Four things.
1. **It missed the incomplete-but-correct case it was sent to find.** The demo's own README links a sibling quickstart, `claude-quickstarts/computer-use-best-practices`, a second Anthropic reference implementation that "runs natively on macOS (no container)". Its artifacts are host-side and higher value than anything in the container: `runs/<timestamp>/` holding a JSONL transcript plus JPEG screenshots of every session, a plaintext API key in a repo-root `.env` with no 0600, `config.toml`/`CU_CONFIG`, loopback listeners on 127.0.0.1:8000 and :8001, and macOS Screen Recording + Accessibility TCC grants that are durable dated proof the agent was authorised to capture the screen. `supported_os: [container]` and "Reference implementation ships as a Docker container" are correct about one of two implementations.
2. **"All four documented docker run invocations" bind-mount `$HOME/.anthropic` — they do not.** There are six blocks; five carry the mount and the **Vertex recipe omits it entirely**. On a Vertex deployment `api_key`, `system_prompt` and `error_*.md` never reach the host and die with the container. The prior finding asserts the opposite, which inverts the collection instruction for one of three providers.
3. **Its own new_rows are incomplete-but-correct.** It never fetched `start_all.sh`, which `entrypoint.sh` calls first. Two more logs exist (`/tmp/tint2_stderr.log`, `/tmp/mutter_stderr.log`, both `rm`'d on successful startup, so presence means a failed start) plus `/tmp/.X1-lock`. Also: `error_{datetime.now().timestamp()}.md` is a **float**, so the real name is `error_1771000000.123456.md` and the glob must be `error_*.md`; and the traceback only appears on the non-RateLimitError branch.
4. **Its `mcp_capable` sourcing overreached.** It claimed no MCP import "anywhere" while never fetching `computer_use_demo/tools/`. The verdict is right and I firmed it up with the check it skipped: `requirements.txt` is streamlit / anthropic[bedrock,vertex] / jsonschema / boto3 / google-auth — no MCP package is installed, so no MCP client can exist in the image.

THREE CATALOG DEFECTS NEITHER PARTY CAUGHT. The `~/.aws` row is typed `secret_type: aws-sso-token`, but the vendor documents "the host's AWS credentials file and AWS profile" — long-lived keys; SSO tokens live only in `sso/cache/`, which the README never mentions. `collection.guidance` orders the responder to "capture the MCP config" in an entry whose own `mcp_capable` is false — template boilerplate that should point at `~/.anthropic/system_prompt` instead. And `GOOGLE_APPLICATION_CREDENTIALS`, documented in the README's Vertex section as an alternative pointer to an arbitrary credential file, is a sixth credential location that is absent from the block.

Minor count corrections: the docs page carries four stale old-name links, not three; and two recipes build locally (Vertex and Development), not one. Catalog file inspected, not modified: C:\Users\depal\ai-dfir-toolkit\artifacts\catalog\claude-computer-use.yml

---

## AIRT-0011

*10 pages fetched, 10 at 2xx*

- `200` https://modelcontextprotocol.io/docs/2026-07-28/develop/connect-local-servers **(redirected)**
- `200` https://raw.githubusercontent.com/Homebrew/homebrew-cask/master/Casks/c/claude.rb
- `200` https://support.claude.com/en/articles/12611117-deploy-claude-desktop-for-macos
- `200` https://claude.com/docs/third-party/claude-desktop/data-storage
- `200` https://support.claude.com/en/articles/12622667-enterprise-configuration-for-claude-desktop
- `200` https://raw.githubusercontent.com/romgX/openrelay/main/src/cookie.ts
- `200` https://github.com/anthropics/claude-code/issues/51834
- `200` https://cyberdom.blog/the-ai-middleware-risks-in-claude-desktop/
- `200` https://support.claude.com/en/articles/10949351-getting-started-with-local-mcp-servers-on-claude-desktop
- `200` https://www.electronjs.org/docs/latest/api/app

### Claims

**[confirmed / vendor-documented]** artifacts.disk: ~/Library/Application Support/Claude/claude_desktop_config.json (os: macos, artifact_type: mcp-config, confidence: high)
  - actual: Correct and verbatim. Provenance caveat sharpened: the only page that currently states this exact string is the MCP protocol-docs site, not a support.claude.com page.
  - evidence: RE-FETCHED MYSELF (200). Under 'Access Developer Settings' the page prints verbatim: '**macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`' and '**Windows**: `%APPDATA%\Claude\claude_desktop_config.json`'. Case and the space in 'Application Support' match the catalog row exactly. Confirmed the requested URL 200s but redirects to the date-versioned canonical https://modelcontextprotocol.io/docs/2026-07-28/develop/connect-local-servers - cite the dated form. PROVENANCE NOTE for the maintainer: modelcontextprotocol.io is the MCP project's documentation site (repo under the modelcontextprotocol GitHub org), not support.claude.com. It is authoritative protocol documentation using Claude Desktop as its worked example, so I retain 'vendor-documented', but a strict reading of your taxonomy ('the vendor's own site or their own repo') would treat it as first-party-adjacent standards docs rather than the Anthropic support site proper. It is NOT an aggregator - do not read this caveat as a third-party downgrade.
  - source: https://modelcontextprotocol.io/docs/2026-07-28/develop/connect-local-servers

**[confirmed / vendor-documented]** artifacts.disk: ~/Library/Logs/Claude/mcp*.log (os: macos, artifact_type: log, confidence: high)
  - evidence: RE-FETCHED MYSELF (200). Under 'Getting logs from Claude Desktop': 'macOS: `~/Library/Logs/Claude`'; '`mcp.log` will contain general logging about MCP connections and connection failures'; 'Files named `mcp-server-SERVERNAME.log` will contain the stderr output from the named server.' The page prints the exact glob `tail -n 20 -f ~/Library/Logs/Claude/mcp*.log`, which covers both mcp.log and mcp-server-*.log. Same protocol-docs provenance caveat as the config.json row.
  - source: https://modelcontextprotocol.io/docs/2026-07-28/develop/connect-local-servers

**[corrected / vendor-documented]** Implied by the Linux rows: that macOS ~/Library/Application Support/Claude/ contains a logs/ subdirectory, mirroring host-verified ~/.config/Claude/logs/
  - actual: It does NOT. On macOS the log directory is a sibling of Application Support: ~/Library/Logs/Claude/. Do not add a ~/Library/Application Support/Claude/logs/ row.
  - evidence: I independently substantiated this, the report's strongest catch. (1) I grep-confirmed the Electron app docs verbatim: `appData` 'by default points to: %APPDATA% on Windows, $XDG_CONFIG_HOME or ~/.config on Linux, ~/Library/Application Support on macOS', and the log path 'will result in this directory being set to `~/Library/Logs/YourAppName` on macOS, and inside the `userData` directory on Linux and Windows.' So the three Claude dirs are one Electron userData dir under different names, and logs are the one thing Electron does NOT nest on macOS. (2) The MCP page lists macOS logs at ~/Library/Logs/Claude but Windows logs at %APPDATA%\Claude\logs. (3) I re-fetched the Homebrew cask and confirmed its zap list carries '~/Library/Application Support/Claude' and '~/Library/Logs/Claude' as two separate top-level entries. The Linux logs/ row must not be generalised to macOS.
  - source: https://www.electronjs.org/docs/latest/api/app

**[missing-row / third-party]** ~/.config/Claude/config.json exists (linux only); no macOS equivalent recorded
  - actual: ~/Library/Application Support/Claude/config.json exists on macOS; security research additionally reports it world-readable (0644) and holding encrypted oauth:tokenCache and dxt:allowlistCache.
  - evidence: RE-FETCHED both sources. Issue #51834 (labels platform:macos, area:desktop) contains the exact path twice: 'persist trust state to `~/Library/Application Support/Claude/config.json`' and the repro 'ls -la ~/Library/Application\ Support/Claude/config.json'. NOTE the issue alone proves only existence + role; the 0644 / oauth:tokenCache / dxt:allowlistCache attributes come solely from cyberdom.blog, which elides the middle of the path as '~/Library/.../config.json' (corroborates the file, not the full string). Both sources third-party (user-authored tracker issue; security blog). Medium confidence, as the report states.
  - source: https://github.com/anthropics/claude-code/issues/51834

**[missing-row / third-party]** ~/.config/Claude/ant-did exists (linux only); no macOS equivalent recorded
  - actual: ~/Library/Application Support/Claude/ant-did exists on macOS, with a ~/Library/Application Support/Claude/Default/ant-did fallback.
  - evidence: RE-FETCHED cookie.ts. findDeviceIdDarwin() probes exactly path.join(homedir,'Library','Application Support','Claude','ant-did') then '...','Claude','Default','ant-did'. The 'Default/' fallback is a variant the Linux row lacks. The vendor (data-storage page, 3P build) documents the file's name/role generically ('generates a random UUID and writes it (base64-encoded) to the `ant-did` file in the application-data directory'); cyberdom independently lists 'ant-did | Anthropic Device ID string | No (base64)'. The standard-build macOS path string rests on the third-party tool + blog. Sourcing third-party correct.
  - source: https://raw.githubusercontent.com/romgX/openrelay/main/src/cookie.ts

**[missing-row / third-party]** ~/.config/Claude/ config-dir root (linux only, mode 0700); macOS has no application-data-root row
  - actual: ~/Library/Application Support/Claude/ is the macOS application-data root and deserves a config-dir row. DOWNGRADED sourcing from vendor-documented to third-party. Do not copy the Linux 'mode 0700' claim - research reports config.json inside it at 0644.
  - evidence: SOURCING DOWNGRADE. The report labelled this vendor-documented, but its cited source is the Homebrew cask ('~/Library/Application Support/Claude' in the zap list), which is a COMMUNITY-maintained package repo = third-party, not the vendor. The Anthropic data-storage page it also cites documents '~/Library/Application Support/Claude-3p/' - a DIFFERENT directory for the separate 3P build - not this one. The standard-build root's existence is only vendor-INFERABLE, as the parent of the MCP-documented .../Claude/claude_desktop_config.json. So the honest label is third-party (Homebrew) plus inference-from-a-vendor-documented-child. The row itself survives - the directory unquestionably exists as the parent of the confirmed config.json - but not at vendor-documented confidence. Subdirectory claude-code-sessions/ is evidenced on macOS by issue #51834 ('Stale session files in `~/Library/Application Support/Claude/claude-code-sessions/`'), also third-party.
  - source: https://raw.githubusercontent.com/Homebrew/homebrew-cask/master/Casks/c/claude.rb

**[missing-row / vendor-documented]** Entry lacks a macOS install path (rows carry install_method: DMG but no install location)
  - actual: /Applications/Claude.app (or ~/Applications/Claude.app for a user-folder install). Distribution is .pkg (recommended for enterprise) or .dmg - so install_method: DMG is incomplete.
  - evidence: RE-FETCHED deploy-claude-desktop-for-macos (200). Verbatim: 'Claude Desktop installs to `/Applications` and updates automatically'; troubleshooting lists '`~/Applications` (user folder): Users can update without administrator privileges' vs '`/Applications` (system folder): Users need administrator access and write permissions to `/Applications`, `Claude.app`'; formats '`.pkg` - Standard macOS installer package (recommended for enterprise)' and '`.dmg` - Drag-and-drop installation'. The full string '/Applications/Claude.app' composes the vendor-documented /Applications with the bundle name Claude.app (the vendor page names Claude.app; the cask confirms `app "Claude.app"`, `depends_on macos: :monterey`). PKG being the MDM/enterprise path is a legitimate incomplete-but-correct catch on install_method.
  - source: https://support.claude.com/en/articles/12611117-deploy-claude-desktop-for-macos

**[missing-row / vendor-documented]** Entry lacks a macOS bundle identifier
  - actual: com.anthropic.claudefordesktop (helper com.anthropic.claudefordesktop.helper). Vendor documents the string as the PREFERENCE DOMAIN, not explicitly as CFBundleIdentifier.
  - evidence: RE-FETCHED enterprise-configuration (200). Verbatim: 'Claude Desktop reads preferences from the domain `com.anthropic.claudefordesktop`.' The vendor documents it as the preference domain for MDM profiles. The cask independently uses it as the bundle id for process termination ('uninstall quit: ["com.anthropic.claudefordesktop","com.anthropic.claudefordesktop.helper"]') and keys every per-app path on it. The bare-string is vendor-documented; the .helper identifier and the domain==CFBundleIdentifier equivalence are third-party (cask). Report framing is accurate.
  - source: https://support.claude.com/en/articles/12622667-enterprise-configuration-for-claude-desktop

**[missing-row / third-party]** No macOS row records where enterprise policy (incl. isLocalDevMcpEnabled) is stored, though Windows HKLM\SOFTWARE\Policies\Claude is implied
  - actual: ~/Library/Preferences/com.anthropic.claudefordesktop.plist holds user-domain preferences; MDM profiles land in the same domain. Policy keys include isLocalDevMcpEnabled, isDesktopExtensionEnabled, isDesktopExtensionDirectoryEnabled, disableAutoUpdates, allowedWorkspaceFolders, forceLoginOrgUUID.
  - evidence: RE-FETCHED both. The exact FILE PATH '~/Library/Preferences/com.anthropic.claudefordesktop.plist' appears only in the Homebrew cask zap list = third-party. The enterprise-config page confirms the DOMAIN and the full key table verbatim (all six keys above present, plus autoUpdaterEnforcementHours, effortLevel, isClaudeCodeForDesktopEnabled, secureVmFeaturesEnabled) and the Windows 'HKLM:\SOFTWARE\Policies\Claude' block - but names NO macOS file path ('through your MDM solution using configuration profiles'). isLocalDevMcpEnabled gates local MCP servers, so this is forensically material. The report correctly flags that /Library/Managed Preferences/com.anthropic.claudefordesktop.plist is inferred from convention and stated on no page - do not catalog it as documented. Sourcing third-party is right (path from cask; keys from vendor).
  - source: https://raw.githubusercontent.com/Homebrew/homebrew-cask/master/Casks/c/claude.rb

**[confirmed / third-party]** credentials: ~/Library/Application Support/Claude/ (macos, encrypted, oauth-token, 'encrypted under Keychain', confidence: medium)
  - actual: Directionally correct and sharpenable: ciphertext is the oauth:tokenCache field in ~/Library/Application Support/Claude/config.json; AES-128-CBC key is macOS Keychain generic-password service 'Claude Safe Storage', account 'Claude Key'.
  - evidence: RE-FETCHED both. cookie.ts calls: execFileSync('security',['find-generic-password','-w','-s','Claude Safe Storage','-a','Claude Key']) then pbkdf2Sync(password,'saltysalt',1003,16,'sha1') for the AES-128-CBC key. cyberdom tabulates the same item as the 'OSCrypt AES-128-CBC key' encrypting 'oauth:tokenCache in config.json' and reports config.json at mode 0644. Both third-party. Storage: encrypted remains correct; naming the file + Keychain service makes the row actionable.
  - source: https://cyberdom.blog/the-ai-middleware-risks-in-claude-desktop/

**[missing-row / third-party]** No macOS row for the Chromium Cookies database (credentials block covers OAuth tokens only)
  - actual: ~/Library/Application Support/Claude/Cookies (fallback .../Claude/Default/Cookies) is a Chromium cookie SQLite DB holding the live claude.ai sessionKey, lastActiveOrg and anthropic-device-id, encrypted with the same 'Claude Safe Storage' Keychain key. Distinct credential class from the OAuth row.
  - evidence: RE-FETCHED cookie.ts. findCookiesDbPathDarwin() probes exactly those two paths, then queries 'SELECT name, encrypted_value, host_key FROM cookies WHERE host_key LIKE \'%claude.ai%\'', switching on sessionKey / lastActiveOrg / anthropic-device-id / cf_clearance / __cf_bm; decryption is v10-prefixed AES-128-CBC with a 16-byte 0x20 IV. cyberdom independently lists 'Local App Storage (IndexedDB and Cookies)' in 'Chromium database format' holding 'the active sk-ant-xxx platform session key, allowing full user profile cloning'. Two third-party sources; medium confidence.
  - source: https://raw.githubusercontent.com/romgX/openrelay/main/src/cookie.ts

**[unverifiable / inferred]** artifacts.process: name 'Claude', os macos, persistence: 'LaunchAgent (login items)', confidence: high
  - evidence: I independently confirmed the counter-evidence. RE-FETCHED the full cask: its zap list enumerates eight user-domain locations (Application Support, Caches x2 incl .ShipIt, HTTPStorages, Logs, Preferences, Saved Application State, and the LSSharedFileList sfl entry) and contains NO ~/Library/LaunchAgents/ path. No page I opened documents a LaunchAgent for Claude Desktop; modern macOS login items register via SMAppService into the background-task store rather than a user LaunchAgent plist, so 'LaunchAgent (login items)' likely conflates two mechanisms. Absence from a zap list is not proof - this needs a live macOS host - but the confidence: high is unsupported by anything openable. The report's refusal to rubber-stamp this row is correct.
  - source: https://raw.githubusercontent.com/Homebrew/homebrew-cask/master/Casks/c/claude.rb

**[stale-reference / vendor-documented]** references[0]: 'Getting started with local MCP servers on Claude Desktop', support.claude.com article 10949351, accessed 2026-08-13
  - actual: URL is healthy (200, no redirect) but its CONTENT no longer supports the macOS path claims. The page is now about Desktop Extensions (.mcpb) and contains no filesystem path for claude_desktop_config.json on any OS.
  - evidence: RE-FETCHED MYSELF (200, no redirect). Full text covers installing .mcpb extensions, the Extension Developer section, admin allowlist controls, Enterprise Policy Controls (isDesktopExtensionEnabled / isDesktopExtensionDirectoryEnabled) and a developer FAQ. The ONLY storage statement is that sensitive manifest fields are encrypted 'using the operating system's secure storage (Keychain on macOS, Credential Manager on Windows, and your distro's keychain manager on Linux)' - which corroborates the macOS Keychain credential row but the string 'Library/Application Support' does not appear anywhere on the page. So the entry's macOS config-path claim has no citation in its own references list that states the path; the supporting citation (modelcontextprotocol.io) is not in the list and should be added. Verdict confirmed.
  - source: https://support.claude.com/en/articles/10949351-getting-started-with-local-mcp-servers-on-claude-desktop

**[missing-row / vendor-documented]** Entry treats Claude Desktop as a single application with one data directory per OS
  - actual: A second, fully separate installation mode exists - Claude Desktop on 3P - with its own tree: ~/Library/Application Support/Claude-3p/ and ~/Library/Logs/Claude-3p/ on macOS, %LOCALAPPDATA%\Claude-3p\ on Windows (legacy %APPDATA%\Claude-3p\). The catalog has zero rows for it on any platform. Scope strictly to the 3P build.
  - evidence: RE-FETCHED data-storage (200). Verbatim table: macOS 'Application data ~/Library/Application Support/Claude-3p/, Logs ~/Library/Logs/Claude-3p/'; Windows '%LOCALAPPDATA%\Claude-3p\' with 'earlier releases stored this data under %APPDATA%\Claude-3p\'. Documents high-value artifacts with no catalog equivalent: local-agent-mode-sessions/ (conversation history, uploads/ and outputs/ per session), '<sessionId>/audit.jsonl' ('Append-only log ... HMAC-chained to the previous one so edits or deletions are detectable', with companion '.audit-key'), host-creds-<random-id>.json (mode 0600 macOS) and ccd-session-secrets/<session-id>/ (mode 0700 macOS) transient plaintext inference creds, configLibrary/, cowork_plugins/, and user-visible output at ~/Claude/. This is the incomplete-but-correct miss (Windsurf-shaped): existing macOS rows are right but miss a parallel tree. Vendor-documented FOR THE 3P BUILD ONLY - the report correctly warns not to transplant these names onto the standard build; only claude-code-sessions/ and main.log/mcp.log appear in both.
  - source: https://claude.com/docs/third-party/claude-desktop/data-storage

**[missing-row / inferred]** artifacts.disk log rows cover mcp*.log only; Linux host verification additionally found main.log and ssh.log
  - actual: ~/Library/Logs/Claude/main.log is a plausible-but-inferred addition for standard macOS. DOWNGRADED sourcing from vendor-documented to inferred. ssh.log is documented nowhere and remains host-only.
  - evidence: SOURCING DOWNGRADE. RE-FETCHED data-storage: it documents main.log ('application and configuration-validation events'), cowork_vm_node.log, claude.ai-web.log, mcp.log / mcp-server-<name>.log - but explicitly in the Claude-3p LOGS DIRECTORY (~/Library/Logs/Claude-3p/), NOT the standard build's ~/Library/Logs/Claude/. The exact string '~/Library/Logs/Claude/main.log' (standard build, macOS) is stated on NO page I opened. The report's own prose concedes it is 'still inferred-for-standard-macOS' and rests partly on a Linux host I cannot verify (no host access). Labelling it vendor-documented overstates it: mcp.log/mcp-server-<name>.log naming matches the standard build, which is what makes main.log reasonable, but for standard macOS this is inference, not vendor documentation. cowork_vm_node.log and claude.ai-web.log should not be catalogued for the standard build from this page.
  - source: https://claude.com/docs/third-party/claude-desktop/data-storage

**[unverifiable / vendor-documented]** references[1..5]: mcp-quickstart, deploy-claude-desktop-for-windows, desktop-linux, and two learn.microsoft.com MSIX pages all 200 / no-redirect
  - actual: I did NOT re-fetch these five myself, so I cannot personally stand behind the 'confirmed' link-health verdict. No evidence contradicts it; it simply lacks independent re-verification in this pass.
  - evidence: HONESTY DOWNGRADE (confirmed -> unverifiable by me). My re-fetch budget went to the 10 pages carrying the macOS path/credential claims under review. I did independently confirm the healthy-200 status of the three sibling support articles I did fetch (10949351, 12611117, 12622667) and reproduced the connect-local-servers redirect-to-dated-URL. But mcp-quickstart, desktop-linux, deploy-windows, and the two MSIX pages were not re-fetched in this verification, so their 200/no-redirect status and content (the MSIX virtualization reasoning, the apt/Debian-Ubuntu Linux-beta note) are unverified by me. These are peripheral to the macOS rows and I have no reason to doubt the original result - marking unverifiable reflects only that I did not personally check them, not that they are wrong.
  - source: https://code.claude.com/docs/en/desktop-linux

### Candidate new rows

- `~/Library/Application Support/Claude/` [macos] - Application-data root (Electron userData); macOS peer of the Linux ~/.config/Claude/ config-dir row and the anchor for every other macOS disk row. Sourcing is third-party (Homebrew cask) plus inference as the parent of the vendor-documented claude_desktop_config.json - NOT vendor-documented for this exact string. Do not copy the Linux row's mode 0700 claim; research reports config.json inside it at 0644.
  - source: https://raw.githubusercontent.com/Homebrew/homebrew-cask/master/Casks/c/claude.rb
- `~/Library/Application Support/Claude/config.json` [macos] - Application settings distinct from the MCP config beside it; store for encrypted oauth:tokenCache and dxt:allowlistCache. Existence + role vendor-adjacent via issue #51834; the world-readable 0644 and the encrypted-field contents come from cyberdom.blog only. Third-party, medium confidence.
  - source: https://github.com/anthropics/claude-code/issues/51834
- `~/Library/Application Support/Claude/ant-did` [macos] - Per-install, per-OS-user device identifier written base64-encoded at first launch; ties telemetry and sessions to one install. Peer of the Linux ant-did row. A .../Claude/Default/ant-did fallback also exists. Third-party (openrelay tool + cyberdom); vendor documents the file's name/role only for the separate 3P build.
  - source: https://raw.githubusercontent.com/romgX/openrelay/main/src/cookie.ts
- `~/Library/Application Support/Claude/Cookies` [macos] - Chromium cookie SQLite DB holding the live claude.ai sessionKey, lastActiveOrg and anthropic-device-id - a distinct credential class from the OAuth row (live session cookie, full profile cloning). Fallback .../Claude/Default/Cookies. Third-party, medium confidence.
  - source: https://raw.githubusercontent.com/romgX/openrelay/main/src/cookie.ts
- `~/Library/Application Support/Claude/claude-code-sessions/` [macos] - Claude Code conversation/session history inside the desktop app's data root; evidence of what the agent was asked to do and in which workspaces. Third-party (issue #51834, platform:macos).
  - source: https://github.com/anthropics/claude-code/issues/51834
- `~/Library/Preferences/com.anthropic.claudefordesktop.plist` [macos] - macOS counterpart to the Windows HKLM\SOFTWARE\Policies\Claude registry block. Carries isLocalDevMcpEnabled etc. - whether the local-MCP execution primitive was enabled at all. The domain + policy keys are vendor-documented; the FILE PATH itself is third-party (Homebrew cask). Do NOT catalog /Library/Managed Preferences/com.anthropic.claudefordesktop.plist as documented - it is inferred from convention only.
  - source: https://raw.githubusercontent.com/Homebrew/homebrew-cask/master/Casks/c/claude.rb
- `~/Library/Logs/Claude/main.log` [macos] - Application and configuration-validation events, alongside the catalogued mcp*.log. INFERRED for standard macOS: vendor documents main.log only in the Claude-3p logs directory, and the standard-build macOS path is stated on no vendor page. Low/medium confidence; needs host confirmation.
  - source: https://claude.com/docs/third-party/claude-desktop/data-storage
- `/Applications/Claude.app` [macos] - Vendor-documented install location; ~/Applications/Claude.app for user-folder installs (updates without admin). Distribution .pkg (enterprise/MDM) or .dmg - so install_method: DMG is incomplete. Bundle/preference domain com.anthropic.claudefordesktop, helper com.anthropic.claudefordesktop.helper.
  - source: https://support.claude.com/en/articles/12611117-deploy-claude-desktop-for-macos
- `~/Library/Application Support/Claude-3p/` [macos] - Entirely separate installation mode (Claude Desktop on 3P), absent from the catalog on every platform - the incomplete-but-correct miss. Contains local-agent-mode-sessions/ history, per-session HMAC-chained audit.jsonl (+ .audit-key), configLibrary/, cowork_plugins/, and transient plaintext creds host-creds-<random-id>.json (0600) and ccd-session-secrets/<session-id>/ (0700). Logs at ~/Library/Logs/Claude-3p/; Windows %LOCALAPPDATA%\Claude-3p\ (legacy %APPDATA%\Claude-3p\). Vendor-documented for the 3P build only.
  - source: https://claude.com/docs/third-party/claude-desktop/data-storage
- `~/Library/Caches/com.anthropic.claudefordesktop.ShipIt` [macos] - Squirrel.Mac updater cache - update/version history, useful for establishing which build was running when. Siblings ~/Library/Caches/com.anthropic.claudefordesktop and ~/Library/HTTPStorages/com.anthropic.claudefordesktop also exist. Third-party (Homebrew cask zap list).
  - source: https://raw.githubusercontent.com/Homebrew/homebrew-cask/master/Casks/c/claude.rb

### Summary

Adversarial verdict: the report largely holds. I independently re-fetched 10 of the 15 cited pages (all 200) and verified every load-bearing quote verbatim, including grep-confirming the two Electron quotes. I made three corrections; nothing in the report was found to be fabricated or to carry a wrong path string (case and separators all check out).\n\nCONFIRMED and survives: (1) Both catalogued macOS rows are exact-match on modelcontextprotocol.io - ~/Library/Application Support/Claude/claude_desktop_config.json and the ~/Library/Logs/Claude/mcp*.log glob - and I reproduced the redirect to the date-versioned canonical URL, so cite the dated form. (2) The strongest catch, that macOS has NO ~/Library/Application Support/Claude/logs/ subdir, is fully substantiated: Electron nests logs at ~/Library/Logs/<App> on macOS but inside userData on Linux/Windows, the MCP page and the cask agree, so the Linux logs/ row must not be generalised. (3) The stale-reference catch is correct - support article 10949351 is 200 but has been rewritten around Desktop Extensions and no longer contains any filesystem path, so the entry's macOS config-path claim has no supporting URL in its own reference list. (4) The Claude Desktop on 3P parallel tree is real and vendor-documented (3P build only). (5) The LaunchAgent process row is correctly flagged unverifiable - I confirmed the cask's eight-entry footprint has no LaunchAgents path.\n\nCORRECTIONS I made (downgrades I can defend): (a) The ~/Library/Application Support/Claude/ config-dir root row: sourcing vendor-documented -> THIRD-PARTY. Its cited source is the Homebrew cask (community repo, third-party), and the vendor data-storage page it also leans on documents ~/Library/Application Support/Claude-3p/, a different directory. The root's existence is vendor-inferable only as the parent of the MCP-documented config.json. (b) The ~/Library/Logs/Claude/main.log row: sourcing vendor-documented -> INFERRED. The vendor documents main.log only in the Claude-3p logs directory; the standard-build macOS path is stated on no page I opened and rests partly on an unverifiable Linux host. (c) references[1..5] (mcp-quickstart, deploy-windows, desktop-linux, two MSIX pages): confirmed -> UNVERIFIABLE-by-me, because I did not re-fetch those five this pass; no evidence contradicts them, but I cannot personally vouch for the link-health verdict.\n\nOne provenance caveat, not a downgrade: the two catalogued macOS path claims rest on modelcontextprotocol.io, the MCP protocol-docs site rather than support.claude.com. It is authoritative first-party-quality documentation (not an aggregator), so I retained vendor-documented, but flag that a strict reading of the taxonomy would treat it as first-party-adjacent standards docs - and it is now the ONLY page stating those exact strings, since the previously-cited support article dropped them.\n\nThe third-party rows (config.json, ant-did, Cookies, the .plist file path) correctly sit at medium confidence on user-authored tracker issues, an unofficial reverse-engineering tool, and a security blog that elides its own paths as ~/Library/.../config.json - the maintainer should treat them as leads for host verification, not settled facts.

---

## AIRT-0018 LM Studio

*26 pages fetched, 26 at 2xx*

- `200 (GitHub contents API, blob sha 6e0e97787b284e51752c53e4ad08b88209c9bd8f)` https://github.com/lmstudio-ai/lmstudio-js/blob/main/packages/lms-common-server/src/findLMStudioHome.ts
- `200 (GitHub contents API, blob sha fe1f2ea07a86c65d12f46e17e50d2311293e2d27)` https://github.com/lmstudio-ai/lms/blob/main/src/lmstudioPaths.ts
- `200 (GitHub contents API, blob sha 2ca07e70fe04517c1b9fdb905065be38f44c5a9d)` https://github.com/lmstudio-ai/lms/blob/main/src/subcommands/importCmd.ts
- `200 (GitHub contents API, blob sha a76c329b083bfba1379f1c0950030f96819da598)` https://github.com/lmstudio-ai/mlx-engine/blob/main/mlx_engine/__init__.py
- `200 (GitHub contents API, blob sha 7ca336f3d2a277bca3cad7eaf2102ee590974086)` https://github.com/lmstudio-ai/lmstudio-js/blob/main/packages/lms-lmstudio/src/installCli/index.ts
- `200 (GitHub contents API, blob sha 879185dd3ef446a28d3c6fdd5ef25aa117af7bdc)` https://github.com/lmstudio-ai/lmstudio-js/blob/main/packages/lms-es-plugin-runner/src/UtilBinary.ts
- `200 (GitHub contents API, blob sha e1ec860859e0436dd17fe097f771b0d12e1a24de)` https://github.com/lmstudio-ai/lmstudio-js/blob/main/packages/lms-es-plugin-runner/src/NodePluginInstaller.ts
- `200 (GitHub contents API, blob sha e205247ce746e26fac3a7c1d439f6fab402a202a)` https://github.com/lmstudio-ai/lmstudio-js/blob/main/packages/lms-common-server/src/findOrStartLlmster.ts
- `200 (GitHub contents API, blob sha ab59222cea6ed01cbe672938058acf387768f523)` https://github.com/lmstudio-ai/docs/blob/main/0_app/5_advanced/import-model.md
- `200` https://lmstudio.ai/blog/lmstudio-v0.3.17
- `200` https://lmstudio.ai/docs/app/mcp
- `200` https://lmstudio.ai/docs/app/mcp **(redirected)**
- `200` https://lmstudio.ai/docs/app
- `200` https://lmstudio.ai/docs/app/basics/chat
- `200` https://lmstudio.ai/docs/app/presets
- `200` https://lmstudio.ai/docs/app/presets/import
- `200` https://lmstudio.ai/docs/developer/core/authentication
- `200 (GitHub issues API)` https://github.com/lmstudio-ai/lmstudio-bug-tracker/issues/1371
- `200 (GitHub issues API)` https://github.com/lmstudio-ai/lmstudio-bug-tracker/issues/1813
- `200 (0 results)` https://api.github.com/search/code?q=repo%3Almstudio-ai%2Fdocs+%22settings.json%22
- `200 (8 results)` https://api.github.com/search/code?q=repo%3Almstudio-ai%2Fdocs+%22mcp.json%22
- `200 (12 results)` https://api.github.com/search/code?q=repo%3Almstudio-ai%2Fdocs+%22.lmstudio%22
- `200 (1 result)` https://api.github.com/search/code?q=repo%3Almstudio-ai%2Fdocs+credentials
- `200 (2 results)` https://api.github.com/search/code?q=repo%3Almstudio-ai%2Fdocs+config-presets
- `200 (9 results, all in lmstudio-ai/docs plus one localization string file; zero in code)` https://api.github.com/search/code?q=org%3Almstudio-ai+%22mcp.json%22+NOT+path%3Adocs
- `200 (1 result)` https://api.github.com/search/code?q=repo%3Almstudio-ai%2Flmstudio-js+%22new+UtilBinary%28%22

### Claims

**[confirmed / vendor-documented]** PRIOR AGENT'S CORE FINDING: findLMStudioHome() resolves the LM Studio home with no platform branching, in the order (1) ~/.lmstudio-home-pointer, (2) ~/.cache/lm-studio if it exists, (3) fallback ~/.lmstudio — so ~/.cache/lm-studio outranks ~/.lmstudio on every OS and the catalog's macOS framing is wrong
  - actual: Confirmed verbatim and cross-platform. The catalog's os:[macos] framing on the cache-home row is wrong; the discriminator is install history, not operating system.
  - evidence: I re-fetched this file independently and got blob sha 6e0e97787b284e51752c53e4ad08b88209c9bd8f — byte-identical to the sha the prior agent reported, which is itself proof they opened the real file rather than paraphrasing an aggregator. The function body is exactly as described: realpathSync(homedir()), then `const pointerFilePath = join(resolvedHomeDir, ".lmstudio-home-pointer"); if (existsSync(pointerFilePath)) { lmstudioHome = readFileSync(pointerFilePath, "utf-8").trim(); return lmstudioHome; }`, then the comment `// See if ~/.cache/lm-studio exists. If it does, use it.` with `join(resolvedHomeDir, ".cache", "lm-studio")`, then `// Otherwise, fallback to ~/.lmstudio`. There is no `process.platform` reference anywhere in the file. This is the strongest claim in the submission and it survives fully. I add one detail the prior agent omitted: in branches (2) and (3) the function WRITES the resolved path back to the pointer file, so the pointer is created as a side effect of resolution, not only by the app.
  - source: https://github.com/lmstudio-ai/lmstudio-js/blob/main/packages/lms-common-server/src/findLMStudioHome.ts

**[corrected / inferred]** PRIOR AGENT: mcp row 2 ~/.cache/lm-studio/mcp.json is 'vendor-documented' and 'confidence can rise to high on vendor source'
  - actual: ~/.cache/lm-studio/mcp.json is a well-founded inference, not a vendor-documented path. The home resolution is vendor-documented; mcp.json's home-relativity is not. Keep confidence medium and sourcing inferred. The os list should still widen to windows/macos/linux.
  - evidence: This is my strongest refutation. No vendor source anywhere locates mcp.json relative to the LM Studio home. I ran an org-wide code search (org:lmstudio-ai "mcp.json") and got 9 hits: 8 markdown files in lmstudio-ai/docs and one localization string file (lmstudio-ai/localization en/developer.json). ZERO hits in lmstudio-ai/lms or lmstudio-ai/lmstudio-js. lmstudioPaths.ts — the file that enumerates the home-relative paths — defines pluginsFolderPath, lmsKey2Path, cliPrefPath, appInstallLocationFilePath, llmsterInstallLocationFilePath, defaultModelsFolder, serverCtlPath, serverConfigPath and apiServerInfoPath, and does NOT define an mcp.json path. So `~/.cache/lm-studio/mcp.json` is a COMPOSITION of two separately-sourced facts — a vendor-documented home resolution, times a filename the vendor only ever stated relative to ~/.lmstudio — joined by the assumption that mcp.json is home-relative. That assumption is reasonable and is weakly corroborated by two user reports, but it is inference, not vendor documentation. Raising this row to high confidence on 'vendor source' would repeat the Windsurf failure in a subtler form: composing a plausible path and labelling the composite as documented.
  - source: https://github.com/lmstudio-ai/lmstudio-js/blob/main/packages/lms-common-server/src/findLMStudioHome.ts

**[corrected / vendor-documented]** PRIOR AGENT: 'lmstudio-ai/mlx-engine hardcodes _set_outlines_cache_dir(Path("~/.cache/lm-studio/.internal/outlines")) ... which independently confirms both that .internal exists on macOS and that the cache home is live there'
  - actual: Vendor source, correctly quoted, but over-read. It confirms an mlx-engine cache path on macOS; it does not confirm the resolved LM Studio home. Presence of ~/.cache/lm-studio alone must not be treated as proof of the home.
  - evidence: I fetched the file (blob sha a76c329b083bfba1379f1c0950030f96819da598, matching the reported sha) and the final line is exactly `_set_outlines_cache_dir(Path("~/.cache/lm-studio/.internal/outlines"))`. But note what it is NOT: it is a HARDCODED literal, not a call to findLMStudioHome(). It therefore does not establish that the resolved LM Studio home on that macOS host is ~/.cache/lm-studio — mlx-engine points Outlines at that directory unconditionally, whatever the home resolves to. The prior agent's second inference ('the cache home is live there') does not follow from this file. Worse, it inverts into a forensic false-positive trap the catalog should record: because mlx-engine writes under ~/.cache/lm-studio regardless, the mere existence of ~/.cache/lm-studio on a Mac is NOT proof it is the LM Studio home. Only ~/.lmstudio-home-pointer settles that. The narrow claim — that a .internal subtree exists under ~/.cache/lm-studio on macOS — does survive.
  - source: https://github.com/lmstudio-ai/mlx-engine/blob/main/mlx_engine/__init__.py

**[corrected / third-party]** PRIOR AGENT: 'bug-tracker issue #1813 is a WINDOWS 10 user on 0.4.1.1 reporting mcp.json at C:\Users\SystemUser\.cache\lm-studio' — used as cross-platform corroboration
  - actual: A single uncorroborated Windows user report describing an attempted workaround path, not an observed mcp.json location. Third-party, low weight. The cross-platform conclusion stands on vendor source alone.
  - evidence: I fetched the issue. It is real, open, Windows 10 pro, LM Studio 0.4.1.1, opened 2026-04-16 by Fabian00000 (author_association NONE), last updated 2026-05-26, and it has 2 comments — the prior agent's write-up did not mention the comment count. But the characterisation is generous. The user did not report finding mcp.json there; reproduction step 6 reads: 'Adding the entries here (C:\Users\SystemUser\.cache\lm-studio) does not solve it either, any attempt to save within the program rolls back the file to the previous default state'. That is an attempted workaround location, and 'SystemUser' reads as a redacted placeholder rather than a real account name. It is weak evidence that the directory exists on a Windows host, and essentially no evidence about which path the app actually reads. The cross-platform conclusion is carried entirely by findLMStudioHome.ts, which needs no corroboration; this issue should be cited as colour, not as proof.
  - source: https://github.com/lmstudio-ai/lmstudio-bug-tracker/issues/1813

**[corrected / vendor-documented]** PRIOR AGENT: 'credentials returns ZERO hits in the lmstudio-ai/docs repo' — used to justify verdict unverifiable on the ~/.lmstudio/credentials/ row
  - actual: One hit, not zero. No credentials directory is documented, so the row stays unverifiable — but vendor docs do confirm remote-MCP OAuth token handling, which raises the priority of locating that store.
  - evidence: Factually wrong as stated. I ran the same search and got 1 hit, not zero: 4_integrations/1_mcp-remote/index.md contains the heading 'Connecting with your own OAuth credentials' followed by 'Some services require you to bring your own OAuth app, either because they don't support dynamic client registration, or because they need a specific redirect URL whitelisted in their developer portal.' The prior agent's bottom line — that no vendor page documents a credentials DIRECTORY — still holds, and the row stays unverifiable for macOS. But the miss matters in the wrong direction: the vendor documents that LM Studio performs OAuth against remote MCP servers, including dynamic client registration. Those tokens must be persisted somewhere on disk. That makes a credentials/ directory MORE forensically interesting, not less, and it bears directly on the entry's plaintext_credentials: null field.
  - source: https://github.com/lmstudio-ai/docs/blob/main/4_integrations/1_mcp-remote/index.md

**[corrected / inferred]** PRIOR AGENT new_row: '<lmstudio-home>/.internal/utils/ ... UtilBinary resolves join(findLMStudioHome(), ".internal", "utils") and spawns from it — including a bundled node runtime used by the plugin runner. A general-purpose interpreter inside a user-writable directory'
  - actual: <lmstudio-home>/.internal/utils/ is vendor-source-confirmed and does hold spawned executables. The only one named in open source is esbuild. Drop the 'bundled node runtime' assertion — it is unsupported.
  - evidence: The directory claim is confirmed — UtilBinary.ts (sha 879185dd3ef446a28d3c6fdd5ef25aa117af7bdc) contains `const utilsFolder = utilsFolderPathOverride ?? join(findLMStudioHome(), ".internal", "utils");` and `this.path = join(utilsFolder, process.platform === "win32" ? `${name}.exe` : name);`, and it spawns from that path. The 'bundled node runtime' is NOT. I searched the whole lmstudio-js repo for `new UtilBinary(` and there is exactly ONE instantiation in the codebase: NodePluginInstaller.ts line `const esbuild = new UtilBinary("esbuild", { utilsFolderPathOverride });`. It is esbuild, a bundler, not node, and not a general-purpose interpreter. The class is generic over a binary name so other binaries may be dropped in that folder by the closed-source app, but no vendor source I opened names node. The local_code_execution point survives on the real evidence — the app spawns an executable from a user-writable directory under the home, and NodePluginInstaller also runs `new Arborist({...}).reify()`, i.e. an npm dependency install, though notably with ignoreScripts: true.
  - source: https://github.com/lmstudio-ai/lmstudio-js/blob/main/packages/lms-es-plugin-runner/src/NodePluginInstaller.ts

**[corrected / inferred]** PRIOR AGENT new_row: '~/.lmstudio-home-pointer ... LM Studio writes it on first resolution, so it is present on essentially every host. Collect it before anything else.'
  - actual: Vendor-source-confirmed as a file, its format and its precedence. The 'present on essentially every host' ubiquity claim is inference and should be dropped; absence of the pointer proves nothing.
  - evidence: The file, its contents (a trimmed absolute path), and its first-checked precedence are all vendor-source-confirmed — I verified the writeFileSync(pointerFilePath, lmstudioHome, "utf-8") calls in both the cache-home and fallback branches. The triage value is real and I endorse the row. But 'present on essentially every host' is not established by anything either of us opened. findLMStudioHome() lives in @lmstudio/lms-common-server and is called from the lms CLI and the TypeScript SDK; whether the closed-source Electron app invokes this same code path on startup is not shown in any open-source file. A host where the operator only ever used the GUI and never ran lms or the SDK may have no pointer file. Treat absence of the pointer as uninformative rather than as evidence the home is ~/.lmstudio.
  - source: https://github.com/lmstudio-ai/lmstudio-js/blob/main/packages/lms-common-server/src/findLMStudioHome.ts

**[corrected / vendor-documented]** mcp row 1: config_path ~/.lmstudio/mcp.json, confidence high, notes 'This is the documented path'
  - actual: Documented in exactly one place, the 0.3.17 blog post, and only for the default-home case. The current docs state no path. Confidence high is not warranted; note the blog's forward-slash Windows form.
  - evidence: I fetched the blog post (200, no redirect) and the sentence is verbatim: '`mcp.json` lives in `~/.lmstudio/mcp.json` on macOS and Linux, and in `%USERPROFILE%/.lmstudio/mcp.json` on Windows. It's recommended to use the in-app editor to edit this file.' I also fetched https://lmstudio.ai/docs/app/mcp (200) and confirm it names 'the app's mcp.json file' with no filesystem path anywhere on the page. I went further than the prior agent and grepped the docs repo for '.lmstudio' rather than for 'mcp.json': 12 files, and the only filesystem-path statements are conversations, config-presets, hub, models and a Linux systemd bin example. There is no .lmstudio/mcp.json string anywhere in the current docs. One exact-string detail the catalog should carry: the blog writes the Windows form with FORWARD slashes, `%USERPROFILE%/.lmstudio/mcp.json`, whereas every current docs page uses backslashes (`%USERPROFILE%\.lmstudio\conversations`). Reproduce the blog form as written.
  - source: https://lmstudio.ai/blog/lmstudio-v0.3.17

**[confirmed / third-party]** Issue #1371 shows the documented mcp.json path is wrong on macOS
  - actual: One uncorroborated user report, unanswered by the vendor in seven months. The behaviour is real; the macOS attribution in the title is the reporter's inference, not a vendor statement.
  - evidence: I re-fetched the issue and every metadata detail the prior agent gave checks out exactly: author dial481, author_association NONE, state open, created_at 2026-01-14T11:12:08Z, updated_at identical to created_at (never touched since), no comments field returned (zero comments), no labels, no assignee, no linked PRs, zero reactions. Body confirms LM Studio 0.3.33 / macOS 26.2, the claim that ~/.lmstudio 'does not even exist', the block-quoted blog sentence, and the reporter's own hedge 'A copy of LM Studio's mcp.json appears in ~/.cache/lm-studio/mcp.json but this looks like a temporary file?'. The prior agent's reading is right: the title mis-attributes an install-history artifact to the operating system. I confirm their correction rather than refuting it.
  - source: https://github.com/lmstudio-ai/lmstudio-bug-tracker/issues/1371

**[confirmed / vendor-documented]** disk row: ~/.lmstudio/settings.json — 'Server bind address and port, absent on Windows 0.3.22'
  - actual: <lmstudio-home>/settings.json. Vendor-source-confirmed, vendor-documented nowhere. Holds downloadsFolder, not server bind/port. Absence is normal, not anomalous.
  - evidence: I verified all three of the prior agent's corrections and they hold. (1) repo:lmstudio-ai/docs "settings.json" returns total_count 0 — genuinely zero, unlike their credentials claim. (2) importCmd.ts (sha 2ca07e70fe04517c1b9fdb905065be38f44c5a9d, matching) contains locateSettingsJson() with `const lmstudioHome = findLMStudioHome(); const settingsJsonFilePath = join(lmstudioHome, "settings.json");` — home-relative, confirmed. (3) The description correction is right: resolveModelsFolderPath() reads `modelsFolderPath = settings.downloadsFolder` and throws if it is not a string, so settings.json carries the models directory. Server bind/port is serverConfigPath = join(lmstudioHome, ".internal", "http-server-config.json") in lmstudioPaths.ts. The lazy-creation explanation also checks out: locateSettingsJson returns null on ENOENT and the caller warns 'Could not locate LM Studio configuration file, using default path:'.
  - source: https://github.com/lmstudio-ai/lms/blob/main/src/subcommands/importCmd.ts

**[confirmed / vendor-documented]** disk rows: ~/.lmstudio/.internal/ and its contents are os:[windows] only
  - actual: <lmstudio-home>/.internal/ on windows, macos and linux. Confirmed on vendor source.
  - evidence: Refuted correctly by the prior agent and I confirm independently. lmstudioPaths.ts (sha fe1f2ea07a86c65d12f46e17e50d2311293e2d27, matching) builds lmsKey2Path, cliPrefPath, appInstallLocationFilePath, llmsterInstallLocationFilePath, serverCtlPath, serverConfigPath and apiServerInfoPath as join(lmstudioHome, ".internal", ...) with no platform branch anywhere in the file. I additionally fetched findOrStartLlmster.ts (sha e205247ce746e26fac3a7c1d439f6fab402a202a), which the prior agent cited but did not list in pages_fetched: it independently builds the same two install-location paths via getAppInstallLocationFilePath()/getLlmsterInstallLocationFilePath(), also unbranched. The Windows-only os list on these rows is wrong.
  - source: https://github.com/lmstudio-ai/lms/blob/main/src/lmstudioPaths.ts

**[confirmed / vendor-documented]** disk row: ~/.lmstudio/bin/lms | ~/.lmstudio/bin/lms.exe — correct but incomplete (the incomplete-but-correct case)
  - actual: <lmstudio-home>/bin/lms — both ~/.lmstudio/bin/lms and ~/.cache/lm-studio/bin/lms occur. Confirmed on vendor source; the third-party shell-profile corroboration is unverified and should be dropped from the citation.
  - evidence: The incomplete-but-correct finding holds, and I verified the load-bearing source the prior agent cited but omitted from its pages_fetched list. installCli/index.ts (sha 7ca336f3d2a277bca3cad7eaf2102ee590974086) contains `const targetPath = join(`${findLMStudioHome()}`, `bin`);` verbatim, then branches only on platform for HOW to add it to PATH (installCliWin32 vs installCliDarwinOrLinux), never for WHERE. So the bin directory is home-relative on all three OSes and ~/.cache/lm-studio/bin/lms is a real second location. I also confirm the docs-side observation: the only .lmstudio/bin string in the entire docs repo is in 1_developer/0_core/headless_llmster.mdx, a Linux systemd unit using /home/YOUR_USERNAME/.lmstudio/bin/lms. Caveat: I could NOT verify the macOS shell-profile evidence ('# Added by LM Studio CLI (lms)' with /Users/bmachiraju/.cache/lm-studio/bin) — no URL was given for it and I did not locate it. The conclusion does not need it.
  - source: https://github.com/lmstudio-ai/lmstudio-js/blob/main/packages/lms-lmstudio/src/installCli/index.ts

**[confirmed / vendor-documented]** disk row: ~/.lmstudio/models/ — 'Publisher/model/*.gguf layout', vendor-documented for Mac
  - actual: <lmstudio-home>/models/ by default, overridable to an arbitrary path via settings.json downloadsFolder. Vendor-documented without any OS qualifier.
  - evidence: I fetched import-model.md (sha ab59222cea6ed01cbe672938058acf387768f523) — another source the prior agent cited but left out of pages_fetched. It shows exactly `~/.lmstudio/models/` over `└── publisher/ └── model/ └── model-file.gguf`, plus a worked infra-ai/ocelot-v1 example. The incompleteness finding holds: defaultModelsFolder = join(lmstudioHome, "models") in lmstudioPaths.ts, and importCmd.ts overrides it with settings.downloadsFolder. One small correction to the prior agent: the page carries NO operating-system label at all, so 'vendor-documented for Mac' overstates it — the tilde form implies POSIX but the page never says macOS, and no %USERPROFILE% form is given for models anywhere in the docs.
  - source: https://github.com/lmstudio-ai/docs/blob/main/0_app/5_advanced/import-model.md

**[confirmed / vendor-documented]** references: 'LM Studio app docs' /docs/app, 'lms - LM Studio CLI' /docs/cli, 'lmstudio-ai/docs' — all resolve clean
  - actual: /docs/app confirmed clean by me; /docs/cli and the repo root carried forward unverified in this pass. Docs repo head sha independently corroborated.
  - evidence: I re-fetched /docs/app: 200, final url identical, no redirect. I did not independently re-fetch /docs/cli or the docs repo root, so I carry those two forward on the prior agent's report rather than asserting them myself. I did independently confirm the docs repo default-branch head the prior agent quoted — every code-search result I ran against lmstudio-ai/docs came back pinned to ref=b02d17517b73c51f520cd5129855cdf30e0728f7, matching their reported head exactly.
  - source: https://lmstudio.ai/docs/app

**[confirmed / vendor-documented]** stale-reference: /docs/app/plugins/mcp redirects to /docs/app/mcp, and the vendor's own /docs/app page still links to the stale form
  - actual: Confirmed. Cite /docs/app/mcp. The vendor's own index page is the source of the stale link and will keep re-seeding it.
  - evidence: Both halves confirmed independently. Fetching https://lmstudio.ai/docs/app/plugins/mcp returned statusCode 200 with sourceURL https://lmstudio.ai/docs/app/plugins/mcp and final url https://lmstudio.ai/docs/app/mcp — a redirect, serving the identical 'Use MCP Servers' page. I then pulled the link graph of https://lmstudio.ai/docs/app and it does still contain both https://lmstudio.ai/docs/app/plugins/mcp and https://lmstudio.ai/docs/app/plugins/mcp/deeplink. The docs source confirms the move: the repo path is now 0_app/2_mcp/index.mdx, and the served page's own internal links all point at /docs/app/mcp/deeplink. Worth flagging as an active hazard, not just history — an agent crawling /docs/app today harvests the stale form.
  - source: https://lmstudio.ai/docs/app

**[confirmed / vendor-documented]** Entry framing that LM Studio is at 0.3.x; catalogued Windows host checked at 0.3.22
  - actual: Current LM Studio is 0.4.x. Both the 0.3.22 host check and the 0.3.33 issue are pre-0.4 evidence and should be dated. 0.4.x adds API tokens and an auth toggle the entry does not cover.
  - evidence: I fetched the Authentication page (200, no redirect) — a page the prior agent cited but did not list in pages_fetched — and the section heading reads verbatim 'Requires LM Studio 0.4.0 or newer.' Independent corroboration from the two issues I pulled: #1813 is 0.4.1.1 (opened 2026-04-16). The 'Bionic' product name is now surfaced across the docs and blog, including a 'Download LM Studio Bionic' CTA on the 0.3.17 post. The staleness point stands. I would add one item the prior agent missed that matters for this entry's abuse_potential: the Authentication page documents API tokens with configurable permissions and a 'Require Authentication for each request' server toggle (off by default), and its REST example references 'allowing calling servers from mcp.json' as a separate server setting — all 0.4.x-era exposure surface the catalog does not model at all.
  - source: https://lmstudio.ai/docs/developer/core/authentication

**[corrected / third-party]** PROCESS: the prior agent's pages_fetched list is a complete record of the sources behind its claims
  - actual: pages_fetched under-reports by at least seven sources. All seven verified good on re-fetch. No fabricated source found in this submission.
  - evidence: Not a path defect but a provenance defect the maintainer should know about, because the whole review protocol rests on that list. At least seven sources the prior agent cites in its claims or new_rows do not appear in its pages_fetched array: import-model.md, /docs/developer/core/authentication, /docs/app/presets, /docs/app/presets/import, lmstudio-js installCli/index.ts, lmstudio-js UtilBinary.ts, and lmstudio-js findOrStartLlmster.ts. I fetched all seven myself and every one of them checks out, so this is a reporting-hygiene failure rather than fabrication — and the blob-sha matches on the four files they did list are strong evidence they were genuinely opening files. But a citation list that omits load-bearing sources cannot be audited, which is the same class of problem as the redirecting-citation issue the ground rules call out.
  - source: https://github.com/lmstudio-ai/docs

### Candidate new rows

- `~/.lmstudio-home-pointer  |  %USERPROFILE%\.lmstudio-home-pointer` [windows, macos, linux] - Highest-value triage artifact for this entry and absent from the catalog. Plain-text file in the user's home directory whose entire trimmed contents are the absolute path of the LM Studio home; findLMStudioHome() checks it FIRST, before any default. Read it and you know where mcp.json, settings.json, .internal/, models/, conversations/ and bin/ actually live on that host, including relocated and portable installs. Note the path string is a home-relative filename, not a vendor-published path string — no vendor page states it. Caveat against the prior pass: the write happens inside the CLI/SDK resolver, so a GUI-only host may not have one. Absence is uninformative; do not read it as proof the home is ~/.lmstudio.
  - source: https://github.com/lmstudio-ai/lmstudio-js/blob/main/packages/lms-common-server/src/findLMStudioHome.ts
- `~/.cache/lm-studio/  (legacy LM Studio home, higher precedence than ~/.lmstudio)` [windows, macos, linux] - Deserves its own row rather than a footnote on one mcp row. findLMStudioHome() tests this directory before falling back to ~/.lmstudio, so wherever it is the resolved home it is the live config root for the entire entry. A responder who greps only ~/.lmstudio on such a host records a false negative across every row in AIRT-0018. Two cautions the prior pass got wrong: this is install-history-driven, not macOS-specific; and its mere existence does NOT prove it is the home, because mlx-engine hardcodes an Outlines cache under ~/.cache/lm-studio/.internal/outlines regardless of what the home resolves to. Confirm against the pointer file.
  - source: https://github.com/lmstudio-ai/lmstudio-js/blob/main/packages/lms-common-server/src/findLMStudioHome.ts
- `<lmstudio-home>/.internal/http-server-config.json` [windows, macos, linux] - The file the catalog currently mis-attributes to settings.json. Vendor source: serverConfigPath = join(lmstudioHome, ".internal", "http-server-config.json"). Where the OpenAI-compatible server's port and bind address live, so it is the artifact that proves whether 'Serve on Local Network' was on — the exact exposure the entry's abuse_potential is about. Configuration evidence, high forensic value.
  - source: https://github.com/lmstudio-ai/lms/blob/main/src/lmstudioPaths.ts
- `<lmstudio-home>/.internal/http-server.json  and  <lmstudio-home>/.internal/http-server-ctl.json` [windows, macos, linux] - apiServerInfoPath and serverCtlPath in vendor source. Runtime state of the local API server — evidence the listener was actually running, which disk presence of the binary does not prove. Corroborating detail from findOrStartLlmster.ts: the SDK probes http://127.0.0.1:<port>/lms-status and a live server returns package, version and port, so these files pair with a known status endpoint.
  - source: https://github.com/lmstudio-ai/lms/blob/main/src/lmstudioPaths.ts
- `<lmstudio-home>/.internal/app-install-location.json  and  <lmstudio-home>/.internal/llmster-install-location.json` [windows, macos, linux] - Each holds a JSON object of {path, argv, cwd} naming the on-disk install location of the GUI app and of the llmster daemon. On macOS this is how you find a relocated .app bundle without relying on a running process. Forensically sharper than the prior pass noted: findOrStartLlmster reads these files and SPAWNS whatever path they contain — on Windows via powershell.exe Start-Process with -WindowStyle Hidden. A tampered llmster-install-location.json is a persistence and hidden-execution primitive, and llmster is always preferred over the app when both are present. The catalog currently carries llmster as a bare process name with no locator.
  - source: https://github.com/lmstudio-ai/lmstudio-js/blob/main/packages/lms-common-server/src/findOrStartLlmster.ts
- `<lmstudio-home>/.internal/lms-key-2` [windows, macos, linux] - lmsKey2Path in vendor source. A local auth key gating CLI-to-app control. The entry currently has credentials: [] and plaintext_credentials: null — this is a credential-class artifact that belongs in the assessment. Check existence, timestamps and mode only; do not read it.
  - source: https://github.com/lmstudio-ai/lms/blob/main/src/lmstudioPaths.ts
- `<lmstudio-home>/.internal/utils/  (spawned executables; .exe suffix on win32)` [windows, macos, linux] - UtilBinary resolves join(findLMStudioHome(), ".internal", "utils") and spawns binaries from it. Executables in a user-writable directory under the user's home, spawned by the app, are directly relevant to local_code_execution: true. Correcting the prior pass: the only binary named in open source is esbuild (NodePluginInstaller.ts, `new UtilBinary("esbuild", ...)`) — there is no bundled node runtime in any vendor source I opened, so do not carry that claim. Related surface in the same file: plugin install runs an npm Arborist reify, though with ignoreScripts: true.
  - source: https://github.com/lmstudio-ai/lmstudio-js/blob/main/packages/lms-es-plugin-runner/src/UtilBinary.ts
- `<lmstudio-home>/extensions/plugins/` [windows, macos, linux] - pluginsFolderPath in vendor source. MCP servers are surfaced as plugins — the REST docs use integration ids of the form mcp/<server_label> — so this is a second code-execution surface alongside mcp.json and it is not in the catalog. Plugin builds also write a .lmstudio cache folder inside each plugin directory containing a generated entry.ts and production.js.
  - source: https://github.com/lmstudio-ai/lms/blob/main/src/lmstudioPaths.ts
- `~/.lmstudio/conversations/  |  %USERPROFILE%\.lmstudio\conversations` [windows, macos, linux] - Vendor-documented verbatim on a live docs page under the heading 'Conversations folder filesystem path', with those exact two strings (trailing slash on the Mac/Linux form, none on the Windows form) — and entirely missing from the catalog. Chat transcripts in JSON: what the operator actually asked the model, the highest-value evidence in most AI-assistant investigations. The same page states 'Conversations are stored in JSON format' and warns against relying on their structure. Home-relative in practice, so also occurs under ~/.cache/lm-studio/conversations/.
  - source: https://lmstudio.ai/docs/app/basics/chat
- `~/.lmstudio/hub  |  %USERPROFILE%\.lmstudio\hub` [windows, macos, linux] - Vendor-documented verbatim: 'Presets you share, and ones you download from the LM Studio Hub are saved in ~/.lmstudio/hub on macOS and Linux, or %USERPROFILE%\.lmstudio\hub on Windows.' Holds presets pulled from LM Studio Hub — third-party content downloaded onto the host, so a supply-chain and system-prompt-tampering surface. Note the vendor writes no trailing slash on either form.
  - source: https://lmstudio.ai/docs/app/presets/import
- `~/.lmstudio/config-presets  |  %USERPROFILE%\.lmstudio\config-presets` [windows, macos, linux] - Vendor-documented under 'Where Presets are stored' with separate macOS-or-Linux and Windows code blocks. Correcting the prior pass, which rendered this as ~/.lmstudio/config-presets/ with a trailing slash and omitted the Windows form entirely — the vendor writes neither a trailing slash nor a POSIX-only form. Presets are JSON carrying system prompts and inference parameters, so this is where a persistent instruction override would live. The docs also note legacy 0.2.x presets remain readable and old files are NOT deleted on migration, so stale prompt artifacts can survive here.
  - source: https://lmstudio.ai/docs/app/presets

### Summary

VERDICT: the prior agent's central finding is correct and I could not break it. Most of what I refuted is in the sourcing labels and the supporting evidence, not the conclusions.

WHAT SURVIVES. I re-fetched four of the cited GitHub blobs and got byte-identical SHAs to the ones reported (findLMStudioHome.ts 6e0e977…, lmstudioPaths.ts fe1f2ea…, importCmd.ts 2ca07e7…, mlx-engine __init__.py a76c329…), which is strong evidence the agent opened real files rather than paraphrasing an aggregator. findLMStudioHome() is exactly as quoted, with no `process.platform` anywhere in it, so `~/.cache/lm-studio` really does outrank `~/.lmstudio` on every OS and the catalog's macOS framing really is wrong. The 0.3.17 blog sentence is verbatim. `/docs/app/mcp` really states no filesystem path. `/docs/app/plugins/mcp` really 302s, and I confirmed the vendor's own `/docs/app` link graph still emits both stale forms. Issue #1371's metadata checks out to the field. `repo:lmstudio-ai/docs "settings.json"` really is zero hits. The settings.json/downloadsFolder correction, the `.internal` os-list correction, and the incomplete-but-correct finding on `bin/` and `models/` all hold — and I verified the three lmstudio-js files that carry them, which the agent cited but never listed as fetched.

WHAT I BROKE. Four things.

1. The biggest one: `~/.cache/lm-studio/mcp.json` is NOT vendor-documented, and its confidence must not rise to high. I searched the entire lmstudio-ai org for `mcp.json` — nine hits, all in the docs repo plus one localization string file, and ZERO in `lms` or `lmstudio-js`. `lmstudioPaths.ts` enumerates nine home-relative paths and mcp.json is not among them. So that row is a composition: a vendor-documented home resolution times a filename the vendor only ever stated relative to `~/.lmstudio`, joined by an assumption. Reasonable, but inferred. Labelling the composite "vendor-documented" is the Windsurf failure in a subtler dress.

2. "credentials returns ZERO hits in lmstudio-ai/docs" is false — there is one, "Connecting with your own OAuth credentials" in the remote-MCP integrations page. The conclusion survives, but the miss cuts the wrong way: the vendor documents OAuth against remote MCP servers including dynamic client registration, so those tokens persist somewhere, which makes a credentials store *more* interesting given the entry's `plaintext_credentials: null`.

3. The `.internal/utils/` "bundled node runtime" does not exist in any source cited. The only `new UtilBinary(...)` in all of lmstudio-js is `new UtilBinary("esbuild", ...)`. The directory and the spawn-from-user-writable-path finding survive; the interpreter claim must be dropped.

4. mlx-engine is over-read. `~/.cache/lm-studio/.internal/outlines` is a hardcoded literal, not `findLMStudioHome()`-relative, so it does not show the cache home is live on that host — and it inverts into a false-positive trap worth recording: on macOS the presence of `~/.cache/lm-studio` is not by itself proof it is the LM Studio home. Only the pointer file settles that.

Smaller corrections: issue #1813's user was describing an attempted workaround location ("Adding the entries here … does not solve it either"), not a found mcp.json, and "SystemUser" reads as a placeholder — weak corroboration, and the cross-platform conclusion needs none of it. The pointer file's "present on essentially every host" is inference; the write sits in the CLI/SDK resolver, so absence proves nothing on a GUI-only host. `config-presets` was rendered with a spurious trailing slash and its Windows form dropped. `import-model.md` carries no OS label at all, so "documented for Mac" overstates it. The macOS shell-profile evidence for `~/.cache/lm-studio/bin` came with no URL and I could not verify it — drop it; `installCli/index.ts` carries that conclusion alone.

PROCESS DEFECT. `pages_fetched` omits at least seven sources the submission actually relies on. I fetched all seven and every one checks out, so this is hygiene rather than fabrication — but an unauditable citation list is the same class of problem as a citation that moves.

Eleven new rows carried forward with corrected exact strings, led by `~/.lmstudio-home-pointer`. Two additions of my own from sources the prior pass opened but under-read: `llmster-install-location.json` is not merely a locator — `findOrStartLlmster` spawns whatever path it contains, on Windows via `powershell.exe Start-Process -WindowStyle Hidden`, and prefers llmster over the app whenever both are present, making it a persistence and hidden-execution primitive; and 0.4.x ships API tokens with a default-off "Require Authentication" toggle that this entry does not model at all.

No files were modified.

---
